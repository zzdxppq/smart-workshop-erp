# Story 13.1 Review - InspectionDTO schema 补齐 + InspectionCreate.vue Option A 切换

> **Reviewer**：architect 鲁班
> **Date**：2026-06-14
> **Sprint**：V1.3.9 Sprint 13
> **Verdict**：🟢 **APPROVED**（可进入 dev IMPL · 附 3 条 IMPL 注意事项）

---

## 1. 契约评审

### 1.1 端点契约总览

| 项 | 评审结论 |
|----|----------|
| 端点数 | **1**（`POST /api/v1/quality/inspections`）✅ |
| 新增 schema | **4**（`InspectionDTO` / `InspectionCreateRequest` / `InspectionItemDTO` / `InspectionResponse`）✅ |
| tag | `E7-Quality`（与既有 quality tag 一致）✅ |
| security | `bearerAuth: []`（与全平台 JWT 一致）✅ |
| operationId | `createInspectionV1389`（V1.3.9 版本标记）✅ |
| Flyway | 0 迁移（pure schema 调整）✅ |
| AC 数 | 3 个主 AC ✅ |
| 测例 | 0 业务测例 · 8 验证测例（typecheck + grep）✅ |

### 1.2 `POST /api/v1/quality/inspections` 契约

| 维度 | 评审 |
|------|------|
| HTTP 方法 | `POST`（创建语义）✅ |
| Path | `/api/v1/quality/inspections`（v1 prefix · Quality 命名空间）✅ |
| requestBody | `application/json` + `$ref InspectionCreateRequest` ✅ |
| 201 响应 | `$ref InspectionResponse`（含 inspectionId + inspectionNo + status + createdAt）✅ |
| 400 错误 | `INVALID_FIELD`（code 40001 · `details.field` 字段名）✅ |
| 403 错误 | `QC_REQUIRED`（非 QC 角色）✅ |
| 409 错误 | `INSPECTION_DUPLICATE`（物料码已被检验）✅ |
| ErrorResponse schema | 复用既有 `$ref '#/components/schemas/ErrorResponse'`（避免重复定义）✅ |

### 1.3 InspectionDTO schema（V1.3.9 增量 · 完整 DTO）

| 字段 | 类型 | 必填 | nullable | 说明 | 对齐来源 |
|------|------|------|----------|------|----------|
| `id` | integer(int64) | ❌ | ❌ | 主键 | DB PK |
| `inspectionNo` | string | ❌ | ❌ | 检验单号 `INSP-yyyyMMdd-NNNN` | V1.3.7 1.28 |
| `materialCode` | string | ❌ | ❌ | 物料码 `WL-...` 前缀 | V1.3.7 1.28 |
| `materialName` | string | ❌ | ✅ | 物料名 · `mtl_material.name` 关联 | V1.3.7 既有 |
| `batchNo` | string | ❌ | ✅ | 批次号 · `wms_inbound.batch_no` 关联 | V1.3.7 1.29 |
| `inspectionType` | string enum | ❌ | ❌ | `INCOMING/IN_PROCESS/OUTGOING/FA/CMM` · V1.3.7 1.28-1.30 5 类 | V1.3.7 1.28/1.29/1.30 |
| `qualityStatus` | string enum | ❌ | ❌ | `PASS/FAIL/PENDING/REWORK` | Sprint 1.4 MaterialBarcodeParseResponse |
| `inspectItems` | array[InspectionItemDTO] | ❌ | ❌ | 检验项明细 | V1.3.7 既有 |
| `inspectorUserId` | integer(int64) | ❌ | ❌ | 检验人 userId | V1.3.7 既有 |
| `inspectorName` | string | ❌ | ✅ | 检验人姓名 · `sys_user.full_name` | V1.3.7 既有 |
| `inspectionDate` | string(date-time) | ❌ | ❌ | 实际检验日期 | V1.3.7 既有 |
| `actualInspectionDate` | string(date-time) | ❌ | ✅ | 实际检验完成时间（进行中为空）| V1.3.7 既有 |
| `remark` | string(maxLength 500) | ❌ | ✅ | 备注 | V1.3.7 既有 |
| `tenantId` | integer(int64) | ❌ | ❌ | 多租户隔离（**必含**）| V1.3.7 既有 |
| `createdAt` | string(date-time) | ❌ | ❌ | 审计字段 | 平台约定 |
| `updatedAt` | string(date-time) | ❌ | ❌ | 审计字段 | 平台约定 |

> **字段顺序遵循 V1.3.7 既有 DDL**：id → 业务字段 → 审计字段 · 不破坏既有契约

### 1.4 InspectionCreateRequest schema（POST 请求体）

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `materialCode` | string | ✅ | — | 物料码（必填）|
| `inspectionType` | string enum | ✅ | INCOMING/IN_PROCESS/OUTGOING/FA/CMM | 检验类型 |
| `qualityStatus` | string enum | ✅ | PASS/FAIL/PENDING/REWORK | 质检结果 |
| `inspectItems` | array[InspectionItemDTO] | ✅ | minItems: 1 | 至少 1 个检验项 |
| `remark` | string | ❌ | maxLength: 500 · nullable | 备注 |

> required: `[materialCode, inspectionType, qualityStatus, inspectItems]` · 4 个必填字段对齐 V1.3.7 既有契约

### 1.5 InspectionItemDTO schema（检验项明细）

| 字段 | 类型 | 必填 | nullable | 说明 |
|------|------|------|----------|------|
| `itemName` | string(maxLength 50) | ❌ | ❌ | 检验项名称（"外观"/"尺寸"/"硬度"）|
| `standard` | string(maxLength 200) | ❌ | ✅ | 检验标准 |
| `measuredValue` | string(maxLength 100) | ❌ | ✅ | 实测值 |
| `result` | string enum | ❌ | ❌ | `OK/NG/NA` · 单项结果 |
| `sortOrder` | integer | ❌ | ❌ | default 0 |

### 1.6 InspectionResponse schema（201 响应体）

| 字段 | 类型 | 必填 | nullable | 说明 |
|------|------|------|----------|------|
| `inspectionId` | integer(int64) | ❌ | ❌ | 检验单 ID |
| `inspectionNo` | string | ❌ | ❌ | 检验单号 |
| `status` | string enum | ❌ | ❌ | `CREATED/REJECTED` |
| `createdAt` | string(date-time) | ❌ | ❌ | 创建时间 |

> 注：响应 schema 故意保持精简（不返回 InspectionDTO 全字段）· 客户端如需完整数据可调 GET 端点（未来 Story）

### 1.7 错误码契约

| HTTP | code | 含义 | 触发条件 |
|------|------|------|----------|
| 201 | — | CREATED | 检验单创建成功 |
| 400 | 40001 INVALID_FIELD | 字段校验失败 | `details.field` 指明字段名（materialCode / inspectItems 等）|
| 403 | 40301 QC_REQUIRED | 无 QC 权限 | 非 QC 角色调用 |
| 409 | 40901 INSPECTION_DUPLICATE | 物料码已被检验 | 同物料码已有未关闭检验单 |

---

## 2. 技术评审

### 2.1 openapi.yaml 改动

| # | 改动 | 位置 | 评审 |
|---|------|------|------|
| 1 | 新增 `InspectionDTO` schema | `components.schemas` | 🟢 完整 DTO · 字段对齐 V1.3.7 |
| 2 | 新增 `InspectionCreateRequest` schema | `components.schemas` | 🟢 POST 请求体 · 4 required 字段 |
| 3 | 新增 `InspectionItemDTO` schema | `components.schemas` | 🟢 嵌套数组 items |
| 4 | 新增 `InspectionResponse` schema | `components.schemas` | 🟢 精简响应（4 字段）|
| 5 | 新增 `POST /quality/inspections` endpoint | `paths` | 🟢 E7-Quality tag |
| 6 | tag 命名 | `E7-Quality` | 🟢 与既有 quality tag 一致（注意：Story 标注 `E7QualityService` · 实际 tag 为 `E7-Quality`）|

### 2.2 codegen regen 验证

| # | 验证项 | 期望 | 评审 |
|---|--------|------|------|
| 1 | `npm run gen:api` 退出码 | 0 | 🟢 |
| 2 | `InspectionDTO.ts` 生成 | 存在 | 🟢 |
| 3 | `InspectionCreateRequest.ts` 生成 | 存在 | 🟢 |
| 4 | `InspectionItemDTO.ts` 生成 | 存在 | 🟢 |
| 5 | `InspectionResponse.ts` 生成 | 存在 | 🟢 |
| 6 | `QualityService.ts` 包含 `createInspectionV1389` | 存在 | 🟢 |
| 7 | `git diff --exit-code src/api/generated` | 0 | 🟢 baseline 稳定 |

### 2.3 web-impl Option A 切换（10.5 占位替换）

| # | 改动 | 评审 |
|---|------|------|
| 1 | 删除本地 `InspectionFormLocal` interface | 🟢 |
| 2 | 删除 `interface.*Local` 模式（grep 0 命中）| 🟢 |
| 3 | `import type { InspectionCreateRequest, InspectionItemDTO } from '@/api/generated/models'` | 🟢 强类型 import |
| 4 | `const form = ref<InspectionCreateRequest>({ ... })` | 🟢 |
| 5 | `const inspectItems = ref<InspectionItemDTO[]>([])` | 🟢 |
| 6 | 业务逻辑保持不变 | 🟢 仅替换类型 · 不改业务 |

### 2.4 多端影响

| 端 | 13.1 涉及 | 评审 |
|----|----------|------|
| web-impl | ✅ InspectionCreate.vue Option A 切换 | 🟢 |
| android-impl | ❌ 暂不涉及（web 端优先）| 🟢 |

### 2.5 typecheck 门禁

| # | 验证项 | 期望 | 评审 |
|---|--------|------|------|
| 1 | `npm run typecheck`（vue-tsc）| 退出码 0 | 🟢 |
| 2 | `npm run typecheck:ci`（含 `git diff --exit-code src/api/generated`）| 退出码 0 | 🟢 |
| 3 | `npm run build` | 退出码 0 | 🟢 |

---

## 3. 集成评审

### 3.1 上游依赖

| Story | 状态 | 13.1 关系 | 评审 |
|-------|------|----------|------|
| Sprint 10.1 codegen baseline | ✅ Shipped | 132 端点 + generated/ 基础 | 🟢 提供 regen 基础设施 |
| Sprint 10.5 InspectionCreate.vue 占位 | ✅ Shipped | Option A 本地 interface 已落 | 🟢 提供替换对象 |
| Sprint 13.5 enum drift 对齐 | ✅ Shipped（必须）| 7 状态机对齐 · regen baseline 稳定 | 🟢 串行前置 |
| V1.3.7 1.28/1.29/1.30 检验表 | ✅ Shipped | 数据契约对齐基础 | 🟢 字段对齐基准 |
| Sprint 1.4 MaterialBarcodeParseResponse | ✅ Shipped | `qualityStatus` 4 值对齐 | 🟢 enum 来源 |

### 3.2 下游影响

| 模块 | 13.1 影响 | 评审 |
|------|----------|------|
| web-impl InspectionCreate.vue | 享受 generated 强类型 · 后端拼写错误前端拦截 | 🟢 PM 反馈闭环 |
| web-impl 质量检验页 5 处入口 | 同步切换为 generated 类型 | 🟡 需逐一替换（见 IMPL 注意事项 #3）|
| web-impl codegen baseline | 4 文件新增 · diff 稳定 | 🟢 |
| android-impl 品质 Fragment | ❌ 暂不涉及 | 🟢 web 端优先 |
| 后端 Java Controller（未来 Story）| 仅契约发布 · 13.1 不实现 Controller | 🟢 纯契约对齐 |

### 3.3 A 组串行约束

| 约束 | 说明 |
|------|------|
| **A 组 parallel_group** | 13.1 与 13.5 同属 A 组 |
| **顺序约束** | **13.5 必须先 ship** · 13.1 才能启动 |
| **理由** | 13.5 enum drift 对齐后 · codegen baseline 稳定 · InspectionDTO 引用 enum 不漂移 |
| **协调人** | SM 萧何 |
| **当前状态** | 13.5 ✅ Shipped · 13.1 可启动 |

### 3.4 双向契约保护

| 方向 | 保护机制 |
|------|----------|
| 后端 → 前端 | `openapi.yaml` 是唯一权威源 · codegen 自动同步 |
| 前端 → 后端 | `typecheck:ci` 拦截拼写错误 · `git diff gate` 拦截 baseline 漂移 |
| 强类型消费 | `InspectionCreateRequest` 类型贯穿 form ref + submit + error handler |

---

## 4. 测试评审

### 4.1 测例总览

| 测例组 | 数量 | 验证方式 |
|--------|------|----------|
| TC-13.1.1 codegen baseline | 3 | gen:api 退出 0 + 4 文件存在 + diff exit 0 |
| TC-13.1.2 typecheck 严格化 | 3 | typecheck 退出 0 + typecheck:ci 退出 0 + grep 0 命中 |
| TC-13.1.3 端点契约对齐 | 2 | service method 存在 + openapi 端点定义存在 |
| **合计** | **8 验证测例** | — |

> 注：0 业务功能测例（pure schema 调整 · 与 Sprint 10.5 验证风格一致）

### 4.2 8 验证项详表

| # | ID | 验证项 | 步骤 | 期望退出码 |
|---|----|--------|------|----------|
| 1 | TC-13.1.1.1 | codegen 编译通过 | `cd web-impl && npm run gen:api` | 0 |
| 2 | TC-13.1.1.2 | 4 文件生成存在 | `ls src/api/generated/models/Inspection*.ts` | 4 文件 |
| 3 | TC-13.1.1.3 | diff 无意外漂移 | `git diff --exit-code src/api/generated` | 0 |
| 4 | TC-13.1.2.1 | vue-tsc 严格化 | `npm run typecheck` | 0 |
| 5 | TC-13.1.2.2 | typecheck:ci 双重门禁 | `npm run typecheck:ci` | 0 |
| 6 | TC-13.1.2.3 | 本地 interface 清零 | `grep -rn 'InspectionFormLocal' web-impl/src/` | 0 命中 |
| 7 | TC-13.1.3.1 | service method 生成 | `grep createInspectionV1389 src/api/generated/services/` | 命中 |
| 8 | TC-13.1.3.2 | openapi 端点定义存在 | `grep "POST.*inspections" backend/spec/openapi.yaml` | 命中 |

### 4.3 grep 验证清单

| grep 模式 | 期望 | 用途 |
|-----------|------|------|
| `InspectionFormLocal` | 0 命中 | 本地 interface 已删 |
| `interface.*Local`（web-impl/src/views/quality/）| 0 命中 | Option A 全清 |
| `MaterialBarcodeParseResponse.qualityStatus`（复用代码）| 0 命中 | 复用代码已删（仅在 InspectionCreate.vue 内）|
| `createInspectionV1389` | 命中 | codegen service method |
| `InspectionDTO` | 命中 | schema 定义 |

---

## 5. 风险评审

| # | 风险 | 等级 | 缓解措施 |
|---|------|------|----------|
| 1 | InspectionDTO 字段与 V1.3.7 1.28/1.29/1.30 既有端点不一致 | 🟡 中 | dev 启动前 `git log backend/src/main/java/.../quality/` 同步字段 · architect 评审把关 · 本评审 §1.3 字段对齐表已列全字段 |
| 2 | 13.5 未先 ship 导致 codegen baseline 不稳 | 🟡 中 | **顺序约束不可逆**：13.5 → 13.1 · SM 萧何协调启动 · 13.5 已 ✅ Shipped |
| 3 | web-impl InspectionCreate.vue 替换范围超出预期 | 🟢 低 | 仅替换 `import type` + `ref<...>` · 不改业务逻辑 · dev 严格遵循 AC-13.1.3 |
| 4 | 与 Sprint 10.5 typecheck 4 错误冲突 | 🟢 低 | 13.5 已修完 enum drift + 10.5 typecheck 错误 · 13.1 落地后整体 typecheck:ci 通过 |
| 5 | 多租户 tenantId 字段遗漏 | 🟡 中 | V1.3.7 既有 1.28 已有 tenantId · InspectionDTO 必含 · architect 评审确认（本评审 §1.3 已标注）|
| 6 | codegen baseline 漂移 | 🟢 低 | `git diff --exit-code src/api/generated` 拦截 · typecheck:ci 内置 gate |
| 7 | 5 处 web-impl 检验页同步切换遗漏 | 🟡 中 | IMPL 注意事项 #3 强调 · dev 启动前 `grep -rln "interface.*Local" web-impl/src/views/quality/` 全量盘点 |

---

## 6. IMPL 注意事项（3 条）

### 注意事项 #1：10.5 InspectionCreate.vue 切换前必跑 typecheck

**目的**：验证切换前 baseline 干净（无 Sprint 10.5 typecheck 遗留错误）

**步骤**：
```bash
cd web-impl
npm run typecheck   # 必须退出 0 才能启动 IMPL
```

**失败处理**：若 baseline 不干净 · 先回 13.5 / 10.5 修复 · 再启动 13.1

### 注意事项 #2：删除本地 interface 后 grep 验证 0 命中

**目的**：确保 Option A 临时方案彻底清除 · 不留残留

**步骤**：
```bash
# 验证 InspectionFormLocal 全清
grep -rn 'InspectionFormLocal' web-impl/src/   # 期望 0 命中

# 验证 interface.*Local 模式（Option A 通用 pattern）
grep -rn 'interface.*Local' web-impl/src/views/quality/   # 期望 0 命中

# 验证 MaterialBarcodeParseResponse.qualityStatus 复用代码删除
grep -rn 'MaterialBarcodeParseResponse.qualityStatus' web-impl/src/views/quality/InspectionCreate.vue   # 期望 0 命中
```

### 注意事项 #3：5 处 web-impl 检验页同步切换

**目的**：PM 反馈闭环 · 享受 codegen 强类型保护覆盖全部 quality 视图

**步骤**：
```bash
# 1. 全量盘点本地 interface 使用点
grep -rln 'interface.*Local' web-impl/src/views/quality/

# 2. 列出 quality 视图清单
ls web-impl/src/views/quality/*.vue

# 3. 逐一替换为 generated 类型
# InspectionCreate.vue (本次 AC-13.1.3 强制)
# 4 处其他 .vue 同步切换（不强制 · 但建议作为 IMPL 收口）
```

**交付物**：
- AC-13.1.3 强制完成 InspectionCreate.vue 切换 ✅
- 4 处其他 .vue 列入 13.1 IMPL 范围（**建议**）或回退为后续 Story（**兜底**）
- 整体 `npm run typecheck:ci` 退出 0 即可 ship

---

## 7. Verdict

🟢 **APPROVED** · 可进入 dev IMPL

| 维度 | 结论 |
|------|------|
| 契约评审 | 🟢 通过 · 1 端点 + 4 schema · 字段对齐 V1.3.7 |
| 技术评审 | 🟢 通过 · openapi 改动 + codegen + Option A 切换路径清晰 |
| 集成评审 | 🟢 通过 · A 组串行约束 13.5→13.1 已满足 |
| 测试评审 | 🟢 通过 · 8 验证测例充分（typecheck + grep）|
| 风险评审 | 🟢 通过 · 7 项风险已识别 · 关键缓解到位 |
| IMPL 注意事项 | 3 条已附 · 见 §6 |
| Flyway | 0 迁移 ✅ |
| 端点 | 1（`POST /api/v1/quality/inspections`）✅ |
| Schema | 4（InspectionDTO / InspectionCreateRequest / InspectionItemDTO / InspectionResponse）✅ |

---

## 8. 签字

**architect 鲁班** · 2026-06-14 · **APPROVED**

---

## 附录 A · A 组串行约束（强调）

| 项 | 说明 |
|----|------|
| parallel_group | **A 组**（与 13.5 同组）|
| 顺序约束 | **13.5 → 13.1 不可逆** |
| 当前状态 | 13.5 ✅ Shipped · 13.1 可启动 |
| 协调人 | SM 萧何 |
| 阻塞 | **无**（前置条件全部满足）|

## 附录 B · 字段对齐 V1.3.7 既有端点（核对清单）

| 13.1 InspectionDTO 字段 | V1.3.7 来源 | 是否一致 |
|------------------------|-------------|----------|
| `inspectionType` 5 类（INCOMING/IN_PROCESS/OUTGOING/FA/CMM）| V1.3.7 1.28/1.29/1.30 | ✅ |
| `qualityStatus` 4 值（PASS/FAIL/PENDING/REWORK）| Sprint 1.4 MaterialBarcodeParseResponse | ✅ |
| `inspectItems` 嵌套数组 | V1.3.7 既有 | ✅ |
| `tenantId` 多租户隔离 | V1.3.7 既有 1.28 | ✅ |
| `actualInspectionDate` nullable | V1.3.7 既有（进行中为空）| ✅ |
| `inspectionNo` 格式 `INSP-yyyyMMdd-NNNN` | V1.3.7 既有 | ✅ |

---

**Story 13.1 Review 完毕 · APPROVED · 等待 dev agent Opus 4.8 启动 IMPL**