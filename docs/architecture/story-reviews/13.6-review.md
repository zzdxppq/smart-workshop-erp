# Story 13.6 Review - 7 角色 connectedAndroidTest E2E 补齐（Story 1.4 pending_deploy 收口）

> **Reviewer**：architect 鲁班
> **Date**：2026-06-14
> **Sprint**：V1.3.9 Sprint 14 · P0 必做（原 Sprint 13 P2 候选 · V1.3.9 客户反馈后升级为 P0 · 12.1 灰度阶段 4 OPERATOR 协同前置）
> **Story 文件**：`backend/docs/stories/sprint14/14.1-7role-android-e2e.md`
> **关联 PM 决策**：Story 1.4 pending_deploy 第 1/2 项（`core-config.yaml` `assigned_stories` L44-47）
> **Verdict**：🟢 **APPROVED**（Sprint 14 P0 必做 · 启动约束：13.7 ship 后启动）

---

## 1. 契约评审

| 项 | 评审结论 |
|----|----------|
| 任务类型 | **connectedAndroidTest E2E 补齐** · 0 业务端点 · 22 E2E 测例（纯测试增量）· 0 Flyway ✅ |
| 改动范围 | `android-impl/src/androidTest/kotlin/com/btsheng/erp/e2e/`（新增 2 文件 · RoleBasedE2ETest + DrawPermissionE2ETest）|
| 上游消费 | 既有 backend 端点（V1.3.7 + V1.3.8 + V1.3.9 Sprint 12 全部端点）· 0 端点改动 ✅ |
| 下游产出 | Story 1.4 pending_deploy 关闭（android-impl 仓 UI 实施 + 8 角色 E2E 覆盖）|
| 验收方式 | `gradle connectedAndroidTest` 退出码 0（22 测例 PASS）|

### 1.1 0 业务端点

- 0 业务端点改动（保留 V1.3.8 既有契约）
- 0 Flyway 迁移（不动 DB schema）
- 0 codegen 触发（仅消费既有 openapi.yaml）

### 1.2 22 E2E 测例（7 角色 × 3-4 关键链路）

- TC-13.6.1：7 角色 E2E · 14 测例（7 角色 × 2 测例 = 1 流程 + 1 拦截）+ FINANCE 验证 2 测例
- TC-13.6.2：connectedAndroidTest 配置 · 2 测例（deps + 7 角色账号 seed）
- TC-13.6.3：Story 12.1 DrawPermissionInterceptor 集成 · 6 测例
- **合计 22 E2E 测例**

### 1.3 connectedAndroidTest 框架

- Test runner：`androidx.test.runner.AndroidJUnitRunner`（标准 JUnit4）
- Page Object 模式：与 Sprint 8.5 + Sprint 10.2 命名规范保持一致
- 设备要求：Android Studio AVD（API 30+）或物理设备（Android 11+）

---

## 2. 技术评审

### 2.1 android-impl 测试目录

```
android-impl/
├── src/
│   ├── androidTest/
│   │   ├── kotlin/com/btsheng/erp/
│   │   │   ├── e2e/
│   │   │   │   ├── RoleBasedE2ETest.kt          (新增 · 7 角色主流程)
│   │   │   │   └── DrawPermissionE2ETest.kt     (新增 · 12.1 拦截器集成)
│   │   │   ├── page/                            (复用 8.5/10.2 Page Object)
│   │   │   │   ├── LoginPage.kt
│   │   │   │   ├── MainMenuPage.kt
│   │   │   │   ├── ScanPage.kt
│   │   │   │   ├── DrawingPreviewPage.kt
│   │   │   │   └── ...
│   │   │   └── util/
│   │   │       ├── TestAccount.kt               (7 角色 + FINANCE = 8 账号)
│   │   │       └── AdbUtils.kt                  (扫码模拟)
│   │   └── ...
│   └── main/                                    (不动)
├── build.gradle.kts                              (含 androidTestImplementation)
```

### 2.2 7 角色 × 3-4 关键链路

| 角色 | login_user_id | 测例数 | 关键链路 |
|------|--------------|--------|----------|
| **ENGINEER** | 1001 | 4 | 登录 → 图纸列表 → 详情预览 → 打印 → 退出 |
| **PROD_PLANNER** | 1002 | 3 | 登录 → 工单列表 → 工序排产 → 看板 → 退出 |
| **SALES** | 1003 | 4 | 登录 → 报价新建 → 提交审批 → 报价转订单 → 退出 |
| **PURCHASER** | 1004 | 3 | 登录 → 询价 → 中标 → 采购单 → 退出 |
| **WAREHOUSE** | 1005 | 3 | 登录 → 入库扫码 → 库位分配 → 出库 → 退出 |
| **QC** | 1006 | 2 | 登录 → 检验列表 → 检验录入 → 退出 |
| **OPERATOR** | 1007 | 3 | 登录 → 工单扫码 → 开工报工 → 工序过站 → 退出 |
| **合计** | — | **22** | — |

> 注：Story 文件 4.1 节列了 7 角色测试账号 + FINANCE（1008）共 8 账号；22 测例分布在 7 业务角色 + 配置/集成测例中。GM/ADMIN 不参与（按 Story 1.4 约定）。

### 2.3 Page Object 复用

- 与 Sprint 8.5（viewBinding/connectedAndroidTest 雏形）+ Sprint 10.2（Page Object 沉淀）保持命名一致
- 复用既有 `LoginPage` / `MainMenuPage` / `ScanPage` / `DrawingPreviewPage` 等
- 新增页面按既有命名规范扩展（如 `QuoteCreatePage` / `InquiryListPage` / `InspectionEntryPage`）

### 2.4 Test Runner

- `androidx.test.runner.AndroidJUnitRunner`（标准 JUnit4 + Espresso）
- `@LargeTest` 注解标记（E2E 长流程）
- `@FlakyTest` 注解覆盖网络/扫码时序敏感测例
- Test orchestrator：androidx.test.orchestrator（自动清理状态）

### 2.5 设备要求

- **首选**：Android Studio AVD（API 30+ · Pixel 5 镜像）
- **次选**：物理设备（USB 调试 · Android 11+）
- **沙箱限制**：本沙箱无法直接执行 connectedAndroidTest，委托 DevOps 张良执行

---

## 3. 集成评审

### 3.1 上游依赖

| 依赖项 | 来源 Story | 状态 | 13.6 用途 |
|--------|-----------|------|----------|
| APP 端基础（登录/消息/扫码壳）| Story 1.4 | ✅ ship | E2E 登录流程 + 扫码入口 |
| android-impl layout/viewBinding/connectedAndroidTest | Story 8.5 | ✅ ship | 测试目录 + Page Object 基线 |
| OPERATOR 灰度阶段 4 IMPL | Story 12.1 | ✅ ship | OPERATOR 工序关联 E2E 验证 |
| `DrawPermissionInterceptor` 拦截器 | Story 12.1 | ✅ ship | 7 角色权限拦截验证 |
| 7 角色 JWT（V1.3.7 1.1）| Story 1.1 | ✅ ship | 测试账号权限（每角色 1 个账号）|
| backend 各端点契约 | V1.3.7 + V1.3.8 | ✅ stable | E2E 数据驱动（HTTP 请求）|
| web-impl typecheck 收口真实数据 | Story 13.7 | ⏳ 待 ship | 13.6 IMPL 启动前置 |

### 3.2 下游影响

| 影响项 | 评估 |
|--------|------|
| 1.4 pending_deploy 关闭 | ✅ 第 1/2 项（android-impl 仓 UI 实施 + 8 角色 E2E）关闭 |
| android-impl 8 角色 E2E 覆盖 | ✅ 从 0 提升到 22 测例 |
| V1.3.9 客户上线支持 | ✅ 提供 E2E 回归基线 · 客户机房验收 |
| 12.1 灰度阶段 4 启动 | ✅ OPERATOR 工序关联 E2E 验证（不可逆前置）|
| 后续 Story 12.x 沉淀 | ✅ DrawPermissionInterceptor E2E 沉淀为回归基线 |

---

## 4. 测试评审

### 4.1 22 E2E 测例分布

| 测例组 | 数量 | 验证方式 | 占比 |
|--------|------|----------|------|
| TC-13.6.1.1 ~ 1.14 | 14 | 7 角色 × 2 测例（1 流程 + 1 拦截）| 64% |
| TC-13.6.1.15 ~ 1.16 | 2 | FINANCE 验证（菜单不可见 + deep link 拦截）| 9% |
| TC-13.6.2.1 ~ 2.2 | 2 | connectedAndroidTest 配置（deps + seed）| 9% |
| TC-13.6.3.1 ~ 3.6 | 6 | DrawPermissionInterceptor 集成（6 角色拒绝 + 灰度 flag + 异常路径）| 18% |
| **合计** | **22** | — | **100%** |

### 4.2 测例详细分组

| 角色 | 主流程测例 | 拦截测例 | 备注 |
|------|-----------|---------|------|
| ENGINEER | TC-13.6.1.1（5 全成功）| TC-13.6.1.2（admin 拒绝 40301）| 最高权限角色 |
| PROD_PLANNER | TC-13.6.1.3（2 全成功）| TC-13.6.1.4（下载 403）| 工单视角 |
| SALES | TC-13.6.1.5（order 关联 200）| TC-13.6.1.6（order 不关联 40321）| 订单关联 |
| PURCHASER | TC-13.6.1.7（PO 关联 200）| TC-13.6.1.8（PO 不关联 40321）| PO 关联 |
| WAREHOUSE | TC-13.6.1.9（入库单关联 200）| TC-13.6.1.10（不关联 40321）| 入库单关联 |
| QC | TC-13.6.1.11（质检单关联 200）| TC-13.6.1.12（不关联 40321）| 质检单关联 |
| OPERATOR | TC-13.6.1.13（process 关联 200）| TC-13.6.1.14（process 不关联 40322）| 工序关联 |
| FINANCE | TC-13.6.1.15（菜单不可见）| TC-13.6.1.16（deep link 40320）| 零权限角色 |

### 4.3 P0 必做定位

- **Sprint 14 重新激活**：原 Sprint 13 P2 候选 backlog · 升级为 P0 必做
- **12.1 灰度阶段 4 协同前置**：OPERATOR 工序关联 E2E 验证 · 不可逆
- **启动约束**：13.7 baseline typecheck 收口后启动（顺序约束）

### 4.4 4 Worker Parallel + 期望时长

- 4 worker parallel（device 池：4 AVD 实例或 4 物理设备）
- 期望 ≤ 5min（22 测例分布 + Test orchestrator 状态清理）
- 沙箱受限：委托 DevOps 张良全权执行 connectedAndroidTest

---

## 5. 风险评审

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| 1 | 物理设备/AVD 不稳定（API 30+ 兼容性）| 🟡 中 | DevOps 张良 AVD 准备 + 物理设备备份 |
| 2 | 22 测例 CI 串行耗时（expected > 10min）| 🟡 中 | 4 worker parallel + Test orchestrator · 期望 ≤ 5min |
| 3 | 端点漂移（V1.3.9 后续迭代可能改契约）| 🟢 低 | codegen 类型保护 + E2E 截图回归 |
| 4 | 13.6 沙箱受限（沙箱无 Android SDK）| 🟢 低 | DevOps 张良全权执行 connectedAndroidTest |
| 5 | E2E flaky（网络/扫码/UI 渲染时序）| 🟡 中 | `@FlakyTest` 注解 + 重试机制 + Test orchestrator |
| 6 | 7 角色测试账号 seed（生产环境无）| 🟢 低 | staging 环境 + admin 创建（不在 prod seed）|

---

## 6. IMPL 注意事项（3 条）

### 6.1 Android Studio AVD 准备

- **DevOps 张良** + **客户机房** 联动
- AVD 配置：Pixel 5 · API 30+ · 8GB RAM
- 物理设备备份：USB 调试 Android 11+ 备用
- 沙箱限制：沙箱无 Android SDK，DevOps 张良全权执行 connectedAndroidTest

### 6.2 Page Object 复用

- 与 Sprint 8.5（viewBinding/connectedAndroidTest 雏形）+ Sprint 10.2（Page Object 沉淀）保持命名一致
- 复用既有 `LoginPage` / `MainMenuPage` / `ScanPage` / `DrawingPreviewPage`
- 新增页面按既有命名规范扩展（`QuoteCreatePage` / `InquiryListPage` / `InspectionEntryPage`）
- 严禁重复造轮子（命名漂移会破坏 Page Object 沉淀价值）

### 6.3 CI 配置

- GitLab Runner 预装 Android SDK（API 30+）+ `gradle connectedAndroidTest` 脚本
- 4 worker parallel（device 池：4 AVD 实例或 4 物理设备）
- Test orchestrator 启用（自动清理 app 状态）
- 期望时长：22 测例 ≤ 5min（4 worker parallel）

---

## 7. Verdict

🟢 **APPROVED** · 可进入 dev IMPL（13.7 ship 后启动）

- 0 端点改动（pure test 增量）
- 22 E2E 测例覆盖（7 角色 × 2-4 + FINANCE 2 + 配置 2 + 集成 6）
- 风险 6 项已识别（4 项 🟢 低 + 2 项 🟡 中）
- **启动约束**：13.7 baseline typecheck 收口后启动（web-impl 真实数据前置）
- **Sprint 14 P0 必做** · 12.1 灰度阶段 4 OPERATOR 协同前置 · 不可逆

---

## 8. 签字

**architect 鲁班** · 2026-06-14 · **APPROVED**

> 关联签字：PO 范蠡（Sprint 14 P0 SHARD ✅）· SM 萧何（Sprint 14 IMPL 启动 · 顺序约束：13.7 先行）· DevOps 张良（AVD/物理设备就位 · 沙箱受限全权执行）· dev agent Opus 4.8（待启动 · 13.7 ship 后）· QA 商鞅（待 22 测例 test-execute）
