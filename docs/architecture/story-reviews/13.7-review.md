# Story 13.7 · 架构评审（arch REVIEW）

> **Story ID**：13.7
> **标题**：baseline-typecheck-fix 5 错误收口
> **优先级**：🔴 P0 · **复杂度**：S · **工时**：0.5-1 天
> **仓库**：backend（openapi.yaml）+ web-impl（codegen regen + typecheck）
> **端点**：0（补 codegen 链路） · **测例**：0（typecheck:ci 验证 · 0 业务测例）
> **评审日期**：2026-06-14
> **评审人**：architect 鲁班
> **来源依据**：PM 范蠡巡检报告 `docs/orchestrix-pm-audit-2026-06-14.md` §7.1 修补建议 #5
> **前置**：Sprint 10.1 ✅（OpenAPI TypeScript codegen）+ Sprint 12.2 ✅（Printer 端点 ship）+ Sprint 10.5 ✅（.vue any 替换）
> **顺序约束**：**13.7 是 Sprint 14 IMPL 起点** · 所有其他 Story dev IMPL 必须在 13.7 ship 后启动 · **不可逆**
> **关联下游**：12.1 web `<DrawingViewer>` 真实数据接入 · 12.2 admin UI 享受 codegen 强类型 · 12.3 LabelPreview · 12.4 PrintButton · 13.4 仪表盘

---

## 1. 契约评审

### 1.1 端点改动总览

| 维度 | 值 |
|------|-----|
| **业务端点增量** | **0**（pure schema 补齐 · 不新增端点） |
| **openapi.yaml 端点增量** | **20**（6 + 2 + 7 + 1 + 3 + 1 = 20） |
| **openapi.yaml schema 增量** | 7（SysPrinter + PrinterHeartbeatResult + PrinterAvailableResponse + LabelTemplate + InspectionDTO + InspectionResponse + WorkflowEventStats） |
| **Flyway 迁移** | 0（不动 DB schema） |
| **端点实装** | 0（backend 端点已在 Sprint 12-13 ship · openapi.yaml 仅描述补齐） |

### 1.2 openapi.yaml 端点增量清单（按 Story 分组）

| Story | 模块 | 端点数 | 端点清单 |
|-------|------|--------|---------|
| **12.2** | sys_printer（打印机管理）| **6** | `GET /api/v1/printers` · `POST /api/v1/printers` · `PUT /api/v1/printers/{id}` · `DELETE /api/v1/printers/{id}` · `POST /api/v1/printers/{id}/test` · `GET /api/v1/printers/available?type=` |
| **12.3** | label_template（标签模板）| **2** | `GET /api/v1/label-templates` · `POST /api/v1/label-templates/preview` |
| **12.4** | sys_print_log（双模式打印日志）| **7** | `POST /api/v1/print-logs/zpl` · `POST /api/v1/print-logs/pdf-a4` · `GET /api/v1/print-logs` · `GET /api/v1/print-logs/{id}` · `POST /api/v1/print-logs/{id}/replay` · `GET /api/v1/print-logs/statistics` · `GET /api/v1/print-logs/recent` |
| **13.1** | quality inspection（品质检验）| **1** | `POST /api/v1/quality/inspections` |
| **13.3** | drawing link（图纸关联）| **3** | `GET /api/v1/drawings/{id}/links` · `POST /api/v1/drawings/{id}/links` · `DELETE /api/v1/drawings/{id}/links/{linkId}` |
| **13.4** | workflow stats（工作流事件统计）| **1** | `GET /api/v1/workflow/events/stats`（Sprint 10.3 已 ship · 仅补 schema 描述） |
| **合计** | — | **20** | 6 + 2 + 7 + 1 + 3 + 1 |

### 1.3 openapi.yaml schema 增量清单

| Schema | 归属 Story | 用途 | 关键字段 |
|--------|-----------|------|---------|
| **SysPrinter** | 12.2 | 打印机模型 | id / name / type / ip / port / protocol / modelSuggestion / enabled / status / lastHeartbeatAt |
| **PrinterHeartbeatResult** | 12.2 | 心跳测试响应 | status / latencyMs / protocolDetected / error / hint |
| **PrinterAvailableResponse** | 12.2 | 可用打印机查询响应 | printers[] / count |
| **LabelTemplate** | 12.3 | 标签模板模型 | id / name / widthMm / heightMm / fields[] / dpi |
| **InspectionDTO** | 13.1 | 品质检验请求 | materialId / batchNo / inspectorId / result / remarks |
| **InspectionResponse** | 13.1 | 品质检验响应 | id / materialId / batchNo / result / inspectedAt / inspectorName |
| **WorkflowEventStats** | 13.4 | 工作流事件统计响应 | totalCount / byStatus[] / byType[] / recentEvents[] |

### 1.4 契约影响范围

- **运行时影响**：0（已 ship 端点行为不变 · openapi.yaml 仅文档层补齐）
- **codegen 影响**：web-impl `src/api/generated/` 增量 ~20 文件
- **错误码影响**：0（沿用 Sprint 12-13 已 ship 错误码：40901 DUPLICATE_NAME / 50201 PRINTER_OFFLINE 等）

---

## 2. 技术评审

### 2.1 openapi.yaml 改动范围

| Story | 模块 | 端点 | 改动 |
|-------|------|------|------|
| **12.2** | sys_printer | 6 端点 | CRUD + test + available · 含 5 个 admin-only + 1 个 bearer auth |
| **12.3** | label_template | 2 端点 | GET 列表 + POST preview（渲染效果预览） |
| **12.4** | sys_print_log | 7 端点 | zpl + pdf-a4 双模式打印提交 + 日志查询 + 重放 + 统计 |
| **13.1** | quality inspection | 1 端点 | POST 提交检验结果 |
| **13.3** | drawing link | 3 端点 | GET 列表 + POST 新增 + DELETE 删除（图纸关联配置） |
| **13.4** | workflow stats | 1 端点 | `GET /workflow/events/stats`（Sprint 10.3 已 ship） |
| **13.5** | 状态机 enum | enum 调整 | 已 ship（13.7 不动） |

### 2.2 codegen regen 流程

```bash
# 1. 备份现有 generated（IMPL 注意事项 #1）
cp -r src/api/generated src/api/generated.backup

# 2. 删除旧 generated（确保 regen 干净）
rm -rf src/api/generated

# 3. 重新生成（~20 新 model + ~6 service 文件）
npm run gen:api

# 4. 验证 diff（regen 后无未提交变更）
git diff --exit-code src/api/generated

# 5. 验证 typecheck
npm run typecheck:ci

# 6. 验证 build（dist 标题为 V1.3.8）
npm run build
```

### 2.3 codegen 预期生成文件清单（~20 文件）

#### Models（13 个）

| # | 文件 | 归属 Story | 内容 |
|---|------|-----------|------|
| 1 | `models/SysPrinter.ts` | 12.2 | 打印机模型 |
| 2 | `models/PrinterRequest.ts` | 12.2 | 创建/修改请求 |
| 3 | `models/PrinterResponse.ts` | 12.2 | 响应（继承 PrinterRequest）|
| 4 | `models/PrinterListResponse.ts` | 12.2 | 分页响应 |
| 5 | `models/PrinterHeartbeatResult.ts` | 12.2 | 心跳测试响应 |
| 6 | `models/PrinterAvailableResponse.ts` | 12.2 | 可用打印机响应 |
| 7 | `models/LabelTemplate.ts` | 12.3 | 标签模板模型 |
| 8 | `models/SysPrintLog.ts` | 12.4 | 打印日志模型 |
| 9 | `models/PrintLogRequest.ts` | 12.4 | 打印提交请求 |
| 10 | `models/PrintLogStatistics.ts` | 12.4 | 统计响应 |
| 11 | `models/InspectionDTO.ts` | 13.1 | 检验请求 |
| 12 | `models/InspectionResponse.ts` | 13.1 | 检验响应 |
| 13 | `models/WorkflowEventStats.ts` | 13.4 | 工作流统计响应 |

#### Services（6 个）

| # | 文件 | 归属 Story | 方法数 |
|---|------|-----------|--------|
| 1 | `services/PrinterAdminService.ts` | 12.2 | 6（CRUD + test + available）|
| 2 | `services/LabelTemplateService.ts` | 12.3 | 2（list + preview）|
| 3 | `services/PrintLogService.ts` | 12.4 | 7（zpl + pdf-a4 + list + get + replay + stats + recent）|
| 4 | `services/QualityInspectionService.ts` | 13.1 | 1（POST inspections）|
| 5 | `services/DrawingLinkService.ts` | 13.3 | 3（GET/POST/DELETE links）|
| 6 | `services/WorkflowEventStatsService.ts` | 13.4 | 1（GET stats）|

> **注**：12.2 review 文件命名约定为 `AdminPrinterService.ts`，但部分 codegen 工具按 path 拆分会生成 `E12PrinterService.ts`（与 Printers.vue import 路径一致）· dev IMPL 时按实际 codegen 产物调整。

### 2.4 Printers.vue 取消临时 any 绕过度

| 文件 | 行 | 现状（临时绕过） | 13.7 IMPL 后（强类型） |
|------|----|-----------------|---------------------|
| `web-impl/src/views/admin/Printers.vue` | L163 | `import { ... } as any` | `import type { Printer } from '@/api/generated/models/SysPrinter'` |
| `web-impl/src/views/admin/Printers.vue` | L164 | `const service = ... as any` | `const service = new PrinterAdminService(...)` |

### 2.5 GmSummary.vue Dayjs 类型断言

| 文件 | 行 | 现状 | 13.7 IMPL 后 |
|------|----|------|-------------|
| `web-impl/src/views/v138/GmSummary.vue` | L33 | `(data.amountThresholdPassedRate * 100).toFixed(1)` | `(Number(data.amountThresholdPassedRate) * 100).toFixed(1)` |
| `web-impl/src/views/v138/GmSummary.vue` | L47 | `(data.outsourceCostRatio * 100).toFixed(1)` | `(Number(data.outsourceCostRatio) * 100).toFixed(1)` |

> **未来改进**：14.4 strict 化时改 `as Dayjs` 或修正 `GmSummaryResponse` DTO 字段类型（V1.3.10 backlog）· 13.7 接受 `Number()` 强转方案。

### 2.6 vite-env.d.ts 新增

```typescript
// web-impl/src/vite-env.d.ts (新增)
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_BASE_URL: string
  readonly VITE_USE_MOCK: 'true' | 'false'
  readonly VITE_APP_TITLE: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

**覆盖字段**：
- `VITE_API_BASE_URL` — API 基地址
- `VITE_USE_MOCK` — mock 开关（`'true' | 'false'` 字面量类型）
- `VITE_APP_TITLE` — 应用标题（用于 dist 标题）

---

## 3. 集成评审

### 3.1 上游依赖

| 依赖项 | 来源 Story | 13.7 用途 | 状态 |
|--------|-----------|-----------|------|
| OpenAPI TypeScript codegen 集成 | Sprint 10.1 ✅ | 13.7 regen 触发器 | 已 ship |
| `sys_printer` 表 + 6 端点 backend 实装 | Sprint 12.2 ✅ | openapi.yaml 端点 1-6 描述补齐 | 已 ship（V55 Flyway） |
| `label_template` 表 + 2 端点 backend 实装 | Sprint 12.3 ✅ | openapi.yaml 端点 7-8 描述补齐 | 已 ship（V56 Flyway） |
| `sys_print_log` 表 + 7 端点 backend 实装 | Sprint 12.4 ✅ | openapi.yaml 端点 9-15 描述补齐 | 已 ship（V57 Flyway） |
| `quality_inspection` 端点 | Sprint 13.1 ✅ | openapi.yaml 端点 16 描述补齐 | 已 ship |
| `drawing_link` 端点 | Sprint 13.3 ✅ | openapi.yaml 端点 17-19 描述补齐 | 已 ship |
| `workflow/events/stats` 端点 | Sprint 10.3 ✅ | openapi.yaml 端点 20 描述补齐 | 已 ship |
| 状态机 enum 调整 | Sprint 13.5 ✅ | 13.7 不动 | 已 ship |
| sys_dict 字典 admin 页风格 | Sprint 1.3 ✅ | web-impl admin UI 风格样板 | 已 ship |
| tsconfig.json · strict 模式 | V1.3.8 基础 | typecheck:ci gate | 已 ship |

### 3.2 下游影响

| 下游 Story | 消费内容 | 13.7 提供 |
|-----------|---------|-----------|
| **13.6** 7role e2e 覆盖 | typecheck:ci 退出 0 | 13.7 ship 后方可启动 dev IMPL |
| **13.8** escpos-evaluation | typecheck:ci 退出 0 | 13.7 ship 后方可启动 dev IMPL |
| **13.9** PRD §5 Epic table | typecheck:ci 退出 0 | 13.7 ship 后方可启动 dev IMPL |
| **14.1** 7role Android e2e | typecheck:ci 退出 0 | 13.7 ship 后方可启动 dev IMPL |
| **14.2** 14.x 其他 Story | typecheck:ci 退出 0 | 13.7 ship 后方可启动 dev IMPL |
| **12.1** DrawingViewer | `DrawingLinkService` 类型 | 真实数据接入 |
| **12.2** admin UI | `SysPrinter` / `PrinterAdminService` 类型 | 强类型享受 |
| **12.3** LabelPreview | `LabelTemplateService` 类型 | 模板预览 |
| **12.4** PrintButton | `PrintLogService` 类型 | 打印提交 |
| **13.4** 仪表盘 | `WorkflowEventStatsService` 类型 | 统计渲染 |

### 3.3 A 组串行约束

```
13.7 (A · typecheck 收口 · 起点 · 必先 ship)
   ├── 13.6 (A · 7role e2e · 13.7 后启动)
   ├── 13.8 (A · escpos 评估 · 13.7 后启动)
   ├── 13.9 (A · PRD table · 13.7 后启动)
   └── 14.1 (A · 7role android e2e · 13.7 后启动)
```

**关键约束**：13.7 ship 是 Sprint 14 IMPL 起点前置（typecheck:ci gate）· 不可逆 · 任何 Sprint 14 Story dev IMPL 必须在 13.7 ship 后启动。

### 3.4 多仓协同

- **backend**：主仓 · `spec/openapi.yaml` 补 20 端点 + 7 schema
- **web-impl**：从仓 · `src/api/generated/` regen + 2 文件改（Printers.vue 取消 any + GmSummary.vue Number() 强转）+ 1 文件新增（vite-env.d.ts）
- **android-impl**：**不参与**（仅 web-impl typecheck 收口）

---

## 4. 测试评审

### 4.1 测例数

- **业务测例**：0（pure baseline 收口）
- **验证项**：3（pure 自动化验证）

### 4.2 3 验证项

| # | ID | 验证项 | 步骤 | 期望 |
|---|----|-------|------|------|
| 1 | TC-13.7.1.1 | **vite-env.d.ts 字段覆盖** | `cat web-impl/src/vite-env.d.ts` | 含 `VITE_API_BASE_URL` + `VITE_USE_MOCK` + `VITE_APP_TITLE` 三字段 |
| 2 | TC-13.7.2.1 | **codegen 链路** | `cd web-impl && npm run gen:api` | exit 0 |
| 2 | TC-13.7.2.2 | **regen diff** | `git diff --exit-code src/api/generated` | exit 0（无未提交变更） |
| 2 | TC-13.7.2.3 | **Printers.vue 类型替换** | L163-164 codegen 类型导入 | `import type { Printer } from '@/api/generated/models/SysPrinter'` 存在 |
| 3 | TC-13.7.3.1 | **typecheck:ci** | `npm run typecheck:ci` | exit 0 |
| 3 | TC-13.7.3.2 | **build** | `npm run build` | exit 0 + dist 标题为 `V1.3.8` |

---

## 5. 风险评审

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| 1 | **openapi.yaml 端点字段与已 ship IMPL 实际契约不一致**（Sprint 12-13 dev IMPL 阶段遗漏字段描述）| 🟡 中 | dev 启动前按 Sprint 12/13 dev log 比对 · 字段 mismatch 立即修 · 与 architect 评审同步 |
| 2 | **codegen 漂移**（regen 后既有 .vue 组件引用 generated 类型失效）| 🟢 低 | typecheck:ci + `git diff --exit-code src/api/generated` gate 拦截 |
| 3 | **13.7 ship 阻塞 Sprint 14 IMPL**（typecheck:ci 非 0 → 所有 Story dev IMPL 不可启动）| 🟡 中 | SM 萧何协调 · 13.7 ship 后才启动 13.6/13.8/13.9/14.1 dev IMPL |
| 4 | **12.1 灰度阶段 1 启动需 typecheck:ci 退出 0**（2026-06-30 admin + ENGINEER 灰度）| 🟡 中 | 13.7 ship 必须早于 2026-06-30 灰度启动 · 灰度硬前置 |
| 5 | **GmSummary.vue Number() 断言掩盖 DTO 字段类型 bug** | 🟢 低 | 后续 Sprint 14+ 启动 GmSummaryResponse DTO 类型修正（V1.3.10 backlog）|
| 6 | **vite-env.d.ts 字段遗漏**（未来新增 `VITE_*` 字段未声明）| 🟢 低 | dev 启动前 grep `import.meta.env.VITE_` 校验字段覆盖度 · 14.4 strict 化时统合 |

---

## 6. IMPL 注意事项（3 条）

1. **dev 启动前先备份 web-impl/src/api/generated/**（预防回滚）— `cp -r src/api/generated src/api/generated.backup` · regen 后若 diff 异常可秒回滚
2. **按 Sprint 12/13 dev log 逐端点比对字段**（避免契约漂移）— openapi.yaml 字段必须与 backend 实装 100% 一致 · 与 dev agent 共同核对
3. **Printers.vue 取消 any 兜底后 typecheck 双 0 必过**（临时绕过的 any 是 13.7 必清）— L163-164 强类型导入 + typecheck:ci 退出 0 + build 退出 0

---

## 7. Verdict

🟢 **APPROVED** · 可进入 dev IMPL

- **0 业务端点改动**（pure schema 补齐 · 不新增 endpoint）
- **0 Flyway 迁移**（仅文档层 + codegen 层）
- **20 openapi.yaml 端点增量**（6 + 2 + 7 + 1 + 3 + 1 · 补齐 Sprint 12-13 已 ship 端点的契约描述）
- **7 openapi.yaml schema 增量**（SysPrinter 等）
- **~20 codegen 预期生成文件**（13 models + 6 services）
- **风险 4 项已识别并缓解**（3 项中 + 3 项低）
- **A 组串行约束明确**（13.7 ship 是 Sprint 14 起点 · 不可逆）

---

## 8. 签字

**architect 鲁班** · **2026-06-14** · **🟢 APPROVED**
