# Story 13.5 Review - 7 状态机 enum drift 对齐（openapi.yaml 改 V1.3.7 定义）

> **Reviewer**：architect 鲁班
> **Date**：2026-06-14
> **Sprint**：V1.3.9 Sprint 13（parallel_group A · 13.1 前置）
> **关联 PM 决策**：`docs/qa/evidence/sprint10-pm-decision-3-state-machine.md`（2026-06-13 · 选项 A 采纳）
> **关联 Story**：`backend/docs/stories/sprint13/13.5-status-machine-enum-align.md`
> **Verdict**：🟢 **APPROVED**（可进入 dev IMPL · 附 3 条 IMPL 注意事项）

---

## 1. 契约评审

| 项 | 评审结论 |
|----|----------|
| 任务类型 | **schema enum drift 收口** · 0 业务端点 · 0 单测 · 0 Flyway ✅ |
| 改动范围 | `backend/spec/openapi.yaml` 3 处 enum 段（L475 / L478 / L495）+ 1 处 description（L3049）⚠️ |
| codegen 输出 | 9 项 union → 7 项 union（`web-impl/src/api/generated/models/OutsourceState*.ts`）✅ |
| 联动文件 | `web-impl/src/composables/useOutsourceStateMachine.ts` + `OutsourceStateMachine.vue` 4 typecheck 错误修复 ✅ |
| 验收方式 | `grep` 文本 diff + `npm run gen:api` + `npm run typecheck:ci`（含 `git diff --exit-code`）✅ |

### 1.1 0 端点（仅 schema enum 调整）

- 0 业务端点改动（保留 V1.3.7 既有 148 ops · 0 增减）
- 3 处 enum 段调整（schema 层）· 不动 `$ref` 引用方（4 端点 `advanceOutsourceState` / `rollbackOutsourceState` / `getOutsourceStateHistory` / `getOutsourceStateMatrix` 复用同 schema）

### 1.2 3 处 enum 段（含 1 处 description 顺手清理）

| # | 行号 | 段 | 当前 enum | 改后 enum | 备注 |
|---|------|------|----------|----------|------|
| 1 | **L475** | `OutsourceStateHistory.fromState` | `[DRAFT, SENT, ACCEPTED, IN_PRODUCTION, INSPECTED, COMPLETED, CLOSED, REWORK, REJECTED]`（9） | `[DRAFT, SUBMITTED, ACCEPTED, IN_PROGRESS, COMPLETED, REWORK, CLOSED]`（7） | 删 SENT/IN_PRODUCTION/INSPECTED/REJECTED · 增 SUBMITTED/IN_PROGRESS |
| 2 | **L478** | `OutsourceStateHistory.toState` | 同 L475（9） | 同 L475 改后（7） | 同上 |
| 3 | **L495** | `OutsourceStateAdvanceRequest.targetState` | 同 L475（9） | 同 L475 改后（7） | 同上 |
| ⚠️ 4 | **L3049** | `getOutsourceStateMatrix` description | `'8 状态：DRAFT/SENT/.../REJECTED + 终态 CLOSED'` | `'7 状态：DRAFT/SUBMITTED/ACCEPTED/IN_PROGRESS/COMPLETED/REWORK/CLOSED'` | **顺手清理** · 否则契约文档自相矛盾 · PM 决策#3 §5.2 未列出 · dev 必须同步改 |

> 注：PM 决策#3 文档标注的「L473-475 / L476-478 / L494-495」系按「字段标签+值」整体行号计算；实际精确 enum 段行号为 **L475 / L478 / L495**（字段定义在 enum 行）· 以 openapi.yaml 实测为准。
> ⚠️ 评审新增一项：L3049 description 文本需同步更新（7 状态字面量），否则与 schema enum 不一致 · 列入 dev IMPL 清单。

### 1.3 7 状态机精确映射表（V1.3.7 1.22 ↔ codegen ↔ openapi.yaml）

| # | V1.3.7 1.22 合同 | 当前 codegen union（9） | 改后 codegen union（7） | 业务语义 | 决策（PM #3） |
|---|----------------|----------------------|----------------------|----------|------|
| 1 | `DRAFT` | ✅ `DRAFT` | ✅ `DRAFT` | 草稿（生管下单后） | ✅ 保留 |
| 2 | `SUBMITTED` | ❌ 缺 | ➕ `SUBMITTED` | 已提交（厂商待接单） | ➕ 加 |
| 3 | `ACCEPTED` | ✅ `ACCEPTED` | ✅ `ACCEPTED` | 已接单 | ✅ 保留 |
| 4 | `IN_PROGRESS` | ❌ 缺（`IN_PRODUCTION` 占位但语义错） | ➕ `IN_PROGRESS` | 进行中（厂商加工） | ➕ 加 · `IN_PRODUCTION` → `IN_PROGRESS` 重命名 |
| 5 | `COMPLETED` | ✅ `COMPLETED` | ✅ `COMPLETED` | 已完成（厂商加工完成） | ✅ 保留 |
| 6 | `REWORK` | ✅ `REWORK` | ✅ `REWORK` | 返修中 | ✅ 保留 |
| 7 | `CLOSED` | ✅ `CLOSED` | ✅ `CLOSED` | 已关闭（终态） | ✅ 保留 |
| 8 | — | ⚠️ `SENT`（早期 IMPL 残留） | ❌ 删 | codegen 早期 IMPL 扩展 | ❌ 删 |
| 9 | — | ⚠️ `INSPECTED`（早期 IMPL 残留） | ❌ 删 | codegen 早期 IMPL 扩展 | ❌ 删 |
| 10 | — | ⚠️ `REJECTED`（早期 IMPL 残留） | ❌ 删 | codegen 早期 IMPL 扩展 | ❌ 删（`rollback` 终态走 `OutsourceStateRollbackRequest.reason` 路径，不在 advance 转移图内） |

> 关键变更语义：`SUBMITTED`（已提交，厂商待接单）取代 `SENT`（已发送）；`IN_PROGRESS`（进行中）取代 `IN_PRODUCTION`（生产中）· 与 V1.3.7 Story 1.22 合同术语对齐。

---

## 2. 技术评审

### 2.1 openapi.yaml 改动范围（3+1 处）

| 改动 | 当前 | 改后 | 风险 |
|------|------|------|------|
| L475 `fromState` enum | 9 项 | 7 项 | 🟢 收紧 · 向合同看齐 |
| L478 `toState` enum | 9 项 | 7 项 | 🟢 收紧 · 向合同看齐 |
| L495 `targetState` enum | 9 项 | 7 项 | 🟢 收紧 · 向合同看齐 |
| L3049 `getOutsourceStateMatrix` description | "8 状态：DRAFT/SENT/.../REJECTED + 终态 CLOSED" | "7 状态：DRAFT/SUBMITTED/ACCEPTED/IN_PROGRESS/COMPLETED/REWORK/CLOSED" | 🟢 文本同步 · 防止文档自相矛盾 |

> 评审新增 L3049 一项 · PM 决策#3 漏列 · 必须随 enum 同步改。

### 2.2 codegen regen（web-impl 仓）

```bash
cd web-impl
rm -rf src/api/generated
npm run gen:api   # 退出码 0
```

预期 3 文件类型收窄为 7 项 union：

```typescript
// web-impl/src/api/generated/models/OutsourceState.ts
export type OutsourceState = 'DRAFT' | 'SUBMITTED' | 'ACCEPTED' | 'IN_PROGRESS' | 'COMPLETED' | 'REWORK' | 'CLOSED';

// web-impl/src/api/generated/models/OutsourceStateAdvanceRequest.ts
targetState: OutsourceState;  // 7 项

// web-impl/src/api/generated/models/OutsourceStateHistory.ts
fromState?: OutsourceState;   // 7 项
toState?: OutsourceState;     // 7 项
```

### 2.3 typecheck 验证（含 git diff gate）

```bash
cd web-impl
npm run typecheck       # vue-tsc --noEmit → 退出 0
npm run typecheck:ci    # typecheck + git diff --exit-code src/api/generated → 退出 0
npm run build           # vite build → 退出 0
```

- `typecheck:ci` 的 `git diff --exit-code` gate 是关键拦截点：若 codegen 输出未提交或 regen baseline 漂移，门禁会失败
- typecheck 退出 0 是 Sprint 10.5 4 错误彻底解决的判据

### 2.4 受影响文件清单

| # | 文件 | 改动 | 来源 |
|---|------|------|------|
| 1 | `backend/spec/openapi.yaml` L475/L478/L495/L3049 | 3 处 enum + 1 处 description 文本 | 本 Story 13.5 主改 |
| 2 | `web-impl/src/api/generated/models/OutsourceState.ts` | regen 派生 | 13.5 触发 |
| 3 | `web-impl/src/api/generated/models/OutsourceStateAdvanceRequest.ts` | regen 派生 | 13.5 触发 |
| 4 | `web-impl/src/api/generated/models/OutsourceStateHistory.ts` | regen 派生 | 13.5 触发 |
| 5 | `web-impl/src/composables/useOutsourceStateMachine.ts` | 改 `export type OutsourceState = ...` → `export const OutsourceState = [...] as const` + `export type OutsourceState = typeof OutsourceState[number]` | 10.5 临时方案升级 |
| 6 | `web-impl/src/views/production/OutsourceStateMachine.vue` | 删除 5 处 `as any` 桥接（PM 决策#3 落地）· L12/L13 改 `currentOrder` 类型为 `OutsourceOrder` | 10.5 4 typecheck 错误修复 |
| — | `web-impl/src/services/protocol/ProtocolAdapter.ts`（12.4） | ❌ **不涉及** | 12.4 不直接消费 `OutsourceState` 联合类型（ZPL/TSPL 协议生成） |
| — | `backend/src/main/java/.../service/OutsourceOrderService.java`（9.1 recordEvent 触发） | ❌ **不涉及** | 按业务事件触发 · enum 改动不影响 SQL 触发器逻辑 |
| — | `backend/src/main/java/.../service/NoOrderPurchaseService.java`（4.1） | ❌ **不涉及** | 不消费 `OutsourceState` 联合类型 |
| — | `backend/src/main/java/.../service/ProcurementApprovalRouter.java`（4.2） | ❌ **不涉及** | 不消费 `OutsourceState` 联合类型 |
| — | `backend/src/main/java/.../service/RfqService.java`（1.32） | ❌ **不涉及** | 不消费 `OutsourceState` 联合类型 |

### 2.5 9.1 recordEvent 触发器兼容（4 个上游调用方）

| 上游调用方 | 触发条件 | 是否消费 enum | 13.5 影响 |
|----------|---------|-------------|----------|
| 4.1 `NoOrderPurchaseService` | 无订单采购单据状态变更 | ❌ 不消费 | 🟢 无 |
| 4.2 `ProcurementApprovalRouter` | 采购审批路由推进 | ❌ 不消费 | 🟢 无 |
| 1.32 `RfqService` | RFQ 流转 | ❌ 不消费 | 🟢 无 |
| 9.1 `OutsourceOrderService.recordEvent` | 委外单状态迁移后写入 `OutsourceStateHistory` | ⚠️ **写入** `fromState/toState` 字段（string 落库）· 但**不消费** enum 联合类型做编译期判断 | 🟢 无（DB 存 string 文本 · 不参与编译） |

> 关键：`recordEvent` 调用方按业务事件触发（`OrderStateChangedEvent` / `ApprovalPassedEvent` 等）· 写入的 `fromState/toState` 是 string 落库 · enum 联合类型收紧不影响 SQL · 既有 9.1 测例 80/80 PASS 维持不变。

---

## 3. 集成评审

### 3.1 上游依赖

| # | 依赖项 | 来源 | 状态 |
|---|--------|------|------|
| 1 | V1.3.7 1.22 7 状态机合同定义 | Story 1.22 ✅ | enum baseline · 合同源 |
| 2 | Sprint 10 PM 决策#3（选项 A） | `docs/qa/evidence/sprint10-pm-decision-3-state-machine.md` | ✅ 已发布 · 2026-06-13 |
| 3 | Sprint 10.1 codegen baseline（`generated/` 目录） | 10.1 dev log §6.1 | ✅ 已 IMPL |
| 4 | Sprint 10.5 `useOutsourceStateMachine.ts` 临时方案 | 10.5 dev log §9 | ✅ 已 IMPL · 4 typecheck 错误待本 Story 收口 |
| 5 | Sprint 12 12.4 dev log 引用（ZPL/TSPL 协议） | 12.4 dev log | ✅ 不涉及 `OutsourceState` |

### 3.2 下游影响

| # | 下游 | 影响 | 等级 |
|---|------|------|------|
| 1 | `web-impl` codegen 联合类型收紧 | 9 → 7 项 union | 🟢 收紧 · 收口 |
| 2 | 10.5 `OutsourceStateMachine.vue` 4 typecheck 错误 | 全修复 · 删 `as any` 桥接 | 🟢 正向 |
| 3 | 12.4 ZPL/TSPL `ProtocolAdapter` | 不直接消费 `OutsourceState` · 不受影响 | 🟢 无 |
| 4 | 9.1 `recordEvent` SQL 触发器 | 按业务事件触发 · 不受影响 | 🟢 无 |
| 5 | 4.1/4.2/1.32 上游调用方 | 不消费 `OutsourceState` · 不受影响 | 🟢 无 |
| 6 | 13.1 `InspectionDTO` schema | **必须 13.5 先 ship** · 否则 InspectionDTO 引用旧 9 项 union 漂移 | 🟡 **前置依赖** |

### 3.3 A 组串行约束（parallel_group A）

> **13.5 必须先于 13.1 ship**（顺序约束 13.5 → 13.1 不可逆）。

- **理由**：13.1 `InspectionDTO` schema 调整需 regen baseline 稳定（`OutsourceState` 联合类型已收口到 7 项）· 否则 InspectionDTO 引用的 `OutsourceState` 字段会出现 9→7 漂移
- **协调人**：SM 萧何
- **执行规约**：
  1. 13.5 dev IMPL 完成（openapi.yaml 3+1 处 + regen + typecheck:ci 退出 0）
  2. 13.5 QA 商鞅 test-execute 通过（10 验证项全 PASS）
  3. 13.5 ship → 13.1 才允许 dev IMPL 启动
- **若违反**：InspectionDTO 联合类型 9→7 漂移 → web-impl typecheck 二次失败 → 返工

---

## 4. 测试评审

### 4.1 10 验证项清单（0 单测 · 10 验证测例）

| # | ID | 验证项 | 步骤 | 期望 |
|---|----|--------|------|------|
| 1 | TC-13.5.1.1 | `OutsourceState` enum 7 值 | `grep -A 10 'OutsourceState:' backend/spec/openapi.yaml` | 7 值（无 `REJECTED/SENT/IN_PRODUCTION/INSPECTED`） |
| 2 | TC-13.5.1.2 | `targetState` enum 7 值 | `grep -A 10 'targetState:' backend/spec/openapi.yaml` | 7 值 |
| 3 | TC-13.5.1.3 | `fromState` enum 7 值 | `grep -A 10 'fromState:' backend/spec/openapi.yaml` | 7 值 |
| 4 | TC-13.5.1.4 | 其他 148 ops 不动 | `grep -c 'operationId' backend/spec/openapi.yaml` | 计数 = 148（无增减） |
| 5 | TC-13.5.1.5 | L3049 description 同步 | `grep -A 1 'getOutsourceStateMatrix' backend/spec/openapi.yaml` | description = "7 状态：DRAFT/SUBMITTED/ACCEPTED/IN_PROGRESS/COMPLETED/REWORK/CLOSED"（评审新增项） |
| 6 | TC-13.5.2.1 | `gen:api` 退出 0 | `cd web-impl && rm -rf src/api/generated && npm run gen:api` | 退出码 0 |
| 7 | TC-13.5.2.2 | 3 文件类型正确 | `cat src/api/generated/models/OutsourceState.ts` 等 | 7 值 union |
| 8 | TC-13.5.2.3 | regen diff exit 0 | `git diff --exit-code src/api/generated` | 退出码 0（baseline 稳定） |
| 9 | TC-13.5.3.1 | `typecheck` 退出 0 | `npm run typecheck` | 退出码 0（vue-tsc 通过） |
| 10 | TC-13.5.3.2 | `typecheck:ci` 退出 0 | `npm run typecheck:ci` | 退出码 0（含 git diff gate） |
| 11 | TC-13.5.3.3 | `build` 退出 0 | `npm run build` | 退出码 0（vite build 通过） |
| 12 | TC-13.5.3.4 | 10.5 `OutsourceStateMachine.vue` 删 `as any` | `grep -c 'as any' src/views/production/OutsourceStateMachine.vue` | 计数 = 0（5 处全删） |
| 13 | TC-13.5.3.5 | 9.1 `recordEvent` 触发路径不受影响 | `cd backend && mvn test -Dtest=OutsourceOrderServiceTest` | 80/80 PASS 不变 |
| 14 | TC-13.5.3.6 | 12.4 `ProtocolAdapter` 不受影响 | `cd web-impl && npx tsc --noEmit src/services/protocol/ProtocolAdapter.ts` | 退出码 0 |
| 15 | TC-13.5.3.7 | 9.2 既有 codegen 使用方 | `cd web-impl && npm run typecheck`（全量） | 退出码 0（除 OutsourceStateMachine 4 错误已修外无新增） |
| 16 | TC-13.5.3.8 | 1.22 业务路径 V1.3.7 兼容 | V1.3.7 既有部署（FAT/UAT）流程不变 | 业务流转不受影响（enum 改动 ≠ 业务流转改动） |
| 17 | TC-13.5.3.9 | 1.22 `sys_workflow_node` 表兼容 | V1.3.7 既有数据 | DB schema 不动 · 0 Flyway |

> 实际验证项 = 17 项（PM 决策#3 列 10 项 + 评审新增 7 项 L3049 + 5 处 `as any` grep + 9.1/12.4/9.2 三处回归 + DB 兼容 2 项）· 仍属 0 单测范畴（纯 regen + typecheck + grep 验证）。
> ⚠️ 评审细化：将 10 项扩为 17 项，便于 QA 商鞅 *test-execute 时按项打勾。

---

## 5. 风险评审

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| 1 | codegen 输出影响 10.5/12.4 既有代码 | 🟢 低 | 10.5 删 `as any` 桥接 + `useOutsourceStateMachine.ts` 用 `as const` 字面量 · 12.4 不直接消费 `OutsourceState`（typecheck:ci 验证） |
| 2 | 9.1 `recordEvent` 触发逻辑受影响 | 🟢 低 | 按业务事件触发（`OrderStateChangedEvent` 等）· enum 改动不影响 SQL · 9.1 测例 80/80 PASS 维持 |
| 3 | V1.3.7 既有部署兼容 | 🟢 低 | enum 改动 ≠ 业务流转改动 · V1.3.7 实际 7 状态业务路径已上线（PENDING_SHIP/... 是另一模块）· DB 0 Flyway |
| 4 | A 组串行约束 13.5 → 13.1 顺序违反 | 🟡 中 | SM 萧何协调启动顺序 · 13.5 ship 后 13.1 才允许 dev IMPL · 13.1 Story 文档已显式标注「前置必须先 ship」 |
| 5 | codegen 漂移（regen baseline 不稳） | 🟡 中 | `npm run typecheck:ci` 的 `git diff --exit-code src/api/generated` gate 拦截 · 17 项验证项含 8 项 grep/typecheck 互锁 |
| 6 | L3049 description 漏改 | 🟡 中 | 评审新增 TC-13.5.1.5 显式断言 · dev IMPL 清单必须包含 L3049 |
| 7 | 1.22 `OutsourceStateRollbackRequest` enum 误改 | 🟢 低 | L504-510 无 enum 字段（仅 outsourceId/reason/operatorRole）· 不需改 · 评审已确认 |

---

## 6. IMPL 注意事项（3 条）

1. **dev 启动前先备份 `web-impl/src/api/generated/`**
   - `cp -r src/api/generated src/api/generated.bak.$(date +%Y%m%d)`（或 git stash 兜底）
   - 预防回滚 · codegen 二次 regen 是幂等操作 · 备份仅作保险

2. **改动后必跑 `npm run typecheck:ci`**（含 `git diff` gate）
   - 必须 `cd web-impl && npm run typecheck:ci` 退出 0 才算 IMPL 完成
   - 若 `git diff --exit-code src/api/generated` 非 0 → codegen 输出未提交或漂移 → 需 `git add src/api/generated && git commit` 提交 regen baseline

3. **10.5 `OutsourceStateMachine.vue` 同步删 `as any` 桥接**（与 codegen 联合类型对齐）
   - L12/L13 改 `currentOrder` 类型为 `OutsourceOrder`
   - L22 `doTransition` 用 `OutsourceStateAdvanceRequest['targetState']`
   - L44 `composable transition` 函数签名与 codegen 一致
   - 5 处 `as any` → 全部替换为具体类型或 `as unknown as TypeName`（不再兜底）
   - `useOutsourceStateMachine.ts` 用 `as const` 字面量 + `typeof stateSteps[number]` 派生类型

> ⚠️ 评审追加（hidden in #3）：**L3049 description 同步更新**（"8 状态..." → "7 状态..."）· 否则契约文档自相矛盾 · dev IMPL 必做。

---

## 7. Verdict

🟢 **APPROVED** · 可进入 dev IMPL

- 0 端点改动 · 仅 schema enum 调整（3 处 enum + 1 处 description）
- V58 不需新迁移（pure schema 调整 · 0 Flyway）
- 风险 7 项已识别（5 项评审列出 + 2 项评审新增 L3049 / 串行约束）
- A 组串行约束明确（13.5 → 13.1 不可逆）
- 17 项验证项清单可执行
- IMPL 注意事项 3 条 + 1 条 hidden 追加

---

## 8. 签字

**architect 鲁班** · 2026-06-14 · 🟢 **APPROVED**

> **hand_to_dev** → dev agent Opus 4.8（backend + web-impl 双仓协同 IMPL）
> 截止：Sprint 13 周内完成（PO 范蠡设定 0.5 天工时 · 与 PM 决策#3 2026-06-15 EOD 截止一致）
> 顺序：13.5 ship → 13.1 才允许 dev IMPL（SM 萧何协调）
