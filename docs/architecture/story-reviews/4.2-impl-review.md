# Story 4.2 Review - 采购主管审批路由

> **Reviewer**：architect 鲁班
> **Date**：2026-06-13
> **Sprint**：V1.3.8 S7
> **Verdict**：🟡 **APPROVED with precheck**（**dev IMPL 启动前必先完成 §5 TODO · 否则禁止启动**）

---

## 1. 契约评审

| 项 | 评审结论 |
|----|----------|
| 端点数 | 2 端点（route-preview + procurement-manager-perms）· 与 Story 一致 ✅ |
| 角色独立性 | PROCUREMENT_MANAGER 与 PURCHASER 互斥 · 符合 PM 反馈 ✅ |
| 4 阈值路由细化 | 5 条 sys_workflow_config INSERT · 与 PM 反馈一致 ✅ |
| 兼容 legacy | compatible_legacy_route 字段保留老 DEPT_MANAGER 路由 ✅ |

### 1.1 AC 评审

| AC | 评审 |
|----|------|
| AC-4.2.1 PROCUREMENT_MANAGER 角色 | 🟢 可测 · 与 PURCHASER 互斥逻辑明确 |
| AC-4.2.2 4 阈值路由细化 | 🟢 route-preview 端点无副作用 |

---

## 2. 数据库评审（V52）

### V52__procurement_manager_role.sql 评审

| 项 | 评审 |
|----|------|
| 角色 INSERT | 🟢 1 项 PROCUREMENT_MANAGER |
| 权限 INSERT | 🟢 4 项 permission_code |
| 阈值路由 | 🟡 5 条 INSERT · **详见 §2.1** |
| 风险 | 🟡 **dev 必先 grep V1.3.7 现状** |

### 2.1 ⚠️ 5 条 INSERT 需与现状对比

| 阈值 INSERT | 与 V1.3.7 现状可能冲突 |
|-------------|------------------------|
| AMOUNT 10000-50000 → PROCUREMENT_MANAGER | 老路由可能是 DEPT_MANAGER |
| AMOUNT >50000 → GM+PROCUREMENT_MANAGER | 老路由可能只有 GM |
| CATEGORY TOOL → PROCUREMENT_MANAGER | 老路由可能是 DEPT_MANAGER |
| CATEGORY CHEMICAL → PROCUREMENT_MANAGER | 同上 |
| URGENCY URGENT+AMOUNT>10000 → PROCUREMENT_MANAGER | 老路由可能是 PURCHASER_LEAD |

**🟡 dev IMPL 启动前必查现状**（Story §5 TODO），如发现冲突先修订本 Story 再实施。

---

## 3. 技术评审

### 3.1 角色互斥

```java
@PreAuthorize("hasRole('PROCUREMENT_MANAGER') and !hasRole('PURCHASER')")
```

- 数据库层：`sys_user_role` 唯一约束 `(user_id, role_code)`
- 应用层：用户编辑时禁止同时勾选两个角色
- 🟢 双保险

### 3.2 阈值路由算法

- 复用 V1.3.7 1.2 审批工作流引擎
- 多阈值匹配按 sort_order 排序，去重 approver_role
- 🟢 复用现有引擎

### 3.3 route-preview 端点

- 无副作用：仅返回预测路由，不创建审批流
- 用于前端实时预览「这个 PO 会找谁审批」
- 🟢 标准模式

---

## 4. 集成评审

### 4.1 上游依赖

| Story | 端点/字段 | 本 Story 用途 | 风险 |
|-------|-----------|---------------|------|
| 1.2 | 审批工作流 | 引擎复用 | 🟢 |
| 1.32 | RFQ | 审批阈值 | 🟢 |

### 4.2 下游影响

- 1.2 审批工作流：sys_workflow_config 新增 5 条 · 不影响既有
- 1.32 RFQ：路由逻辑更新 · **需回归 18 测例**
- 4.1 无订单采购：审批路由联动 · **需同步发布**

---

## 5. dev 启动前 TODO（**硬性约束**）

```bash
# 1. 列出 V1.3.7 审批路由实现
grep -rn "approver_role\|threshold_type" backend/src/main/java/com/cnc/approval/
grep -rn "PROCUREMENT\|DEPT_MANAGER" backend/src/main/java/com/cnc/approval/

# 2. 列出 sys_workflow_config 现状
mysql> SELECT * FROM sys_workflow_config WHERE workflow_code='PO_APPROVAL';

# 3. 列出 sys_role 现状
mysql> SELECT * FROM sys_role;

# 4. 输出至 docs/dev/logs/4.2-precheck.log
```

输出若发现：
- **已有 `PROCUREMENT_MANAGER` 角色** → 跳过 INSERT，仅补全权限
- **4 阈值路由与本 Story 不一致** → 修订本 Story §1.1 表格后再实施
- **老角色冲突** → 评估迁移路径

**🟡 precheck 不通过则禁止启动 IMPL**。

---

## 6. 测试评审

### 6.1 测例覆盖

| 测例组 | 数量 | 评审 |
|--------|------|------|
| TC-4.2.1 角色权限 | 8 | 🟢 含互斥 + JWT |
| TC-4.2.2 路由细化 | 10 | 🟢 含 5 阈值组合 |
| 回归 1.2 审批 | 12 | 🟡 **必更** |
| 回归 1.32 RFQ | 18 | 🟡 **必更** |
| **合计** | **~48** | 🟢 |

---

## 7. IMPL 注意事项（3 条）

1. **必先完成 §5 precheck**：输出 `docs/dev/logs/4.2-precheck.log`
2. **角色互斥双保险**：DB 唯一约束 + 应用层校验
3. **同 Sprint PR 合并**：与 4.1 同 PR 合并发布，确保审批路由联动生效

---

## 8. Verdict

🟡 **APPROVED with precheck**
- dev IMPL 启动前必先完成 §5 TODO
- precheck 输出 `docs/dev/logs/4.2-precheck.log`
- precheck 不通过则修订 Story 后再实施

---

## 9. 签字

- **architect 鲁班** · 2026-06-13 · APPROVED with precheck
- **PO 范蠡** · 待接收 review
- **dev agent Opus 4.8** · 待启动 IMPL（**必先 §5 precheck**）
- **QA 商鞅** · precheck log 作为测试输入