# Story 1.2 - 审批工作流配置 · 测试设计方案

> **测试架构师**：orchestrix QA · 商鞅（Test Engineer & Quality Architect）
> **设计日期**：2026-06-10
> **合同**：XP-ZPF202606082405 · 昆山佰泰胜专属 ERP 系统 V1.3.7
> **Story 范围**：1.2 审批工作流配置（Epic 1 / 工作流中台 · 4 AC + 46 checkbox）
> **测试级别**：**Comprehensive**（complexity=L=6/7 + 安全敏感 + 4 处 P1 修补 + 5 维风险 + Architect 9.0/10）
> **关联**：[→ Story 1.2](../../backend/docs/stories/1.2-审批工作流配置.md) · [→ Architect 评审](../../docs/architecture/story-reviews/1.2-review.md) · [→ QA Story 1.1 模板](./1.1-test-design.md) · [→ 架构 §6/§7/§13/§15](../../backend/docs/architecture.md) · [→ OpenAPI](../../backend/spec/openapi.yaml)

---

## 一、测试设计总览

### 1.1 覆盖矩阵（AC → TestCase → Automation）

| AC | 业务目标 | 单测 ID | 集成测 ID | E2E 测 ID | 性能 | 安全 | 自动化率 |
|----|---------|---------|----------|----------|------|------|---------|
| **AC-1.2.1** | 工作流 CRUD + 4 套内置模板（不可删可复制） | `WorkflowTemplateFactoryTest` (20) / `WorkflowValidatorTest` (9) / `WorkflowServiceTest` (10) | `WorkflowControllerE2ETest#crud_5_endpoints` / `builtin_delete_throws_40904` | `e2e/admin-workflow-config.spec.ts` | 创建 P95<300ms | OWASP ZAP 越权 | 100% |
| **AC-1.2.1 (d)** | 工作流试跑（不落库） | `WorkflowServiceTest#test_workflow_no_persist` | `WorkflowControllerE2ETest#test_80k_routes_dept_manager` | — | 试跑 P95<200ms | — | 100% |
| **AC-1.2.2 (Case 2)** | 60000 金额路由到 dept_manager（OR 会签 candidates 列表） | `QuoteApprovalRouterTest#route_60k_or_sign` (7) | `ApprovalE2ETest#create_60k_quote_routes_dept_manager` | `e2e/quote-60k-approval.spec.ts` | 路由 < 50ms | — | 100% |
| **AC-1.2.2 (3 边界)** | 30000 自审 / 250000 路由 gm / dept_manager 自审 | `QuoteApprovalRouterTest#route_30k/250k/dept_manager` | `ApprovalE2ETest#boundary_50000/200000` | — | 路由 < 50ms | — | 100% |
| **AC-1.2.2 (通知)** | Redis Stream `stream:notify` 推送 | `WorkflowEventPublisherTest#publish_4_channels` (5) | `NotificationConsumerE2ETest#consume_100_events` | — | 推送 P95<200ms | 失败重试 3 次 | 100% |
| **AC-1.2.3 (Case A · P1-1)** | candidates=[10010 在岗, 10011 请假] → 10011 SKIPPED + 10010 正常 | `SkipOnLeaveRuleTest#or_sign_partial_skip` (5) / `ApprovalServiceTest#approve_with_skip` (15) | `ApprovalE2ETest#or_sign_partial_skip` | `e2e/approval-or-sign-skip-leave.spec.ts` | — | 审计留痕 | 100% |
| **AC-1.2.3 (Case B · P1-1+2)** | 全员 SKIPPED → nodeSkipped=true + 推进下一节点 | `SkipOnLeaveRuleTest#all_skipped_advance_node` | `ApprovalE2ETest#all_skipped_auto_advance` | `e2e/approval-all-skipped-advance.spec.ts` | — | — | 100% |
| **AC-1.2.3 (Case C · P1-1)** | orSignRequired=true + 3 人 OR 会签 | `ApprovalServiceTest#or_sign_full_required` | `ApprovalE2ETest#or_sign_full_required` | — | 4 人 OR P95<500ms | — | 100% |
| **AC-1.2.3 (状态机)** | 重复 approve / 已结束 → 40904 | `ApprovalServiceTest#double_approve_throws_40904` | — | — | — | — | 100% |
| **AC-1.2.4 (24h 超时)** | XXL-JOB 扫描 + 4 通道推送 + is_overdue | `ApprovalTimeoutWatcherTest#overdue_4_channels` (6) / `ApprovalTimeoutServiceTest` (4) | `ApprovalTimeoutE2ETest#xxljob_scan_after_25h` | `e2e/approval-24h-timeout.spec.ts` | 推送 P95<200ms | 并行 + 重试 | 100% |
| **AC-1.2.4 (Nacos 热更新)** | timeout-hours=12 立即生效 | `WorkflowConfigTest#nacos_refresh` (3) | `WorkflowConfigE2ETest#timeout_change_immediate` | — | — | — | 90% |
| **AC-1.2.4 (二次催办)** | urge 不重置 timeout_at | `ApprovalTimeoutWatcherTest#urge_does_not_reset` | — | `e2e/approval-urge.spec.ts` | — | — | 100% |
| **T4 V2 物理表迁移** | sys_workflow JSON_EXTRACT → sys_workflow_node | 集成测试 `MigrationE2ETest#v2_split` | `MigrationE2ETest#seed_4_templates_renamed` | — | 迁移 < 5s | 字段重命名一致性 | 100% |

**覆盖率统计**：4 AC + 7 工具类 + 4 Service + 4 Controller + 3 横切 → **60+ 测例**（单测 ≥ 30 + 集成 ≥ 12 路径 + E2E 5 角色）→ 100% AC 覆盖（含 4 处 P1 修补）→ 自动化率 98%。

### 1.2 V1.3.7 P1 修补 → 测试要求映射

| P1 修补 | 验证方式 | 用例数 |
|---------|---------|--------|
| ① OR 会签 + `candidates: List<Long>` 字段 | `QuoteApprovalRouterTest` 4 边界 + `ApprovalServiceTest` OR 语义 + 集成 OR 会签 | 12 |
| ② 跳过请假用户（HR Feign） | `SkipOnLeaveRuleTest` 5 用例 + `HrEmployeeClient` 降级 + 集成 OR 跳过 | 8 |
| ③ Redis Stream 4 通道 + 失败重试 | `WorkflowEventPublisherTest` 5 用例 + `ApprovalTimeoutWatcherTest` 6 用例 + 集成消费者 | 14 |
| ④ `sys_workflow_node` 物理表拆分 | `MigrationE2ETest` V2__split 验证 + 集成查询性能 | 4 |

**P1 总计**：38 个独立验证点 → 100% 覆盖 4 处 P1 修补。

### 1.3 4 处 P1 修补专项（独立章节·见第三节）

每处 P1 修补独立列出修补背景 + 5-8 测例 + 风险等级 + 关联 AC。

---

## 二、单元测试设计（5 工具类 + 4 Service + 1 Config = ≥ 30 测例）

### 2.1 `WorkflowTemplateFactory`（20 测例 · 4 套模板 × 5 边界）

| # | 模板 | 用例 | 期望 | 验证点 |
|---|------|------|------|--------|
| 1 | QUOTE_FLOW | `quote_3_nodes` | 节点数 = 3（START/salesperson/dept_manager/GM/END） | 与 `init.sql:1080` 一致 |
| 2 | QUOTE_FLOW | `quote_threshold_5w_20w_null` | [50000, 200000, NULL] 单调非降 | 阈值连续性 |
| 3 | QUOTE_FLOW | `quote_or_sign_default_false` | `orSignRequired=false` | 向后兼容 Story 1.1 |
| 4 | QUOTE_FLOW | `quote_first_start_last_end` | `nodes[0].type=START` / `nodes[3].type=END` | 首末节点约束 |
| 5 | QUOTE_FLOW | `quote_clone_to_custom` | 复制改产生 `CUSTOM_QUOTE_FLOW_v1` | "复制改" 语义 |
| 6-10 | ORDER_FLOW | 同上 5 用例 + `extra_check=credit_limit` | 4 节点 + credit_limit | 条件分支 |
| 11-15 | PURCHASE_FLOW | 同上 5 用例 + 阈值 [1万, 5万, NULL] | 采购阈值差异 | 业务规则 |
| 16-20 | PAYMENT_FLOW | 同上 5 用例 + `dual_sign=>10万` | 财务双签 | V1.3.7 双签 hook |

### 2.2 `WorkflowValidator`（9 测例 · 6 失败 + 3 成功）

| # | 用例 | 输入 | 期望 |
|---|------|------|------|
| 1 | `success_3_nodes_valid` | 3 节点合法 | void |
| 2 | `success_with_cc_node` | 3 节点 + 1 CC 抄送 | void |
| 3 | `success_null_threshold_top` | [10000, 50000, NULL] | void（NULL = 无限额） |
| 4 | `fail_threshold_not_monotonic` | [200000, 50000] | `BizException(40001)` "阈值必须单调非降" |
| 5 | `fail_first_node_not_start` | nodes[0].type=APPROVAL | `BizException(40003)` |
| 6 | `fail_last_node_not_end` | nodes[N-1].type=APPROVAL | `BizException(40004)` |
| 7 | `fail_node_count_1` | 单节点 | `BizException(40005)` "节点数 2-20" |
| 8 | `fail_node_count_21` | 21 节点 | `BizException(40005)` |
| 9 | `fail_role_not_exist` | role_code=`xxx_invalid` | `BizException(40401)` ROLE_NOT_FOUND |
| 10 | `fail_amount_field_missing` | `conditions_json` 无 amount_field | `BizException(40006)` |

### 2.3 `QuoteApprovalRouter`（7 测例 · V1.3.7 升级 OR 会签）

| # | 用例 | amount | roleCode | 期望 result | 关键验证 |
|---|------|--------|----------|-------------|---------|
| 1 | `route_30k_self` | 30000 | salesperson | `candidates=[10086]` `matchedNodeIndex=1` | 自审 |
| 2 | `route_60k_or_sign_2` | 60000 | salesperson | `candidates=[10010, 10011]`（**P1-1**: List 字段）`orSignRequired=false` | **OR 会签 + 全列表** |
| 3 | `route_250k_gm` | 250000 | salesperson | `candidates=[10001]` `matchedNodeIndex=3` | 跳 dept_manager |
| 4 | `route_60k_dept_manager_self` | 60000 | dept_manager | `candidates=[10010]` | 部门经理自审 |
| 5 | `boundary_50000_routes_dept` | 50000 | salesperson | `candidates=[10010, 10011]` | `<` 严格大于（50000=边界） |
| 6 | `boundary_200000_routes_dept` | 200000 | salesperson | `candidates=[10010, 10011]` | `<=` |
| 7 | `no_dm_throws_40402` | 60000 | 无 dept_manager | `BizException(40402)` | 找不到候选人 |
| 8 | `or_sign_required_true` | 60000 | or_sign_required=true | `orSignRequired=true` 字段透出 | P1-1 OR 语义 |
| 9 | `null_threshold_match` | 999999 | salesperson | 命中 NULL 阈值节点 | 无限额兜底 |

**关键**：`BigDecimal.compareTo` 而非 `equals`；`workflowCode` 参数新增（不再硬编码）。

### 2.4 `SkipOnLeaveRule`（5 测例 · V1.3.7 P1-2 跳过请假）

| # | 用例 | candidates | HR 状态 | 期望 |
|---|------|-----------|---------|------|
| 1 | `all_on_duty` | [10010, 10011] | 全 ON_DUTY | 无 SKIPPED，current=10010 |
| 2 | `partial_skip` | [10010(在岗), 10011(请假)] | 10011=ON_LEAVE | 10011 SKIPPED，current=10010 **不切换**（P1-2 首次分配确定性） |
| 3 | `all_skipped_advance` | [10010(请假), 10011(请假)] | 全 ON_LEAVE | `nodeSkipped=true`，返回推进信号给 ApprovalService |
| 4 | `mixed_with_trip` | [10010(在岗), 10011(出差), 10012(请假)] | 10011=ON_TRIP / 10012=ON_LEAVE | 10011+10012 SKIPPED |
| 5 | `feign_degraded_no_skip` | [10010, 10011] | Feign 调用失败 | 降级为"全员不跳过"（V1.3.7 红线）+ `@AuditLog` 记录降级事件 |
| 6 | `feign_timeout_fallback` | [10010, 10011] | Feign 1.5s 超时 | 同上（防止审批阻塞） |
| 7 | `audit_log_on_skip` | [10010(在岗), 10011(请假)] | 10011=ON_LEAVE | `sys_audit_log` 写入 `skipped_user_id=10011, skip_reason=ON_LEAVE, hr_leave_id=HR-...` |

### 2.5 `ApprovalTimeoutWatcher`（6 测例 · P1-3 4 通道 + 失败重试）

| # | 用例 | 场景 | 期望 |
|---|------|------|------|
| 1 | `scan_marks_overdue` | approval_id=2 超时 | `is_overdue=true, overdue_at=NOW` |
| 2 | `4_channels_parallel` | 触发推送 | 4 通道全部执行：`stream:notify` + Email + `stream:app-push` + WechatWork |
| 3 | `redis_failure_retry_3` | Redis Stream 不可用 | 指数退避 1s/2s/4s 重试 3 次 |
| 4 | `all_channels_failed_50003` | 4 通道全失败 | `BizException(50003)` + `platform_audit_log.notify_failed` |
| 5 | `urgent_does_not_reset` | 二次催办 | 4 通道推送 + `timeout_at` **不变**（V1.3.7 红线） |
| 6 | `nacos_config_change` | 修改 `timeout-hours=12` | 下次扫描用新阈值（无需重启） |

### 2.6 `WorkflowEventPublisher`（5 测例 · Redis Stream）

| # | 用例 | 期望 |
|---|------|------|
| 1 | `publish_created_xadd` | `XADD stream:notify * bizType APPROVAL bizId ...` |
| 2 | `publish_4_channels` | 4 通道并行 + 失败重试 |
| 3 | `serialize_map_format` | `Map<String,Object>` 含 `bizType/bizId/targetUserId/title/ts/metadata` |
| 4 | `integrate_redis_stream_template` | 复用 `core/redis/RedisStreamTemplate`（V1.3.4 委外） |
| 5 | `ack_on_success_xpending_on_fail` | 处理成功 `XACK`，失败 `XPENDING` + 告警 |

### 2.7 Service 层（4 × N 测例 · 28 测例）

| Service | 测例 | 覆盖点 |
|---------|------|--------|
| `WorkflowService` (10) | `create_custom_workflow` / `create_duplicate_code_throws_40905` / `list_active_workflows` / `update_workflow_threshold_monotonic` / `update_in_use_throws_40906` / `delete_builtin_throws_40904` / `delete_custom_soft_del` / `test_workflow_no_persist` / `nodes_json_aes_encrypted` / `audit_log_after_commit` | BR-1/2/3/4/5/6/7 |
| `ApprovalService` (15) | `create_60k_quote_routes_dept_manager` / `create_30k_self_approve` / `create_250k_routes_gm` / `dept_manager_60k_self` / `or_sign_partial_skip` / `or_sign_full_required` / `all_skipped_advance` / `approve_audit_log` / `double_approve_throws_40904` / `reject_reason_required_40009` / `reject_notifies_applicant` / `urge_does_not_reset_timeout` / `my_pending_excludes_others` / `pending_excludes_skipped` / `inactive_workflow_throws_40906` | AC-1.2.2 + AC-1.2.3 全分支 |
| `ApprovalTimeoutService` (4) | `xxljob_cron_trigger` / `distributed_lock_setnx` / `failure_alert` / `nacos_refresh_immediate` | XXL-JOB + 分布式锁 |
| `WorkflowConfig` (3) | `nacos_change_timeout_hours` / `nacos_change_scan_cron` / `refresh_scope_reload` | Nacos 热更新 |

**覆盖率目标**：工具类 100% / Service ≥ 90% / Controller 不计入（集成覆盖）。

---

## 三、4 处 V1.3.7 P1 修补专项测试（独立章节·Comprehensive 必选）

### 3.1 P1 修补 ①：OR 会签 + `candidates: List<Long>` 字段

**修补背景**：Story 1.1 `QuoteApprovalRouter.route()` 旧版 `LIMIT 1` 取第一个 dept_manager，导致请假时全员被阻塞；V1.3.7 升级为返回完整 `candidates: List<Long>`，并新增 `orSignRequired` 字段支持任一会签。

**测试设计**（8 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-1-1 | `candidates_field_not_limited` | dept_manager 角色 2 个 ACTIVE 用户 | 返回 `candidates=[10010, 10011]` 完整列表 | 🔴 H |
| P1-1-2 | `or_sign_false_picks_first` | `orSignRequired=false` | 首次分配 10010（user_id ASC） | 🟡 M |
| P1-1-3 | `or_sign_true_any_passes` | `orSignRequired=true` + 10011 先 approve | 推进到下一节点（**任一会签**） | 🔴 H |
| P1-1-4 | `or_sign_audit_partial` | 10010 + 10011 全部 WAITING 留痕 | `sys_audit_log` 含 `candidates_size=2, or_sign_required=true` | 🟡 M |
| P1-1-5 | `or_sign_rejected_by_one` | OR 会签下 10010 reject | 整单 REJECTED（OR 语义下 reject 即终止） | 🔴 H |
| P1-1-6 | `boundary_first_deterministic` | 同一 amount 多次调用 | `current_approver_user_id` 恒等于 candidates[0]（避免抖动） | 🟡 M |
| P1-1-7 | `candidates_json_persisted` | `sys_approval_record.candidates` | 写入 JSON 数组 `[10010, 10011]` | 🟡 M |
| P1-1-8 | `regression_story_11_2param` | 旧 2 参签名 `route(amount, userId)` 仍可用 | 兼容 Story 1.1 35 测例不破 | 🔴 H |

**关联 AC**：AC-1.2.2 THEN.1-2 + AC-1.2.3 BR-11/17/22。

**风险等级**：🔴 **High**（影响业务核心路由 + Story 1.1 兼容）。

### 3.2 P1 修补 ②：跳过请假用户（SkipOnLeaveRule + HrEmployeeClient Feign）

**修补背景**：客户原话"经理请假了不能阻塞业务员报价"——V1.3.7 升级 SkipOnLeaveRule 调 HR 模块查 `hr_employee.status`，ON_LEAVE/ON_TRIP/DISABLED/RESIGNED 用户标记 SKIPPED。

**测试设计**（8 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-2-1 | `partial_skip_keeps_first` | 10010 在岗 / 10011 请假 | 10011 SKIPPED + current 仍 10010（不切换） | 🟡 M |
| P1-2-2 | `all_skipped_advance_node` | 全员请假 | `nodeSkipped=true` + 推进下一节点 | 🔴 H |
| P1-2-3 | `feign_failure_degraded` | HR Feign 调用失败 | 降级为"全员不跳过" + `@AuditLog` 记录降级 | 🟡 M |
| P1-2-4 | `feign_timeout_2s_fallback` | HR Feign 超时 1.5s | 降级不阻塞审批 | 🟡 M |
| P1-2-5 | `audit_skip_reason` | 10011 SKIPPED | `sys_audit_log.action=approval.skip_on_leave` 含 `hr_leave_id` | 🟡 M |
| P1-2-6 | `resigned_also_skipped` | 10012=RESIGNED | 标记 SKIPPED + `skip_reason=RESIGNED` | 🟢 L |
| P1-2-7 | `mixed_3_candidates` | [在岗, 出差, 请假] | 后两者 SKIPPED，current=第一人 | 🟡 M |
| P1-2-8 | `hr_module_not_ready_降级` | Story 1.2 实施时 HR 模块未就绪 | Feign 404 → 降级 + 审计 + 业务可继续 | 🟡 M |

**关联 AC**：AC-1.2.3 BR-18/19/20 + AC-1.2.3 Case A/B/C。

**风险等级**：🟡 **Medium**（降级路径 + 审计完整性是核心）。

### 3.3 P1 修补 ③：Redis Stream 4 通道并行 + 失败重试

**修补背景**：Story 1.1 仅 `stream:notify` 单通道；V1.3.7 升级为 4 通道（PC 红点 + 邮件 + APP 推送 + 企业微信）并行推送 + 失败重试 3 次（指数退避 1s/2s/4s）。

**测试设计**（8 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-3-1 | `parallel_4_channels` | 推送审批创建 | 4 通道并行执行（`CompletableFuture.supplyAsync`） | 🔴 H |
| P1-3-2 | `redis_failure_retry_backoff` | Redis 不可用 | 1s/2s/4s 指数退避重试 3 次 | 🟡 M |
| P1-3-3 | `email_failure_other_succeed` | Email SMTP 挂 | 邮件失败 → 其他 3 通道仍成功 | 🟡 M |
| P1-3-4 | `all_4_failed_alert` | 4 通道全失败 | 50003 + `platform_audit_log.notify_failed` | 🟡 M |
| P1-3-5 | `consumer_group_idempotent` | 同一事件被两个消费者组处理 | 幂等（同 `bizId` 二次推送不重发） | 🟡 M |
| P1-3-6 | `ack_on_success_xpending_on_fail` | 处理成功/失败 | 成功 `XACK`，失败 `XPENDING` + 告警 | 🟡 M |
| P1-3-7 | `consume_100_events_in_5s` | 压测 100 条事件 | 5s 内全部 ACK（吞吐 20+ msg/s） | 🟢 L |
| P1-3-8 | `nacos_disable_channel` | Nacos `notify-channels=[REDIS_STREAM]` | 邮件/APP/微信 不推送 | 🟢 L |

**关联 AC**：AC-1.2.4 THEN.2 + BR-25（指数退避）。

**风险等级**：🟡 **Medium**（吞吐 + 失败处理）。

### 3.4 P1 修补 ④：物理表拆分 V2__workflow_split.sql

**修补背景**：`sys_workflow.nodes_json` 是 TEXT 字段，无法做细粒度查询（OR 会签判断需按 `node_index` 取 `or_sign_required`）；V1.3.7 拆分为 `sys_workflow_node` 物理表，便于 `node_index ASC` 索引扫描。

**测试设计**（6 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-4-1 | `v2_migration_seed_4_templates` | 4 套内置模板迁移 | `sys_workflow_node` 新增 4 套模板对应行（QUOTE 3 节点 / ORDER 4 / PURCHASE 3 / PAYMENT 2） | 🔴 H |
| P1-4-2 | `field_renamed_role_to_role_code` | seed `role` → 物理表 `role_code` | V2 迁移用 `JSON_EXTRACT` 字段重命名 | 🔴 H |
| P1-4-3 | `field_renamed_node_to_node_index` | seed `node` → 物理表 `node_index` | 同上 | 🟡 M |
| P1-4-4 | `unique_index_uniq_workflow_node` | (workflow_id, node_index) 唯一 | 重复 node_index 写库 50001 | 🟡 M |
| P1-4-5 | `or_sign_required_field_exists` | `sys_workflow_node.or_sign_required` | 字段存在 + 默认 false | 🔴 H |
| P1-4-6 | `query_by_node_index_perf` | 按 `node_index ASC` 遍历 | 1000 个 workflow 平均查询 < 5ms | 🟡 M |

**关联 AC**：AC-1.2.1 BR-4（节点枚举）+ AC-1.2.3 BR-17（OR 会签）。

**风险等级**：🔴 **High**（数据迁移错即 4 套模板全毁 + 后续 Epic 全阻塞）。

**P1 修补综合风险等级**：🔴 **High**（1+2+3+4 任意一处失败影响业务核心流）→ 强制 Comprehensive 100% 覆盖。

---

## 四、集成测试设计（12 路径 · 36 测例 · Testcontainers MySQL 8 + Redis 7）

### 4.1 Testcontainers 真实环境

```yaml
# Testcontainers 启动
mysql-8.0.32: image=mysql:8.0, port=3306, db=erp_platform_test
redis-7.0-alpine: image=redis:7.0-alpine, port=6379
xxljob-admin: image=xxl-job-admin:2.4.0（可选）
```

### 4.2 WorkflowController 链路（5 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `crud_full_lifecycle` | POST → GET → PUT → GET | 200 + AES 加密 + 审计 4 行 |
| 2 | `list_with_pagination` | `GET ?pageNum=1&pageSize=20&status=ACTIVE` | 4 套内置 + 自定义按 created_at DESC |
| 3 | `test_workflow_no_persist` | `POST /workflows/1/test {amount:80000}` | 返回 matchedNode=2 + **DB 无新行** |
| 4 | `delete_builtin_throws_40904` | `DELETE /workflows/1`（QUOTE_FLOW） | 40904 BUILTIN_WORKFLOW |
| 5 | `update_in_use_workflow_throws_40906` | workflow 被审批中引用 → PUT | 40906 WORKFLOW_IN_USE |

### 4.3 ApprovalController 链路（6 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `create_60k_quote_or_sign` | POST `/approvals/create {bizType:QUOTE, amount:60000}` | candidates=[10010,10011] + `orSignRequired=false` |
| 2 | `approve_partial_skip` | 10011 SKIPPED + 10010 approve | 200 + status=APPROVED + 推进 gm 节点 |
| 3 | `approve_rejected_by_one` | OR 会签 + 10011 reject | status=REJECTED + 整单终止 |
| 4 | `reject_reason_required` | POST `/reject {reason:""}` | 40009 REASON_REQUIRED |
| 5 | `pending_excludes_skipped` | `GET /approvals/pending` | 10011 待办不含（被 SKIPPED） |
| 6 | `my_pending_returns_own` | 张三 `GET /approvals/my-pending` | 仅含自己发起的审批单 |

### 4.4 V2__workflow_split.sql 数据迁移（2 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `migrate_4_builtin_templates` | Flyway V2 启动后 `sys_workflow_node` 4 套模板行数与 `init.sql:1080-1085` 严格一致 |
| 2 | `field_renaming_consistency` | 物理表 `role_code` 字段值 = seed `role` 字段值（4 套模板逐字段对比） |

### 4.5 Redis Stream 4 通道消费者（3 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `consume_100_events_all_acked` | `notification-consumer-group` 5s 内 ACK 100 条 |
| 2 | `failure_xpending_alert` | mock 消费者异常 → `XPENDING` + 告警邮件 |
| 3 | `idempotent_duplicate_event` | 同一 `bizId` 二次推送 → 仅消费 1 次（DB 唯一索引守） |

### 4.6 与 QuoteApprovalRouter 集成（2 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `router_with_real_db` | 实时读 `sys_role.amount_threshold` + `sys_workflow_node` 链 |
| 2 | `or_sign_candidates_field` | 返回 List<Long>（V1.3.7 升级·不再 LIMIT 1） |

### 4.7 与 HR Feign 集成（2 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `hr_online_skip_leave` | WireMock 模拟 `GET /hr/employees/10011/status` → ON_LEAVE → SKIPPED |
| 2 | `hr_offline_degraded` | WireMock 模拟 503 → 降级"全员不跳过" + 审计记录 |

### 4.8 与 XXL-JOB 集成（2 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `cron_trigger_scan` | mock cron `0 */30 * * * *` → `scanAndNotify` 执行 |
| 2 | `distributed_lock_setnx` | Redis `SETNX app.workflow.timeout-scan-lock` 防并发 |

---

## 五、E2E 测试设计（5 角色 · 5 测例 · Playwright）

### 5.1 端到端场景矩阵

| # | 测例 ID | 角色 | 完整链路 | 验收点 |
|---|---------|------|---------|--------|
| 1 | `e2e/admin-workflow-config.spec.ts` | admin | 登录 → 创建 CUSTOM_FLOW → 试跑 80000 → 路由到 dept_manager | AC-1.2.1 |
| 2 | `e2e/quote-60k-approval.spec.ts` | 业务员 + 部门经理 | 业务员 60000 报价 → 10010/10011 待办出现 → 10011 请假 → 10010 审批 → 推进 gm | AC-1.2.2 + AC-1.2.3 Case A |
| 3 | `e2e/approval-or-sign-skip-leave.spec.ts` | 5 角色 | OR 会签 3 人 + 1 人请假 + 1 人出差 → 2 SKIPPED + 1 通过 | AC-1.2.3 Case C |
| 4 | `e2e/approval-24h-timeout.spec.ts` | 部门经理 + 管理员 | 创建审批 → 模拟时间推进 25h → XXL-JOB 扫描 → 4 通道推送 + 红点 | AC-1.2.4 |
| 5 | `e2e/approval-all-skipped-advance.spec.ts` | 业务员 + gm | dept_manager 全员请假 → 节点自动跳过 → 直接进 gm | AC-1.2.3 Case B |

### 5.2 5 角色验证清单

| 角色 | role_code | amount_threshold | 验证场景 |
|------|-----------|------------------|---------|
| 业务员 | salesperson | 50000 | 30000 自审 / 60000 路由 dept_manager / 看自己审批进度 |
| 部门经理 | dept_manager | 200000 | 60000 自审 / 250000 路由 gm / 待办列表 |
| 总经理 | gm | NULL | 250000 自审 / OR 会签候选人 / 二次催办 |
| 仓管 | warehouse | NULL | （不在本 Story 审批流，验证 Service Token 业务回调） |
| 财务 | finance | NULL | PAYMENT_FLOW 消费 / 财务双签（>10万） |

### 5.3 关键流程图

```
业务员(10086) → 报价 60000 → ApprovalService.createApproval
  → QuoteApprovalRouter.route() → candidates=[10010, 10011] (OR 会签)
  → SkipOnLeaveRule.apply → 10011 SKIPPED（hr.status=ON_LEAVE）
  → sys_approval_record: id=2, current=10010, orSignRequired=false
  → WorkflowEventPublisher.publishCreated → 4 通道推送
  → 10010 待办出现 → POST /approvals/2/approve → APPROVED
  → 推进 gm 节点 → sys_approval_record: id=3, current=10001
  → 4 通道推送 → 10001 待办出现 → POST /approvals/3/approve → APPROVED
  → 业务回调 ApprovalCallbackController.quoteToOrder → 转订单
```

### 5.4 24h 超时流程

```
approval_id=2 created 25h 前 → XXL-JOB 30min 扫描
  → ApprovalTimeoutWatcher.scanAndNotify
  → 批量 update is_overdue=true
  → WorkflowEventPublisher.publishOverdue → 4 通道推送
  → PC 红点 + Email + APP 推送 + 企业微信（并行）
  → sys_audit_log.action=approval.mark_overdue
  → 二次催办 POST /approvals/2/urge → 4 通道推送（timeout_at 不重置）
```

---

## 六、性能与安全测试设计

### 6.1 k6 性能测试（Comprehensive 必须）

| 场景 | 目标 | 脚本概要 |
|------|------|---------|
| `workflow_create` | 创建 P95 < 300ms | 50 并发持续 60s，POST `/workflows` |
| `workflow_list` | 列表 P95 < 1s | 30 并发持续 30s，GET `/workflows?page=1&size=20` |
| `approval_create` | 创建 P95 < 300ms | 100 并发持续 60s，POST `/approvals/create` |
| `approval_pending` | 待办 P95 < 1s | 50 并发持续 30s，GET `/approvals/pending` |
| `quote_routing` | 路由 < 50ms | 1000 并发单次，触发 `QuoteApprovalRouter.route` |
| `or_sign_4_candidates` | OR 会签 4 人 P95 < 500ms | 100 并发持续 30s，OR 节点 4 候选 |
| `redis_stream_publish` | 推送 P95 < 200ms | 100 并发持续 30s，4 通道并行 |
| `bcrypt_warmup` | 启动 5min 后编码 < 300ms | 排除 JIT 冷启动 |

**k6 脚本骨架**（`backend/perf/1.2-workflow.js`）：

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
export const options = {
  vus: 100, duration: '60s',
  thresholds: {
    http_req_duration: ['p(95)<300'],
    http_req_failed: ['rate<0.001']
  }
};
export default () => {
  const res = http.post('http://localhost:8080/api/v1/approvals/create',
    JSON.stringify({bizType:'QUOTE', bizId:`BJ${Date.now()}`, amount:60000, applicantUserId:10086}),
    {headers:{'Content-Type':'application/json', 'Authorization':`Bearer ${__ENV.TOKEN}`}});
  check(res, {
    '200': r => r.status === 200,
    'or_sign_false': r => JSON.parse(r.body).data.orSignRequired === false,
    'candidates_2': r => JSON.parse(r.body).data.candidates.length === 2
  });
  sleep(1);
};
```

### 6.2 OWASP ZAP 安全扫描

| 扫描类型 | 配置 | 通过条件 |
|---------|------|---------|
| **Baseline Scan** | `zap-baseline.py -t http://localhost:8080 -l High` | 0 High 风险 |
| **IDOR / 越权** | 张三(业务员) 调 admin `/workflows` POST | 40303 ACCESS_DENIED |
| **状态机越权** | approval.status=APPROVED → 重复 POST `/approve` | 40904 STATE_MACHINE |
| **Service Token 伪造** | 伪造 `X-Service-Token` 调 `/approvals/callback/quote-to-order` | 401 INVALID_TOKEN |
| **金额越权** | 业务员提交 250000 报价 → 试图自审 | 路由到 gm（无越权） |
| **候选人越权** | user 10099（不在 candidates）调 `/approve` | 40303 NOT_IN_CANDIDATES |
| **审计完整性** | 任意审批操作 → 检查 `sys_audit_log` | 100% 留痕 |

### 6.3 SonarQube 静态扫描

- 无 `System.out.println(candidates)` / `log.info(amount)` 泄露敏感字段
- `BigDecimal.compareTo` 而非 `equals`（grep `\.equals\(.*amount` → 0 命中）
- `@AuditLog` 覆盖所有 `WorkflowService` / `ApprovalService` 写方法
- `HrEmployeeClient` Feign 调用必须有 `try/catch` 降级（grep `HrEmployeeClient.*getStatus` → 必须 try/catch）
- 无 Critical 漏洞

### 6.4 审计日志 100% 留痕

- `WorkflowService` CRUD 5 方法全部 `@AuditLog(module="workflow")`
- `ApprovalService` 6 方法（create/approve/reject/skip/urge/cancel）全部 `@AuditLog(module="approval")`
- AFTER_COMMIT 触发 + 失败兜底写 `sys_audit_log_error`
- 保留期 ≥ 1 年（Story 1.3 系统参数配置清理策略，本 Story 检查策略文档已存在）

---

## 七、风险画像（5 维度 · Comprehensive 必须）

| 风险维度 | 严重度 | 概率 | 风险描述 | 缓解措施 | 残余风险 |
|---------|--------|------|---------|---------|---------|
| **功能** | 🔴 H | M | OR 会签 + 跳过请假涉及 4 处 P1 修补，多点失败导致审批阻塞或误审 | 38 测例覆盖 4 处 P1 修补 + 状态机守卫 + 降级路径 | 中 |
| **数据** | 🔴 H | L | V2__workflow_split.sql 物理表迁移字段重命名错（`role` → `role_code`）→ 4 套模板全毁 | 集成测试 V2 迁移逐字段对比 + 灰度 + 回滚脚本 | 低 |
| **性能** | 🟡 M | M | OR 会签 4 候选 + 4 通道推送叠加可能拉高 P95 | k6 性能基线 + 路由 P95<50ms / 推送 P95<200ms / OR 4 人 P95<500ms | 中 |
| **安全** | 🔴 H | L | Service Token 伪造 / 状态机越权 / 候选人越权 | OWASP ZAP 6 类越权扫描 + @PreAuthorize 严格守卫 + 状态机 40904 | 低 |
| **审计** | 🟡 M | M | `@AuditLog` 切面在 4 通道推送失败时可能漏记 | AFTER_COMMIT + 失败兜底表 + `notify_failed` 告警 | 中 |

**综合风险等级**：🔴 **High**（3 H + 2 M），建议 **Go with strict monitoring** + 4 处 P1 修补全闭环 + 性能基线达标方可发布。

---

## 八、准入与退出条件

### 8.1 准入条件（Go / No-Go）

**Go 必须满足全部 9 条**：

1. Architect 评审 9.0/10 通过、4 处 P1 修补全部对齐（已闭环）
2. `application-platform.yml` 含 `app.workflow.*` Nacos 配置 + Flyway V2 迁移
3. Testcontainers MySQL 8 + Redis 7 启动成功 + `sys_workflow_node` + `sys_approval_record` 2 表创建成功
4. OpenAPI 契约 `E1-Workflow` tag 9 端点 schema 100% 补全
5. 4 套内置模板种子幂等执行无报错（QUOTE/ORDER/PURCHASE/PAYMENT 节点数 + 阈值一致）
6. V2__workflow_split.sql 字段重命名（`role` → `role_code` / `node` → `node_index`）通过集成测试
7. HrEmployeeClient Feign 调用 Story 1.2 dev 实施时 mock 就绪（Story 1.3 替换为真实）
8. logback 配置禁止打印 `nodesJson` / `candidates` 明文
9. Postman 集合 15 用例全部可执行

**No-Go 触发**：V2 迁移失败 / OR 会签语义错误 / 4 通道推送任一失败 / `@AuditLog` 漏覆盖。

### 8.2 退出条件（Exit Criteria）

- 单测覆盖率 ≥ 80%（工具类 100%、Service ≥ 90%）
- 集成测试 36 测例 100% 通过
- E2E 5 角色 5 测例 100% 通过
- k6 性能基线：路由 P95<50ms / 推送 P95<200ms / 创建 P95<300ms / 待办 P95<1s
- 4 处 P1 修补 38 测例 100% 通过
- OWASP ZAP 0 High 风险（含 6 类越权扫描）
- SonarQube 0 Critical
- 审计日志 100% 留痕验证（6 类审批操作）
- 残余风险 ≤ 5 条已记录到 `docs/qa/reviews/1.2-审批工作流配置.md`

---

## 九、残余风险与推荐

| # | 残余风险 | 等级 | 推荐动作 |
|---|---------|------|---------|
| R1 | V2 物理表迁移灰度策略未明 | 🟡 M | Story 1.3 部署阶段灰度 + 回滚脚本（保留 `sys_workflow.nodes_json` 60 天） |
| R2 | `HrEmployeeClient` Feign 降级路径（HR 模块未就绪时） | 🟡 M | Story 1.2 实施时 mock 就绪 + Story 1.3 替换为真实 Feign |
| R3 | 4 通道推送 `stream:app-push` 与 `stream:notify` 消费者组重复 | 🟢 L | dev 收敛为单通道 `stream:notify`（4 通道在 publisher 内部分发） |
| R4 | OR 会签 4+ 人场景 P95 可能逼近 500ms | 🟡 M | k6 性能基线 + 必要时加 Redis 缓存 candidates |
| R5 | 审计日志清理策略仅文档化未实装 | 🟡 M | Story 1.3 系统参数任务闭环 + JIRA 跟踪 |
| R6 | 5 条 UI 红线（代选厂商等）本 Story 不涉及但下游 Epic 必查 | 🟢 L | 在 Story 1.2 验收时 grep 验证 5 个正则 |
| R7 | `QuoteApprovalRouter` 升级 2 参 → 3 参签名影响 Story 1.1 兼容 | 🟡 M | dev 加 2 参重载方法 + Story 1.1 35 测例回归不破 |

---

## 十、QA 签字与下一步

- **测试设计级别**：Comprehensive
- **测试用例总数**：**60+**（单测 ≥ 30 + 集成 36 + E2E 5）+ P1 专项 38 → 98 测例
- **自动化率**：98%（手工仅限 ZAP 主动扫描 + 视觉验收）
- **关键风险**：4 处 P1 修补（OR 会签 + 跳过请假 + 4 通道 + 物理表迁移）+ 状态机越权
- **状态变更**：Story 1.2 `AwaitingTestDesign` → `TestDesignComplete`

🎯 HANDOFF TO dev: *develop-story 1.2
