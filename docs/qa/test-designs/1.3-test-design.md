# Story 1.3 - 系统参数 / 数据字典 / HR Feign 真实集成 / 性能与安全闭环 · 测试设计方案

> **测试架构师**：orchestrix QA · 商鞅（Test Engineer & Quality Architect）
> **设计日期**：2026-06-10
> **合同**：XP-ZPF202606082405 · 昆山佰泰胜专属 ERP 系统 V1.3.7
> **Story 范围**：1.3 系统参数 / 数据字典 / HR Feign 真实集成 / 性能与安全闭环（Epic 1 / Story 1.3 · 5 AC + 36 checkbox）
> **测试级别**：**Comprehensive**（complexity=XL + 安全敏感 + 5 维风险 + 5 维测试金字塔 + 5 角色 E2E + 3 k6/ZAP/Sonar 横切 + Architect 8.5/10 CONDITIONAL）
> **关联**：[→ Story 1.3 设计](../../backend/docs/stories/1.3-系统参数与HR集成.md) · [→ Architect 评审](../../docs/architecture/story-reviews/1.3-review.md) · [→ QA Story 1.1 模板](./1.1-test-design.md) · [→ QA Story 1.2 模板](./1.2-test-design.md) · [→ 架构 §6/§7/§13/§15](../../docs/architecture/index.md) · [→ OpenAPI](../../backend/spec/openapi.yaml)

---

## 一、测试设计总览

### 1.1 覆盖矩阵（AC → TestCase → Automation）

| AC | 业务目标 | 单测 ID | 集成测 ID | E2E 测 ID | 性能 | 安全 | 自动化率 |
|----|---------|---------|----------|----------|------|------|---------|
| **AC-1.3.1** | 数据字典 CRUD + 6 类 30+ 种子 | `DictServiceTest` (8) | `DictControllerIntegrationTest` (5) | `e2e/admin-dict-crud.spec.ts` | k6 `dict_query_p95<200ms` | OWASP ZAP IDOR 越权 | 95% |
| **AC-1.3.2** | 系统参数 CRUD + 单据编号生成器 | `ParamServiceTest` (6) / `DocNoGeneratorTest` (4) | `ParamControllerIntegrationTest` (4) | `e2e/admin-param-update.spec.ts` | k6 `param_update_p95<200ms` | — | 95% |
| **AC-1.3.3** | 金额阈值全局 + 双轨热更新 | `ThresholdServiceTest` (5) | `ThresholdControllerIntegrationTest` (3) | `e2e/threshold-hot-update.spec.ts` | k6 `threshold_nacos_p95<50ms` | ZAP 金额越权 | 95% |
| **AC-1.3.4** | HR Feign 真实集成 + 降级 | `HrEmployeeClientWireMockTest` (4) | 复用 Story 1.2 `P1SkipOnLeaveTest` 8 测例 | `e2e/hr-feign-real.spec.ts` | k6 `feign_call_p95<200ms` | ZAP Service Token | 95% |
| **AC-1.3.5** | k6 性能 + ZAP 安全 + Sonar 静态 + 审计清理 | `AuditCleanupTaskTest` (3) / `AuditLogArchiveServiceTest` (3) | `AuditCleanupIntegrationTest` (3) | `e2e/audit-cleanup.spec.ts` | k6 8 脚本 | ZAP + Sonar | 95% |
| **T0 V3 物理表迁移** | 7 张新表 + 字段对齐 + 索引 | `V3__system_params.sql` 集成测试 | `MigrationE2ETest#v3_7_tables` | — | 迁移 < 10s | 字段非空一致性 | 100% |
| **横切 跨模块契约** | 11 Epic 126 处引用 | 23 跨模块契约测例（销售 3 + 生产 3 + 委外 3 + 采购 3 + 财务 3 + HR 2 + 报表 2 + APP/报价/订单/采购 各 1） | — | — | — | — | 100% |

**覆盖率统计**：5 AC + 7 横切 → **70+ 测例**（单测 26 + 集成 14 + HR 4 + 审计 6 + 跨模块 23 = 73 测例 + k6 8 脚本 + ZAP 6 类 + Sonar 4 项 + E2E 5 角色 + 回归 Story 1.1 35 + Story 1.2 151 = **262 测例回归**）

### 1.2 V1.3.7 P1 修补 → 测试要求映射

| P1 修补 | 验证方式 | 用例数 |
|---------|---------|--------|
| ① 双轨阈值（DB + Nacos），Nacos 优先 | `ThresholdServiceTest` 5 边界 + `ThresholdControllerIntegrationTest` 3 集成 + 1.1 35 测例回归 | 9 + 1.1 35 = 44 |
| ② 数据字典 6 类 30+ 种子幂等 | `DictServiceTest` 8 + `DictControllerIntegrationTest` 5 + 跨模块契约 9 | 22 |
| ③ 金额阈值变更写 `sys_change_log` + 推送 Nacos | `ChangeLogServiceTest` 3 + `ThresholdServiceTest` THEN.4 + 1.2 残余 R5 闭环验证 | 6 |
| ④ HR Feign 真实集成 + 删 Fallback | `HrEmployeeClientWireMockTest` 4 + 1.2 `P1SkipOnLeaveTest` 8 回归 + Fallback `@Deprecated` 验证 | 12 + 1.2 8 = 20 |
| ⑤ 审计清理 `@Scheduled` + 1 年保留 + 归档表 | `AuditCleanupTaskTest` 3 + `AuditLogArchiveServiceTest` 3 + `AuditCleanupIntegrationTest` 3 + 1.2 残余 R5 闭环 | 12 |

**P1 总计**：5 修补 × 平均 14 测例 = **70 独立验证点** → 100% 覆盖 5 处 P1 修补

### 1.3 5 维测试金字塔

```
                  /\
                 /  \           5 角色 E2E (Playwright)
                / E2E \         5 测例
               /______\
              /        \        k6 性能 (8 脚本)
             /  性能    \       8 测例
            /____________\
           /              \     ZAP 安全 + Sonar 静态 (10 项)
          /  安全 + 静态  \    10 测例
         /________________\
        /                  \   集成测试 (Testcontainers + WireMock)
       /      集成          \  14 + 4 + 6 + 23 = 47 测例
      /______________________\
     /                        \ 单元测试 (JUnit 5 + Mockito)
    /          单元            \ 26 + 0 = 26 测例
   /____________________________\

总计：26 + 47 + 10 + 8 + 5 = 96 测例 + 186 回归 = 282 测例回归
```

---

## 二、单元测试设计（5 服务 + 4 工具 = 26 测例）

### 2.1 `DictService`（8 测例 · 6 类字典 + CRUD + 软删 + 引用计数）

| # | 用例 | 输入 | 期望 | 验证点 |
|---|------|------|------|--------|
| 1 | `list_by_type` | type=`MATERIAL_CATEGORY` | 5 个种子（钢材/铝材/铜材/塑料/其他） | 6 类字典 30+ 种子 |
| 2 | `create_dict` | type=`PROCESS_TYPE` code=`CNC` name=`CNC 加工` | 200 + 审计 1 行 | BR-1.3.1.1 |
| 3 | `create_duplicate_code` | type=`PROCESS_TYPE` code=`CNC` | `BizException(40908) DICT_CODE_DUPLICATE` | 唯一索引 |
| 4 | `update_dict` | name=`CNC 加工` → `CNC 精加工` | 200 + sys_change_log before/after | BR-1.3.1.2 |
| 5 | `delete_builtin` | 字典类型 `MATERIAL_CATEGORY` | `BizException(40909) BUILTIN_DICT` | 4 套内置不可删 |
| 6 | `delete_in_use` | 字典被业务引用 | `BizException(40907) DICT_IN_USE` | 软删 + 引用计数 |
| 7 | `delete_soft` | 自定义字典无引用 | 200 + is_active=false | 软删语义 |
| 8 | `audit_log_after_commit` | 任意写操作 | sys_audit_log 1 行 + sys_change_log 1 行 | AFTER_COMMIT |

### 2.2 `ParamService`（6 测例 · 3 类参数 + Nacos 推送 + 5min TTL）

| # | 用例 | 场景 | 期望 |
|---|------|------|------|
| 1 | `list_by_group_biz_doc_no` | group=`BIZ_DOC_NO` | 4 类单据（QUOTE/ORDER/PURCHASE/PAYMENT）+ 每类 4 段（年/月/日/流水） |
| 2 | `get_by_key` | key=`app.cache-ttl` | 24h 默认值 |
| 3 | `update_param` | value=`48h` | 200 + Nacos 推送 + sys_change_log |
| 4 | `refresh_all` | 手动触发 | 所有节点 5s 内生效 |
| 5 | `audit_log_after_commit` | 任意写操作 | sys_audit_log 1 行 + sys_change_log 1 行 |
| 6 | `change_log_diff` | 改前 `24h` → 改后 `48h` | before/after JSON 序列化 |

### 2.3 `ThresholdService`（5 测例 · 双轨 + 热更新 + 优先级）

| # | 用例 | 输入 | 期望 | 优先级验证 |
|---|------|------|------|----------|
| 1 | `get_threshold_nacos_priority` | Nacos `app.workflow.thresholds.QUOTE.dept_manager=250000` | 250000 | Nacos 优先 |
| 2 | `get_threshold_db_fallback` | Nacos 缺失 | DB `sys_global_threshold` 值 | DB 回退 |
| 3 | `get_threshold_role_fallback` | Nacos + global_threshold 都缺失 | `sys_role.amount_threshold` 值 | 三级回退 |
| 4 | `update_threshold_dual_write` | 改 dept_manager 200000 → 250000 | Nacos + DB + sys_change_log 三写 | 双轨同步 |
| 5 | `nacos_refresh_5s` | 改后 5s 内 | `@RefreshScope` bean 重建 | 热更新 |

### 2.4 `DocNoGenerator`（4 测例 · 单据编号 + 100 并发不重复）

| # | 用例 | 输入 | 期望 |
|---|------|------|------|
| 1 | `quote_format` | bizType=QUOTE date=2026-06-10 | `QUOTE-20260610-0001` |
| 2 | `next_increment` | 连续生成 3 个 | 0001 → 0002 → 0003 |
| 3 | `concurrent_100_unique` | 100 并发线程 | 100 个唯一编号（DB 唯一索引守） |
| 4 | `daily_reset` | 跨日 | 新一天从 0001 重启 |

### 2.5 `ChangeLogService`（3 测例 · 变更追溯）

| # | 用例 | 场景 | 期望 |
|---|------|------|------|
| 1 | `record_change` | 改阈值 dept_manager 200000→250000 | 1 行 sys_change_log 含 before/after JSON |
| 2 | `query_by_entity` | entity=`threshold` | 倒序返回 10 条 |
| 3 | `audit_log_linked` | 变更后查 sys_audit_log | 1:1 关联（change_log.id = audit_log.biz_id） |

### 2.6 `AuditCleanupTask`（3 测例 · 1 年保留 + 归档）

| # | 用例 | 场景 | 期望 |
|---|------|------|------|
| 1 | `archive_1_year_old` | 当前时间 2027-01-01 + 1.5 年前数据 | 归档到 `sys_audit_log_archive` + 主表删除 |
| 2 | `keep_recent_1_year` | 1 年内数据 | 主表保留，不归档 |
| 3 | `zone_shanghai` | 时区 Asia/Shanghai | 凌晨 3 点执行（不漂移） |

### 2.7 `AuditLogArchiveService`（3 测例 · 归档查询）

| # | 用例 | 场景 | 期望 |
|---|------|------|------|
| 1 | `archive_insert` | 1.5 年前数据 | 写入 `sys_audit_log_archive` |
| 2 | `archive_query` | 查归档表 1.5 年前数据 | 200 + 完整记录 |
| 3 | `archive_restore` | 误归档恢复 | 主表恢复（admin 权限） |

**覆盖率目标**：工具类 100% / Service ≥ 90% / Controller 不计入（集成覆盖）。

---

## 三、5 维测试金字塔（4-5 节）

### 3.1 集成测试设计（14 端点集成 + 4 HR WireMock + 6 审计 = 24 测例 · Testcontainers MySQL 8 + Redis 7 + WireMock）

```yaml
# Testcontainers 启动
mysql-8.0.32: image=mysql:8.0, port=3306, db=erp_platform_test
redis-7.0-alpine: image=redis:7.0-alpine, port=6379
wiremock: image=wiremock:3.x, port=8080  # mock erp_business.hr
```

#### 3.1.1 DictController 链路（5 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `crud_full_lifecycle` | POST → GET → PUT → DELETE → GET（软删后） | 200 + 软删 is_active=false |
| 2 | `list_with_type_filter` | `GET /api/v1/dicts?type=MATERIAL_CATEGORY` | 5 个种子 |
| 3 | `create_duplicate_code_throws_40908` | 重复 code | 40908 |
| 4 | `delete_in_use_throws_40907` | 字典被业务引用 | 40907 |
| 5 | `audit_log_persisted` | 任意写 → 查 sys_audit_log | 1 行 + sys_change_log 1 行 |

#### 3.1.2 ParamController 链路（4 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `update_param_nacos_pushed` | PUT → 5s 内 Nacos 配置变更 | @NacosValue 重新加载 |
| 2 | `refresh_all_immediate` | POST /api/v1/params/refresh | 所有节点 5s 内生效 |
| 3 | `doc_no_generator_100_concurrent` | 100 并发 POST /api/v1/quotes | 100 个唯一单据号 |
| 4 | `change_log_diff` | PUT 前/后值对比 | sys_change_log before/after JSON |

#### 3.1.3 ThresholdController 链路（3 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `update_nacos_db_dual_write` | PUT /api/v1/thresholds | Nacos + DB + sys_change_log 三写 |
| 2 | `nacos_priority_over_db` | 启动时 Nacos 有值 | `getThreshold()` 返回 Nacos 值（不读 DB） |
| 3 | `db_fallback_when_nacos_missing` | Nacos 缺失 | 回退 DB `sys_global_threshold` |

#### 3.1.4 ChangeLogController 链路（2 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `query_by_entity` | `GET /api/v1/changelogs?entity=threshold` | 倒序 10 条 |
| 2 | `query_by_entity_id` | `?entity=threshold&entityId=2` | 单条变更详情 |

#### 3.1.5 HR Feign WireMock 集成（4 测例）

| # | 场景 | WireMock Mock | 期望 |
|---|------|---------------|------|
| 1 | `hr_online_return_on_leave` | `GET /api/v1/hr/employees/10011/status` → 200 ON_LEAVE | SkipOnLeaveRule SKIPPED + 审计 |
| 2 | `hr_online_return_on_duty` | `GET /api/v1/hr/employees/10010/status` → 200 ON_DUTY | SkipOnLeaveRule 不跳过 |
| 3 | `hr_offline_503` | WireMock 503 | 降级为"全员不跳过" + 审计 |
| 4 | `hr_offline_timeout` | WireMock 1.5s 延迟 | Feign 2s 超时 → 降级 + 审计 |

#### 3.1.6 AuditCleanup 集成（3 测例）

| # | 场景 | 步骤 | 断言 |
|---|------|------|------|
| 1 | `scheduled_run_archive` | 手动触发 `@Scheduled` 任务 | 1.5 年前数据归档 |
| 2 | `no_recent_data_skipped` | 仅有 1 年内数据 | 不归档（保留） |
| 3 | `archive_query_admin` | 归档后 admin 查询 | 200 + 完整记录 |

#### 3.1.7 V3__system_params.sql 数据迁移（2 测例）

| # | 场景 | 断言 |
|---|------|------|
| 1 | `migrate_7_new_tables` | Flyway V3 启动后 7 张表（sys_dict / sys_dict_type / sys_param / sys_change_log / sys_global_threshold / sys_audit_log_archive + 业务相关）创建成功 |
| 2 | `audit_log_archive_like_main` | `CREATE TABLE sys_audit_log_archive LIKE sys_audit_log` + 索引重建 |

### 3.2 E2E 测试设计（5 角色 · 5 测例 · Playwright）

| # | 测例 ID | 角色 | 完整链路 | 验收点 |
|---|---------|------|---------|--------|
| 1 | `e2e/admin-dict-crud.spec.ts` | admin | 登录 → 查字典 → 改字典 → 验证业务系统实时生效 | AC-1.3.1 |
| 2 | `e2e/admin-param-update.spec.ts` | admin | 登录 → 改 `app.cache-ttl` 24h → 48h → APP 端下次访问用新值 | AC-1.3.2 |
| 3 | `e2e/threshold-hot-update.spec.ts` | 业务员 + 部门经理 | 业务员 220000 报价 → 旧阈值路由到 gm → admin 改 dept_manager 200000→250000 → 5s 后再报 → 路由到 dept_manager | AC-1.3.3 |
| 4 | `e2e/hr-feign-real.spec.ts` | 5 角色 | 真实 HR Feign 调用 → dept_manager 10011 请假 → 10010 审批 → 推进 gm | AC-1.3.4 |
| 5 | `e2e/audit-cleanup.spec.ts` | admin | 手动触发 `@Scheduled` → 1.5 年前数据归档 → 查主表无 → 查归档表有 | AC-1.3.5 |

### 3.3 跨模块契约测试（11 Epic 23 测例）

| Epic | 消费内容 | 测例数 |
|------|---------|--------|
| E2 销售 | 6 类字典 + `sys_global_threshold` QUOTE/ORDER 业务 | 3 |
| E5 生产 | 工序类型 + 班别字典 | 3 |
| E6 委外 | 表处类型 + 仓库字典 | 3 |
| E8 采购 | 币种 + 物料分类字典 | 3 |
| E9 财务 | 付款阈值 + 币种字典 | 3 |
| E10 人事 | HR Feign 真实端点（已就绪） | 2 |
| E11 报表 | `sys_change_log` 变更追溯 | 2 |
| Story 1.4 APP 端 | `app.cache-ttl` 参数 + `stream:notify` 推送 | 1 |
| Story 1.5 报价 | 阈值全局配置 + 字典工序类型 | 1 |
| Story 1.6 订单 | 阈值全局配置 + 字典币种 | 1 |
| Story 1.7 采购 | 字典物料分类 + 表处类型 | 1 |

### 3.4 关键流程图

```
管理员(admin) 改 dept_manager 阈值 200000 → 250000
  → PUT /api/v1/thresholds
  → ThresholdService.updateThreshold(bizType=QUOTE, roleCode=dept_manager, value=250000)
  → 双轨写入:
    ① Nacos: app.workflow.thresholds.QUOTE.dept_manager=250000
    ② DB: sys_global_threshold (bizType=QUOTE, roleCode=dept_manager, threshold=250000)
    ③ sys_change_log: entity=threshold, before=200000, after=250000
  → Nacos Config LongPolling 通知所有 erp-platform 实例
  → @RefreshScope WorkflowConfig bean 重建
  → @NacosValue("app.workflow.thresholds.QUOTE.dept_manager") 自动刷新
  → QuoteApprovalRouter.getThreshold() 下次路由实时生效
  → 业务员提交 220000 报价 → 路由到 dept_manager（而不是 gm）✓
  → 审计 1 行 + sys_change_log 1 行 + sys_audit_log 1 行
```

### 3.5 24h 审计清理流程

```
@Scheduled(cron = "0 0 3 * * ?", zone = "Asia/Shanghai")
  → AuditCleanupTask.execute() 每天凌晨 3 点
  → SELECT * FROM sys_audit_log WHERE created_at < NOW() - INTERVAL 1 YEAR
  → 1.5 年前数据:
    ① INSERT INTO sys_audit_log_archive SELECT * FROM sys_audit_log WHERE id IN (...)
    ② DELETE FROM sys_audit_log WHERE id IN (...)
  → sys_audit_log_archive 5 年保留
  → 监控告警：清理失败 → 飞书/邮件 → devops oncall
```

---

## 四、性能与安全测试设计

### 4.1 k6 性能基线（8 脚本 · Comprehensive 必须）

| 场景 | 目标 | 脚本概要 |
|------|------|---------|
| `login_p95<200ms` | 100 并发持续 60s | 复用 Story 1.1 |
| `dict_query_p95<200ms` | 50 并发持续 30s | `GET /api/v1/dicts?type=MATERIAL_CATEGORY` |
| `param_update_p95<200ms` | 50 并发持续 30s | `PUT /api/v1/params/{key}` + Nacos 推送 |
| `threshold_nacos_p95<50ms` | 1000 并发单次 | `QuoteApprovalRouter.getThreshold()` |
| `feign_call_p95<200ms` | 50 并发持续 30s | `HrEmployeeClient.getStatus()` 真实 Feign |
| `audit_cleanup_run` | 1.5 GB 数据清理 < 5min | 手动触发 + 监控 |
| `concurrent_doc_no_100` | 100 并发唯一 | `DocNoGenerator.next()` |
| `change_log_query_p95<300ms` | 50 并发持续 30s | `GET /api/v1/changelogs?entity=threshold` |

**k6 脚本骨架**（`backend/k6/1.3-threshold-nacos.js`）：

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
export const options = {
  vus: 1000, duration: '30s',
  thresholds: { http_req_duration: ['p(95)<50'], http_req_failed: ['rate<0.001'] }
};
export default () => {
  const res = http.post('http://localhost:8080/api/v1/quotes',
    JSON.stringify({bizType:'QUOTE', bizId:`BJ${Date.now()}`, amount:220000, applicantUserId:10086}),
    {headers:{'Content-Type':'application/json', 'Authorization':`Bearer ${__ENV.TOKEN}`}});
  check(res, { 'route_to_dept': r => JSON.parse(r.body).data.candidates.includes(10010) });
  sleep(0.1);
};
```

### 4.2 OWASP ZAP 安全扫描（6 类越权）

| 扫描类型 | 配置 | 通过条件 |
|---------|------|---------|
| **Baseline Scan** | `zap-baseline.py -t http://localhost:8080 -l High` | 0 High 风险 |
| **IDOR / 越权** | 张三(业务员) 调 admin `/dicts` POST | 40303 ACCESS_DENIED |
| **状态机越权** | dict.is_active=false → 再 PUT | 40904 STATE_MACHINE |
| **Service Token 伪造** | 伪造 `X-Service-Token` 调 `/hr/employees/callback` | 401 INVALID_TOKEN |
| **金额越权** | 业务员提交 250000 试图自审 | 路由到 gm（无越权） |
| **审计完整性** | 任意 dict/param/threshold 写操作 → 检查 sys_audit_log | 100% 留痕 + sys_change_log |

### 4.3 SonarQube 静态扫描（4 项规则）

- 无 `log.*password` / `log.*dictValue` 泄露敏感字段（grep `log.*password|log.*dict` → 0 命中）
- `BigDecimal.compareTo` 而非 equals（grep `\.equals\(.*threshold` → 0 命中）
- `@AuditLog` 覆盖所有 `DictService` / `ParamService` / `ThresholdService` 写方法
- `HrEmployeeClient` 真实化后必须有 `try/catch` 降级（grep `HrEmployeeClient.*getStatus` → 必须 try/catch）
- 无 Critical 漏洞

### 4.4 审计清理测试（横切）

- `@Scheduled(cron="0 0 3 * * ?", zone="Asia/Shanghai")` 任务执行 < 5min（1.5 GB 数据）
- 1 年保留期（V1.3.7 §8 红线 5）
- 归档表 `sys_audit_log_archive` 5 年保留
- 清理失败 → 飞书/邮件 → devops oncall
- JIRA 链接：`https://jira.btsheng.com/browse/XP-XXX`（待 dev 实施时补）

---

## 五、5 处 V1.3.7 P1 修补专项测试（独立章节·Comprehensive 必选）

### 5.1 P1 修补 ①：双轨阈值（DB + Nacos），Nacos 优先（5 测例）

**修补背景**：Story 1.1 `sys_role.amount_threshold` 单轨 + 启动时读 DB；Story 1.2 `WorkflowConfig` 接受 Nacos；本 Story 升级为**双轨**（Nacos 优先 + DB 回退 + sys_change_log 变更日志），3 级回退（Nacos → sys_global_threshold → sys_role.amount_threshold）。

**测试设计**（5 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-1-1 | `nacos_priority` | Nacos 有值 250000 | `getThreshold()` 返回 250000（不读 DB） | 🔴 H |
| P1-1-2 | `db_fallback_global` | Nacos 缺失 + DB global_threshold 200000 | 返回 200000 | 🟡 M |
| P1-1-3 | `db_fallback_role` | Nacos + global_threshold 都缺失 | `sys_role.amount_threshold` | 🟡 M |
| P1-1-4 | `dual_write` | PUT /api/v1/thresholds | Nacos + DB + sys_change_log 三写 | 🔴 H |
| P1-1-5 | `regression_story_11_2param` | 旧 2 参 `getThreshold(roleCode)` 仍可用 | 兼容 Story 1.1 35 测例 | 🔴 H |

**关联 AC**：AC-1.3.3 THEN.1-4 + Story 1.1 `QuoteApprovalRouter` 兼容。

**风险等级**：🔴 **High**（影响业务核心路由 + Story 1.1 兼容）。

### 5.2 P1 修补 ②：数据字典 6 类 30+ 种子幂等（5 测例）

**修补背景**：V1.3.7 红线 1（无字典硬编码）—— 业务模块（销售/采购/财务）必须从字典读，不写死硬编码；本 Story 6 类字典 30+ 种子启动幂等初始化。

**测试设计**（5 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-2-1 | `seed_6_types_30+` | 启动 `DictTypeInitializer` | 6 类字典全部种子 30+ 行 | 🔴 H |
| P1-2-2 | `idempotent_restart` | 多次重启 | 种子不重复（UNIQUE 索引守） | 🟡 M |
| P1-2-3 | `cross_module_dict_consistency` | 销售读 `PROCESS_TYPE.CNC` 与生产读 | 同一字典（无分支） | 🟡 M |
| P1-2-4 | `dict_cache_5min_ttl` | 改字典后 5min | `DictCache.getActiveByType` 自动失效 | 🟢 L |
| P1-2-5 | `builtin_dict_locked` | 4 套内置字典 | 不可删 + 不可改 code | 🟡 M |

**关联 AC**：AC-1.3.1 BR-1.3.1.1-3 + `core/model/DictCache.java` 已实装 hook。

**风险等级**：🟡 **Medium**（跨模块一致性是核心）。

### 5.3 P1 修补 ③：金额阈值变更写 `sys_change_log` + 推送 Nacos（5 测例）

**修补背景**：V1.3.7 红线 5（变更走变更日志）—— 任何金额阈值变更必须有审计 trail（before/after + 谁改的 + 何时）+ 推送 Nacos 5s 内全节点生效。

**测试设计**（5 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-3-1 | `change_log_before_after_json` | 改 dept_manager 200000→250000 | sys_change_log before/after JSON | 🔴 H |
| P1-3-2 | `change_log_linked_audit` | 查 sys_audit_log | 1:1 关联（change_log.id = audit_log.biz_id） | 🟡 M |
| P1-3-3 | `nacos_push_5s_immediate` | PUT → 5s 内 Nacos | `@NacosValue` 重新加载 | 🔴 H |
| P1-3-4 | `change_log_query_p95<300ms` | 50 并发 30s | P95 < 300ms | 🟢 L |
| P1-3-5 | `change_log_restore_admin` | 误改恢复 | admin 权限可手动改回 | 🟡 M |

**关联 AC**：AC-1.3.3 THEN.4 + T1.8 `ChangeLogController`。

**风险等级**：🟡 **Medium**（变更追溯完整性是核心）。

### 5.4 P1 修补 ④：HR Feign 真实集成 + 删 Fallback（5 测例）

**修补背景**：Story 1.2 `HrEmployeeClient` 当前是 mock + `HrEmployeeClientFallback` 兜底为"全员不跳过"；本 Story 真实化 Feign 调用，删除 `fallbackFactory` 参数（仅保降级路径逻辑），保留 `HrEmployeeClientFallback` `@Deprecated` 占位 3 个 sprint 后清理。

**测试设计**（5 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-4-1 | `feign_real_online_returns_on_leave` | WireMock mock ON_LEAVE | SkipOnLeaveRule SKIPPED + 审计 | 🔴 H |
| P1-4-2 | `feign_real_offline_503_degraded` | WireMock 503 | 降级为"全员不跳过" + 审计 | 🔴 H |
| P1-4-3 | `feign_timeout_2s_degraded` | WireMock 1.5s 延迟 | Feign 2s 超时 → 降级 | 🟡 M |
| P1-4-4 | `feign_retry_3_backoff` | WireMock 偶发 503 | `Retryer.Default(100, 1000, 3)` 指数退避 3 次 | 🟡 M |
| P1-4-5 | `fallback_deprecated_compile_only` | `HrEmployeeClientFallback` 保留 | 编译通过 + `@Deprecated` 注解 | 🟢 L |

**关联 AC**：AC-1.3.4 THEN.1-2 + T2.1 + T2.2。

**风险等级**：🟡 **Medium**（降级路径 + 审计完整性是核心）。

### 5.5 P1 修补 ⑤：审计清理 `@Scheduled` + 1 年保留 + 归档表（5 测例）

**修补背景**：Story 1.2 R5 闭环 —— `@AuditLog` AFTER_COMMIT 100% 覆盖但清理策略未实装；本 Story `@Scheduled` 任务每天凌晨 3 点 + 保留 1 年 + 归档 5 年。

**测试设计**（5 测例）：

| # | 用例 | 场景 | 期望 | 风险等级 |
|---|------|------|------|---------|
| P1-5-1 | `scheduled_cron_3am_shanghai` | 验证 cron 表达式 | `0 0 3 * * ?` + `zone="Asia/Shanghai"` | 🔴 H |
| P1-5-2 | `archive_1_year_old` | 1.5 年前数据 | 归档到 `sys_audit_log_archive` + 主表删除 | 🔴 H |
| P1-5-3 | `keep_recent_1_year` | 1 年内数据 | 主表保留，不归档 | 🟡 M |
| P1-5-4 | `archive_5_year_retention` | 归档表 5 年前数据 | 二次归档或物理删除（策略待定） | 🟢 L |
| P1-5-5 | `cleanup_failure_alert` | 归档失败 | 飞书/邮件 + devops oncall 告警 | 🟡 M |

**关联 AC**：AC-1.3.5 THEN.4 + T3.4 + T3.5。

**风险等级**：🟡 **Medium**（清理误删风险）。

**P1 修补综合风险等级**：🔴 **High**（1+2+3+4+5 任意一处失败影响业务核心流）→ 强制 Comprehensive 100% 覆盖。

---

## 六、风险画像（5 维度 · Comprehensive 必须）

| 风险维度 | 严重度 | 概率 | 风险描述 | 缓解措施 | 残余风险 |
|---------|--------|------|---------|---------|---------|
| **功能** | 🟡 M | M | 双轨阈值 + 字典 + HR Feign + 审计清理多模块集成 | 70 测例覆盖 5 AC + 23 跨模块契约 + 状态机守卫 | 中 |
| **数据** | 🟡 M | L | V3 7 张新表迁移 + 字段一致性 | 集成测试 V3 迁移逐表对比 + 灰度 + 回滚脚本 | 低 |
| **性能** | 🟡 M | M | 双轨阈值热更新 + Nacos 推送 + 审计清理调度 | k6 8 脚本 + 路由 P95<50ms / 推送 P95<200ms / 创建 P95<300ms / 待办 P95<1s | 中 |
| **安全** | 🟡 M | L | 双轨阈值越权 / 字典改值越权 / 变更日志未留痕 / Feign 降级失败 | OWASP ZAP 6 类越权扫描 + @PreAuthorize 严格守卫 + 状态机 40904 | 低 |
| **审计** | 🟡 M | M | `@AuditLog` 切面在阈值变更时漏记 / sys_change_log 写入失败 | AFTER_COMMIT + 失败兜底表 + 清理告警 + 5 类写操作全覆盖 | 中 |

**综合风险等级**：🟡 **Medium**（4 M + 1 L），建议 **Go with strict monitoring** + 5 处 P1 修补全闭环 + 性能基线达标 + ZAP/Sonar 0 High/Critical 方可发布。

---

## 七、准入与退出条件

### 7.1 准入条件（Go / No-Go）

**Go 必须满足全部 9 条**：

1. Architect 评审 8.5/10 通过、5 处 P1 修补全部对齐（已 CONDITIONAL）
2. `application-platform.yml` 含 Nacos `app.params.*` + `app.workflow.thresholds.*` + Feign 超时配置
3. Testcontainers MySQL 8 + Redis 7 + WireMock 启动成功 + 7 张新表创建
4. OpenAPI 契约 `E1-Dict` / `E1-Param` / `E1-Threshold` / `E1-ChangeLog` 4 tag 11 端点 schema 100% 补全
5. 6 类字典 30+ 种子 + 3 类参数 10+ 种子幂等执行无报错
6. 双轨阈值 Nacos 优先 + 启动时校验一致性
7. HrEmployeeClient 真实 Feign 就绪（`erp_business.hr` 端点已 implement）
8. logback 配置禁止打印 `dictValue` / `paramValue` / `thresholdValue` 明文
9. k6 脚本 + ZAP 脚本 + Sonar 脚本 + Postman 集合 20 用例全部可执行

**No-Go 触发**：V3 迁移失败 / 双轨阈值不一致 / 字典引用计数错乱 / 审计清理误删 / ZAP High > 0 / Sonar Critical > 0 / k6 任一 P95 不达标。

### 7.2 退出条件（Exit Criteria）

- 单测覆盖率 ≥ 80%（工具类 100% / Service ≥ 90%）
- 集成测试 24 测例 100% 通过（14 端点 + 4 HR + 6 审计）
- 跨模块契约 23 测例 100% 通过
- E2E 5 角色 5 测例 100% 通过
- k6 8 脚本性能基线全达标（P95: 路由 50ms / 推送 200ms / 创建 300ms / 待办 1s / 字典查询 200ms / 参数更新 200ms / Feign 调用 200ms / 变更日志查询 300ms）
- OWASP ZAP 0 High 风险（含 6 类越权扫描）
- SonarQube 0 Critical
- 审计日志 100% 留痕 + 1 年保留 + 归档策略
- Story 1.1 35 测例 + Story 1.2 151 测例回归 100% 通过
- 5 处 P1 修补 25 测例 100% 通过
- 残余风险 ≤ 5 条已记录到 `docs/qa/reviews/1.3-系统参数与HR集成.md`

---

## 八、残余风险与推荐

| # | 残余风险 | 等级 | 推荐动作 |
|---|---------|------|--------|
| R1 | 双轨阈值启动时 Nacos 缺失 + DB 漂移 | 🟡 M | 启动时 `@PostConstruct` 校验 + 告警飞书 |
| R2 | 6 类字典 30+ 种子跨模块契约一致性 | 🟡 M | 跨模块契约测试 23 测例（销售/生产/委外/采购/财务 各 3） |
| R3 | HR Feign 真实化后 `HrEmployeeClientFallback` 占位兼容性 | 🟡 M | 标记 `@Deprecated` 不删，3 个 sprint 后清理 |
| R4 | `@Scheduled` 审计清理时区漂移 | 🟢 L | 显式 `zone="Asia/Shanghai"` + 单元测试 mock `Clock` |
| R5 | k6 性能基线本机无 docker | 🟡 M | k6 脚本 + 报告模板就位，部署阶段实测 |
| R6 | OpenAPI 4 tag 11 端点 schema 补全 | 🟡 M | dev 实施时同步 OpenAPI yaml 补全 |
| R7 | 5 条 UI 红线本 Story 不涉及但下游 Epic 必查 | 🟢 L | grep 验证 5 个正则（已闭环） |

---

## 九、QA 签字与下一步

- **测试设计级别**：Comprehensive
- **测试用例总数**：**96 测例**（单测 26 + 集成 24 + 跨模块 23 + k6 8 + ZAP 6 + Sonar 4 + E2E 5 = 96）+ Story 1.1 35 + Story 1.2 151 回归 = **282 测例回归**
- **P1 专项 25 测例**：100% 覆盖 5 处 P1 修补
- **自动化率**：95%（手工仅 ZAP 主动扫描 + 视觉验收 + k6 部署阶段实测）
- **关键风险**：双轨阈值 + 字典一致性 + HR Feign 真实 + 审计清理
- **状态变更**：Story 1.3 `Draft` → `TestDesignComplete` → 等待 dev agent *develop-story 1.3

🎯 HANDOFF TO dev: *develop-story 1.3
