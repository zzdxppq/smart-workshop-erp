# QA Reviews：Story 10.5 - 5 个 .vue 中 any 类型替换为 OpenAPI 类型

> **状态**：**READY for dev IMPL** · Standard · 0 单测 · 6 项验证清单 + 9.2 回归
> **签字**：**QA 商鞅 · 2026-06-13 · Story 10.5 verification 清单完成**
> **关联 Story**：`backend/docs/stories/sprint10/10.5-vue-any-replace.md`
> **关联 architect review**：[→ 10.5-review](../../architecture/story-reviews/10.5-review.md)（鲁班 APPROVED）
> **关联实施**：web-impl 仓 `src/views/{quote,order,outsource,quality}/**` 5 文件

---

## 一、验证原则

重构任务 · **0 单测** · 由 6 项**验证清单**代替 · 全部通过 + 9.2 既有 4 Playwright 测例回归 = dev IMPL OK。

## 二、验证清单（6 项 · 全部必过）

### 验证 1：5 文件 any 命中数 = 0

```bash
cd web-impl
for f in src/views/quote/Detail.vue \
         src/views/order/List.vue \
         src/views/order/ProfitChart.vue \
         src/views/outsource/StateMachine.vue \
         src/views/quality/InspectionForm.vue; do
  count=$(grep -c ': any' "$f" || true)
  echo "$f: $count"
  test "$count" -le 1 || exit 1  # 每文件 ≤ 1（含 ECharts 边界 as any）
done
```

**期望**：
- 5 文件输出 5 行
- 每行 count ≤ 1
- 退出码 0

### 验证 2：仓总 any 数 ≤ 2

```bash
cd web-impl
total=$(grep -rn ': any' src/ | wc -l)
echo "total any count: $total"
test "$total" -le 2 || exit 1
```

**期望**：
- 输出 `total any count: 0` 或 `1` 或 `2`（仅 ECharts 局部 as any 边界）
- 退出码 0

### 验证 3：5 文件 import type from generated

```bash
cd web-impl
for f in src/views/quote/Detail.vue \
         src/views/order/List.vue \
         src/views/order/ProfitChart.vue \
         src/views/outsource/StateMachine.vue \
         src/views/quality/InspectionForm.vue; do
  hits=$(grep -c "from '@/api/generated/models" "$f" || true)
  echo "$f: $hits import(s)"
  test "$hits" -ge 1 || exit 1
done
```

**期望**：
- 5 文件输出 5 行
- 每行 hits ≥ 1
- 退出码 0

### 验证 4：typecheck 退出码 0

```bash
cd web-impl
npm run typecheck
```

**期望**：
- 退出码 0
- 输出无 `error TS` 关键字
- vue-tsc 报告 0 error

### 验证 5：build 退出码 0

```bash
cd web-impl
npm run build
```

**期望**：
- 退出码 0
- `dist/` 目录生成
- 输出无 `ERROR` 关键字

### 验证 6：5 目标 DTO 在 generated/ 存在

```bash
cd web-impl
for dto in QuoteDTO OrderDTO OrderProfitDTO OutsourceStateDTO InspectionDTO; do
  test -f "src/api/generated/models/${dto}.ts" || exit 1
  echo "$dto: OK"
done
```

**期望**：
- 5 DTO 全部存在
- 退出码 0

> 注：本验证项确认 Story 10.1 已完成（generated/ 目录存在 5 目标 DTO）

## 三、9.2 Playwright 回归（4 测例 · 行为不变）

```bash
cd web-impl
npx playwright test --project=chromium \
  e2e/a/login.spec.ts \
  e2e/b/dashboard.spec.ts \
  e2e/b/order-list.spec.ts \
  e2e/b/quote-detail.spec.ts
```

**期望**：
- 4/4 PASS
- 行为零变化（纯类型层替换）

## 四、5 文件替换明细（建议 commit 顺序）

| # | 文件 | 替换 any 数 | 目标类型 | 建议 commit message |
|---|------|-------------|----------|---------------------|
| 1 | `quote/Detail.vue` | 3 | `QuoteDTO` / `QuoteItemDTO[]` | `refactor(web): replace any with QuoteDTO in Detail.vue` |
| 2 | `order/List.vue` | 5 | `OrderDTO` / `PageResponse<OrderDTO>` | `refactor(web): replace any with OrderDTO in List.vue` |
| 3 | `order/ProfitChart.vue` | 2 | `OrderProfitDTO` + ECharts 保留 | `refactor(web): replace any with OrderProfitDTO in ProfitChart.vue` |
| 4 | `outsource/StateMachine.vue` | 4 | `OutsourceStateDTO` / `OutsourceTransitionDTO` | `refactor(web): replace any with OutsourceStateDTO in StateMachine.vue` |
| 5 | `quality/InspectionForm.vue` | 6 | `InspectionDTO` / `InspectionItemDTO[]` | `refactor(web): replace any with InspectionDTO in InspectionForm.vue` |

每个文件 1 个 commit（InspectionForm.vue 复杂可拆 2-3 个）· 共 5-9 commit

## 五、风险与缓解

| 风险 | 等级 | 缓解 | 验证项 |
|------|------|------|--------|
| 替换后类型不匹配 | 🟡 中 | dev 启动前 regen · typecheck 一次过 | V4 |
| `as any` 残留超限 | 🟢 低 | AC 限制每文件 ≤ 1 | V1/V2 |
| ECharts option 类型不全 | 🟢 低 | 局部保留 any + 注释 | V1/V2 |
| 大型 .vue 替换复杂 | 🟡 中 | 拆 5-9 commit 逐步替换 | git log |
| 与 10.1 generated 漂移 | 🟢 低 | 10.1 CI gate 拦截 | V6 |
| 行为回归 | 🟢 低 | Playwright 9.2 4 测例回归 | 三 |

## 六、签字

**QA 商鞅 · 2026-06-13 · Story 10.5 verification 清单完成 · 6 项必过 + 9.2 回归 4 测例**

签字：商鞅

时间戳：2026-06-13T23:20:00+08:00

**dev agent Opus 4.8 · 待启动 IMPL**（5 文件替换 + 自验证 6 项 + 9.2 回归）
