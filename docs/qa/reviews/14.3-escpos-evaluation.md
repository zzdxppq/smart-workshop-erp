# QA Reviews：Story 14.3 - ESC/POS 票据打印机 V1.3.10 评估启动（PM 决策 1 采纳 A）

> **状态**：**P1 协同 · READY for dev IMPL** · Simple · 0 单测 · 4 评估项设计完毕
> **签字**：**QA 商鞅 · 2026-06-14 · Story 14.3（13.8）test-design 完成**
> **关联 Story**：`backend/docs/stories/sprint14/14.3-escpos-evaluation.md`
> **关联 architect review**：[→ 14.3-review](../../architecture/story-reviews/)（待 arch 鲁班 *review · 重点协议扩展影响面 + EscPosAdapter 抽象位置 + 风险评估合理性）
> **关联实施**：backend + web-impl（评估阶段 · 仅文档输出）
> **关联依赖**：Sprint 12.2 ✅（V55 sys_printer 表 + ProtocolAdapter 抽象）+ Sprint 12.4 ✅（V57 sys_print_log 表 + Socket 抽象）+ Sprint 13.2 ✅（思源黑体嵌入）
> **关联 PM 决策**：PM 巡检 §9 PM 决策 1（采纳 A · V1.3.10 backlog 评估）

---

## 一、测试设计原则

| 维度 | 策略 | 理由 |
|------|------|------|
| 单元 | 不适用 | 评估阶段 · 无业务逻辑 |
| 集成 | 不适用 | 评估阶段 · 不动契约 |
| E2E | 不新增 | 评估阶段 · V1.3.10 backlog 准备 |
| 性能 | 不适用 | 评估阶段 · 仅工时估算 |
| 契约 | 0 端点改动（pure 评估文档）| 评估草案就位 · 后续 V1.3.10 IMPL 阶段实施 |

> **核心策略**：4 评估项（协议扩展 + code_type 扩展 + EscPosAdapter 抽象 + 工时/风险）· 评估文档 review + 决策签字
> **核心约束**：**评估阶段 · 0 端点 · 0 Flyway · V1.3.10 backlog 准备** · 与 14.2/14.1/14.4 完全并行 · 客户验收阶段可能提出

---

## 二、测例详细（4 评估项）

### 评估项 1：协议扩展评估（AC-14.3.1）

#### V-14.3.1.1 V55 sys_printer.protocol 枚举扩 ESC_POS 草案就位

- **前置**：Sprint 12.2 V55 `sys_printer.protocol` 枚举：`PDF_BROWSER` / `ZPL` / `TSPL`
- **步骤**：
  1. review 评估文档 · `docs/architecture/escpos-evaluation-v1310.md` §协议扩展
- **期望**：
  - V55 SQL 草案：`ALTER TABLE sys_printer MODIFY protocol ENUM('PDF_BROWSER','ZPL','TSPL','ESC_POS')` 就位
  - 端点契约草案：openapi.yaml `Printer.protocol` 枚举扩 `ESC_POS` 字段就位
  - 客户端适配草案：web-impl `PrinterForm.vue` + android-impl `PrinterFormActivity.kt` 加 `ESC_POS` 选项就位
  - 心跳调度草案：ESC/POS 设备 Socket 9100 心跳复用 + 2s connect timeout + fail_count ≥ 2 验证方案就位

#### V-14.3.1.2 协议影响面评估完整

- **步骤**：
  1. review 评估文档 §协议影响面
- **期望**：
  - 列出 Sprint 12.2 + 12.4 + 13.2 所有与 `Printer.protocol` 字段关联的代码位置（grep 全量）
  - 标识 `ProtocolAdapter` 工厂方法需新增 `ESC_POS` case 分支
  - 标识 V55 Flyway 迁移需在 V1.3.10 启动时新增（V1.3.10-V58）
  - 标识与 12.4 既有 ZPL/TSPL 协议的并行兼容性

### 评估项 2：code_type 扩展评估（AC-14.3.2）

#### V-14.3.2.1 V57 sys_print_log.code_type 枚举扩 RECEIPT/DELIVERY_NOTE 草案就位

- **前置**：Sprint 12.4 V57 `sys_print_log.code_type` 枚举：`QUOTE` / `STATEMENT` / `DRAWING` / `LABEL_*`（GD/LZ/SB/WW）
- **步骤**：
  1. review 评估文档 §code_type 扩展
- **期望**：
  - V57 SQL 草案：`ALTER TABLE sys_print_log MODIFY code_type ENUM(...,'RECEIPT','DELIVERY_NOTE')` 就位
  - 端点契约草案：openapi.yaml `PrintRequest.codeType` 枚举扩 `RECEIPT` / `DELIVERY_NOTE` 字段就位
  - 模板渲染草案：`ReceiptRenderer`（销售小票 · 80mm 热敏纸）+ `DeliveryNoteRenderer`（发货单 · 80mm 热敏纸）· 与 `LabelPngRenderer` 平级
  - 中文厂名草案：复用 Sprint 13.2 思源黑体嵌入 jar 资源 + ESC/POS 字节流指令集兼容性验证方案

#### V-14.3.2.2 code_type 影响面评估完整

- **步骤**：
  1. review 评估文档 §code_type 影响面
- **期望**：
  - 列出 Sprint 12.4 + 13.2 所有与 `codeType` 字段关联的代码位置（grep 全量）
  - 标识 `print/code` 端点的 `codeType` 字段需新增 `RECEIPT` / `DELIVERY_NOTE` case 分支
  - 标识模板渲染器工厂方法需新增 `ReceiptRenderer` / `DeliveryNoteRenderer` case
  - 标识 80mm 热敏纸 vs A4 PDF 双模式共存方案（web-impl `PrintDialog.vue` 弹框选择）

### 评估项 3：EscPosAdapter 抽象评估（AC-14.3.3）

#### V-14.3.3.1 PrintProtocolAdapter 接口草案就位

- **前置**：Sprint 12.4 `ProtocolAdapter` 抽象（`ZplAdapter` + `TsplAdapter` + `PdfBrowserAdapter`）
- **步骤**：
  1. review 评估文档 §EscPosAdapter 抽象
- **期望**：
  - 接口定义草案就位：`interface PrintProtocolAdapter { fun build(payload: PrintPayload): ByteArray }`
  - 实现类草案就位：`EscPosAdapter`（含 ESC @ 初始化 + ESC ! 字号 + ESC a 对齐 + GS V 切纸 + GS ( k 二维码）
  - 字节流断言测试草案就位：ESC/POS 设备型号（飞鹅 FP-58 Ⅱ / 佳博 GP-58MB）字节流单测方案
  - 错误码复用方案就位：50201-50203 复用（与 ZPL/TSPL 一致）

#### V-14.3.3.2 EscPosAdapter 抽象合理性

- **步骤**：
  1. review 评估文档 §抽象位置
- **期望**：
  - `EscPosAdapter` 与 `ZplAdapter` / `TsplAdapter` / `PdfBrowserAdapter` 平级
  - 4 选 1 工厂方法清晰（`PrintProtocolAdapterFactory.create(protocol: Printer.protocol)`）
  - 字节流 Payload 复用 Sprint 12.4 既有 `PrintPayload` DTO
  - 单测覆盖 3-5 个 ESC/POS 设备型号（飞鹅 / 佳博 / 爱普生 80mm 主流型号）

### 评估项 4：工时 + 风险评估（AC-14.3.4）

#### V-14.3.4.1 工时估算合理性

- **步骤**：
  1. review 评估文档 §工时估算
- **期望**：
  - backend 工时：5-7 天（V58 Flyway + V55 protocol 扩 + V57 code_type 扩 + EscPosAdapter + 4 设备型号单测）
  - web-impl 工时：2-3 天（PrinterForm 加 ESC_POS + PrintDialog 80mm 选项 + codegen）
  - android-impl 工时：1-2 天（PrinterFormActivity 加 ESC_POS + PrintDialog 80mm 选项）
  - 合计工时：8-12 天 · 与 PM 巡检建议 #1 一致

#### V-14.3.4.2 4 项风险评估就位（含等级 + 缓解）

- **步骤**：
  1. review 评估文档 §风险评估
- **期望**：
  - 风险 1：ESC/POS 设备型号碎片化（飞鹅 / 佳博 / 爱普生 80mm 指令差异）· 🟡 中
  - 风险 2：中文厂名渲染（思源黑体 + ESC/POS 字节流指令集兼容性）· 🟡 中
  - 风险 3：Socket 9100 心跳在 ESC/POS 设备的容差（drop 而非 RST）· 🟡 中
  - 风险 4：80mm 热敏纸 vs A4 PDF 双模式共存 · 🟢 低
  - 每项风险含：等级 + 触发条件 + 缓解方案 + V1.3.10 IMPL 启动前的准备动作

#### V-14.3.4.3 V1.3.10 backlog IMPL 准备就位

- **步骤**：
  1. review 评估文档 §V1.3.10 backlog 准备
- **期望**：
  - V1.3.10 backlog Story 拆分草案：14.x（协议扩展）+ 14.x（code_type 扩展）+ 14.x（EscPosAdapter + 4 设备型号单测）
  - V1.3.10 启动条件：客户在 V1.3.9 验收阶段提出 ESC/POS 设备型号需求
  - V1.3.10 启动时序：V1.3.9 灰度阶段 4 完成后启动（不可在 V1.3.9 灰度期间启动）
  - 工时 + 风险评估签字：QA 商鞅 + architect 鲁班 + PM 范蠡

---

## 三、CI 集成

### 评估阶段 · 无 CI 集成

> 14.3 评估阶段不动契约 · 不动 DB · 不动端点 · 仅文档输出
> V1.3.10 IMPL 启动时按 Sprint 12.2/12.4 模式接入 GitLab CI

### V1.3.10 backlog 启动条件

- 客户在 V1.3.9 验收阶段（6/30 - 7/31）提出 ESC/POS 设备型号需求
- PM 范蠡 + 客户对齐 ESC/POS 设备清单（飞鹅 / 佳博 / 爱普生具体型号）
- 评估文档 review 完成后启动 V1.3.10 Sprint

---

## 四、性能基线

| 维度 | V1.3.9 现状 | V1.3.10 backlog 目标 |
|------|------------|---------------------|
| 协议支持 | 3（PDF_BROWSER / ZPL / TSPL）| 4（+ESC_POS）|
| code_type 支持 | 7（QUOTE/STATEMENT/DRAWING/LABEL_*）| 9（+RECEIPT/DELIVERY_NOTE）|
| 票据模板 | 0 | 2（ReceiptRenderer + DeliveryNoteRenderer）|
| ESC/POS 设备型号单测 | 0 | 3-5（飞鹅 / 佳博 / 爱普生主流型号）|
| Socket 9100 心跳 | 60s · 2s connect timeout | 60s · 2s connect timeout · fail_count ≥ 2 |
| 中文厂名 | 思源黑体（13.2 已 ship）| 思源黑体复用 · ESC/POS 字节流指令集兼容 |

> **评估阶段性能基线**：仅作 V1.3.10 IMPL 启动时参考 · 14.3 不实施

---

## 五、风险与缓解

| 风险 | 等级 | 缓解 | 评估项 |
|------|------|------|--------|
| ESC/POS 设备型号碎片化（飞鹅 / 佳博 / 爱普生 80mm 指令差异）| 🟡 中 | V1.3.10 IMPL 阶段单测覆盖主流型号（3-5 个）| V-14.3.3.2 |
| 中文厂名渲染（思源黑体 + ESC/POS 指令集兼容性）| 🟡 中 | 复用 Sprint 13.2 思源黑体嵌入 · 与 `LabelPngRenderer` 平级 | V-14.3.2.1 |
| Socket 9100 心跳在 ESC/POS 设备的容差 | 🟡 中 | 复用 Sprint 12.2 心跳调度 · 2s connect timeout + fail_count ≥ 2 | V-14.3.1.1 |
| 80mm 热敏纸 vs A4 PDF 双模式共存 | 🟢 低 | `codeType` 字段区分 · web-impl `PrintDialog.vue` 弹框选择 | V-14.3.2.2 |
| V1.3.10 backlog 工时估算偏差（8-12 天 → 实际 14+ 天）| 🟢 低 | 评估阶段签字 · V1.3.10 启动时重新评估 | V-14.3.4.1 |
| 客户未在 V1.3.9 验收阶段提出 ESC/POS 需求 | 🟢 低 | V1.3.10 backlog 推迟 · 不阻塞 V1.3.9 灰度收口 | V-14.3.4.3 |
| EscPosAdapter 抽象与 12.4 既有 ProtocolAdapter 冲突 | 🟢 低 | 14.3 评估阶段先 review 抽象位置 · 14.x IMPL 阶段才实施 | V-14.3.3.2 |
| openapi.yaml enum 扩 触发 Sprint 10.1 codegen 漂移 | 🟢 低 | V1.3.10 启动时按 13.7 模式（补 openapi.yaml + regen + `git diff --exit-code`）| V-14.3.1.1 |

---

## 六、回归策略

- **Sprint 12.2 sys_printer 表不破坏**（14.3 评估阶段不动 DB · V1.3.10 IMPL 阶段才 ALTER TABLE）→ V-14.3.1.1 SQL 草案
- **Sprint 12.4 sys_print_log 表不破坏**（14.3 评估阶段不动 DB · V1.3.10 IMPL 阶段才 ALTER TABLE）→ V-14.3.2.1 SQL 草案
- **Sprint 12.4 ProtocolAdapter 抽象不破坏**（14.3 评估阶段不动代码 · V1.3.10 IMPL 阶段才新增 EscPosAdapter）→ V-14.3.3.1 接口草案
- **Sprint 13.2 思源黑体嵌入不破坏**（14.3 评估阶段复用既有 jar 资源）→ V-14.3.2.1 中文厂名草案
- **后续 V1.3.10 端点改动**：自动触发 codegen + typecheck:ci + 端到端 E2E（V1.3.10 IMPL 阶段）

---

## 七、签字

**QA 商鞅 · 2026-06-14 · Story 14.3（13.8）test-design 完成 · 4 评估项 · P1 协同 · V1.3.10 backlog 评估启动**

签字：商鞅

时间戳：2026-06-14T21:55:00+08:00

**dev agent Opus 4.8 · 待启动 backend + web-impl IMPL**（评估阶段 · 仅文档输出 · 0 端点 · 0 Flyway · V1.3.10 backlog 准备 · 客户验收阶段可能提出 · **PM 决策 1 采纳 A**）
