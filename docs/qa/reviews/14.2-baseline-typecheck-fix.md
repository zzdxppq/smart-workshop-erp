# QA Reviews：Story 14.2 - baseline-typecheck-fix 5 错误收口（Sprint 14 IMPL 起点 · P0 必做 · typecheck:ci gate）

> **状态**：**P0 必做 · READY for dev IMPL** · Simple · 0 单测 · 3 验证项设计完毕
> **签字**：**QA 商鞅 · 2026-06-14 · Story 14.2（13.7）test-design 完成**
> **关联 Story**：`backend/docs/stories/sprint14/14.2-baseline-typecheck-fix.md`
> **关联 architect review**：[→ 14.2-review](../../architecture/story-reviews/)（待 arch 鲁班 *review · 重点 vite-env.d.ts 字段覆盖 + openapi.yaml Printers schema 完整性 + GmSummary 类型断言安全性）
> **关联实施**：web-impl 仓 `src/vite-env.d.ts`（新增）+ `backend/spec/openapi.yaml`（补 Printers 端点 schema）+ `src/views/admin/Printers.vue` L163-164（codegen 类型导入）+ `src/views/v138/GmSummary.vue` L33/47（Number() 类型断言）
> **关联依赖**：Sprint 10.1 ✅（codegen）+ Sprint 12.2 ✅（Printer 端点 ship · 6 端点）+ Sprint 10.5 ✅（5 个 .vue any 替换）
> **关联 PM 决策**：PM 巡检 `docs/orchestrix-pm-audit-2026-06-14.md` §7.1 修补建议 #5

---

## 一、测试设计原则

| 维度 | 策略 | 理由 |
|------|------|------|
| 单元 | 不适用 | pure typecheck 收口 · 无业务逻辑 |
| 集成 | codegen + typecheck:ci + git diff | openapi.yaml → TS 类型的契约一致性 |
| E2E | 不新增（10.2 端到端覆盖）| enum 改动 ≠ 业务流转改动 |
| 性能 | codegen < 10s · typecheck < 30s · build < 60s | CI 增量 < 2min |
| 契约 | 与 Sprint 10.1 codegen baseline 对齐 | 端点/类型漂移是核心风险 |

> **核心策略**：3 验证项（vite-env.d.ts 字段覆盖 + openapi.yaml Printers codegen 链路 + GmSummary Dayjs 类型断言）· typecheck:ci + regen diff + build 三重门禁
> **核心约束**：**Sprint 14 IMPL 起点 · 不可逆前置** · 所有其他 Story dev IMPL 必须在 14.2 ship 后启动 · 12.1 灰度阶段 1 前置

---

## 二、测例详细（3 验证项）

### 验证项 1：vite-env.d.ts 字段覆盖（AC-14.2.1）

#### V-14.2.1.1 vite-env.d.ts 文件就位

- **前置**：web-impl 已有 `src/api/http.ts` L13 `import.meta.env` 引用
- **步骤**：
  1. `cat web-impl/src/vite-env.d.ts`
- **期望**：
  - 含 `interface ImportMetaEnv` 定义
  - 含 `readonly VITE_API_BASE_URL: string`
  - 含 `readonly VITE_USE_MOCK: 'true' | 'false'`
  - 含 `readonly VITE_APP_TITLE: string`
  - 含 `interface ImportMeta { readonly env: ImportMetaEnv }`
  - 首行含 `/// <reference types="vite/client" />`
- **断言**：
  ```bash
  cat web-impl/src/vite-env.d.ts | grep -q "VITE_API_BASE_URL" && \
  cat web-impl/src/vite-env.d.ts | grep -q "VITE_USE_MOCK" && \
  cat web-impl/src/vite-env.d.ts | grep -q "VITE_APP_TITLE" && \
  cat web-impl/src/vite-env.d.ts | grep -q "ImportMetaEnv"
  ```

#### V-14.2.1.2 typecheck:ci 通过（vite-env.d.ts 修复验证）

- **步骤**：
  1. `cd web-impl`
  2. `npm run typecheck:ci 2>&1 | tee /tmp/typecheck.log`
- **期望**：
  - exit code = 0
  - 无 `import.meta.env` 类型错误
  - 错误数从 baseline 5 → 0
- **断言**：
  ```bash
  ERRORS=$(npm run typecheck:ci 2>&1 | grep -c "error TS")
  test $ERRORS -eq 0
  ```

### 验证项 2：openapi.yaml Printers codegen 链路补齐（AC-14.2.2）

#### V-14.2.2.1 openapi.yaml Printers 端点 schema 就位

- **前置**：Sprint 12.2 ✅ Printer 端点 6 端点已 ship（backend 实装完成）
- **步骤**：
  1. `grep -c 'operationId: listPrinters\|createPrinter\|updatePrinter\|deletePrinter\|getPrinter' backend/spec/openapi.yaml`
  2. `grep -A 2 'Printer:' backend/spec/openapi.yaml | grep -E 'protocol|modelSuggestion'`
- **期望**：
  - 6 个 operationId 齐全（listPrinters / createPrinter / getPrinter / updatePrinter / deletePrinter / + 1）
  - `Printer.protocol` 枚举：`PDF_BROWSER` / `ZPL` / `TSPL`
  - `Printer.modelSuggestion` 枚举：`DELI_DL888B` / `ZEBRA_ZD420` / `TSC_TTP244PRO` / `OTHER`
  - `PrinterCreateRequest` 含 `name` + `type` 必填字段

#### V-14.2.2.2 npm run gen:api 退出 0

- **步骤**：
  1. `cd web-impl`
  2. `rm -rf src/api/generated`
  3. `npm run gen:api 2>&1 | tee /tmp/codegen.log`
- **期望**：
  - exit code = 0
  - `src/api/generated/models/Printer.ts` 存在
  - `src/api/generated/services/AdminPrinterService.ts` 存在（含 6 方法）

#### V-14.2.2.3 git diff --exit-code src/api/generated 退出 0

- **步骤**：
  1. `git status src/api/generated/`
  2. `git diff --exit-code src/api/generated/`
- **期望**：
  - exit code = 0（regen 后无 diff · 已有 baseline 已 ship）
  - 若 exit ≠ 0：dev 必须 check baseline 是否与 12.2 ship 时的 codegen 一致
- **断言**：
  ```bash
  cd web-impl
  git diff --exit-code src/api/generated 2>&1
  # 期望输出：""（exit 0）
  ```

#### V-14.2.2.4 Printers.vue L163-164 codegen 类型导入

- **步骤**：
  1. `grep -A 1 'import type { Printer }' web-impl/src/views/admin/Printers.vue`
- **期望**：
  - L163-164 含 `import type { Printer } from '@/api/generated/models/Printer'`
  - 移除 Sprint 13.7 IMPL 前的临时 any 兜底（`Printer as any` 或 `interface Printer { ... }` 局部定义）

### 验证项 3：GmSummary.vue L33/47 Dayjs 类型断言（AC-14.2.3）

#### V-14.2.3.1 GmSummary.vue L33 修复（amountThresholdPassedRate）

- **前置**：`GmSummaryResponse` DTO 已 codegen（Sprint 4.3 + Sprint 10.1）
- **步骤**：
  1. `grep -B 1 -A 1 'amountThresholdPassedRate' web-impl/src/views/v138/GmSummary.vue`
- **期望**：
  - L33 含 `Number(data.amountThresholdPassedRate) * 100` 包装
  - 无 `data.amountThresholdPassedRate * 100` 原始表达式（无 Number 包装会触发类型错误）
- **断言**：
  ```bash
  grep -A 1 "金额阈值审批通过率" web-impl/src/views/v138/GmSummary.vue | grep -q "Number(data.amountThresholdPassedRate)"
  ```

#### V-14.2.3.2 GmSummary.vue L47 修复（outsourceCostRatio）

- **步骤**：
  1. `grep -B 1 -A 1 'outsourceCostRatio' web-impl/src/views/v138/GmSummary.vue`
- **期望**：
  - L47 含 `Number(data.outsourceCostRatio) * 100` 包装
  - 无 `data.outsourceCostRatio * 100` 原始表达式

#### V-14.2.3.3 typecheck:ci 退出 0（最终验证）

- **步骤**：
  1. `cd web-impl`
  2. `npm run typecheck:ci 2>&1 | tee /tmp/typecheck-final.log`
- **期望**：
  - exit code = 0
  - 错误数从 baseline 5 → 0
  - 无 GmSummary.vue L33/47 类型错误
  - 无 Printers.vue L163-164 类型错误
  - 无 `import.meta.env` 类型错误

#### V-14.2.3.4 npm run build 退出 0（构建验证）

- **步骤**：
  1. `cd web-impl`
  2. `npm run build 2>&1 | tee /tmp/build.log`
- **期望**：
  - exit code = 0
  - `dist/` 目录生成
  - 无 TypeScript 编译错误
  - bundle size 增量 < 100KB（baseline 收口不影响 bundle 体积）

---

## 三、CI 集成

### GitLab CI 配置

```yaml
# web-impl/.gitlab-ci.yml (新增 14.2 typecheck:ci gate)
typecheck_baseline:
  stage: gate
  image: node:20
  before_script:
    - cd web-impl
    - npm ci
  script:
    # 1. typecheck:ci 必须 clean
    - npm run typecheck:ci
    # 2. codegen 必须一致（baseline 收口后）
    - npm run gen:api
    - git diff --exit-code src/api/generated
    # 3. build 必须成功
    - npm run build
  artifacts:
    when: on_failure
    paths:
      - web-impl/dist/
    expire_in: 7 days
  allow_failure: false
  # Sprint 14 IMPL 起点前置 gate · 不可逆
  only:
    - merge_requests
    - main
```

### 启动约束

- **Sprint 14 IMPL 起点** · 不可逆前置（所有其他 Story dev IMPL 必须在 14.2 ship 后启动）
- **V1.3.9 灰度阶段 1 启动前必须 ship**（6/30 admin + ENGINEER 灰度）

---

## 四、性能基线

| 操作 | 期望耗时 | 实测容忍 |
|------|----------|----------|
| `npm run typecheck:ci`（web-impl 全量 TS 检查）| < 30s | < 60s |
| `npm run gen:api`（OpenAPI → TS codegen）| < 10s | < 20s |
| `git diff --exit-code src/api/generated` | < 1s | < 5s |
| `npm run build`（vite production build）| < 60s | < 120s |
| **CI gate 总耗时** | **< 2min** | **< 5min** |

> **增量影响**：5 错误收口不影响 bundle 体积（Number() 包装为编译期 + vite-env.d.ts 为 type-only）

---

## 五、风险与缓解

| 风险 | 等级 | 缓解 | 测例 |
|------|------|------|------|
| openapi.yaml 改动可能触发 Sprint 10.1 codegen 既有类型漂移 | 🟢 低 | dev 二次 regen + `git diff --exit-code` 拦截 + 0.5h 增量 | V-14.2.2.3 |
| Sprint 12.2 已 ship 端点的 schema 描述与 backend 实装不一致 | 🟡 中 | dev 启动前先 `git log 12.2-printer-management.md` 同步字段 + 与 architect 评审 | V-14.2.2.1 |
| GmSummary.vue Number() 断言可能掩盖 DTO 字段类型 bug | 🟢 低 | 后续 Sprint 14+ 启动 GmSummaryResponse DTO 类型修正（V1.3.10 backlog）| V-14.2.3.1/3.2 |
| **typecheck:ci gate 是 Sprint 14 IMPL 起点前置 · 不可逆** | 🟡 中 | 14.2 必须在 Sprint 14 IMPL 起点 ship · 其他 Story IMPL 必须在 14.2 ship 后启动 | V-14.2.3.3 |
| **V1.3.9 灰度阶段 1 启动前 14.2 必须 ship** | 🟡 中 | 灰度阶段 1（6/30 admin + ENGINEER）启动前 · web-impl typecheck:ci 必须 clean | V-14.2.3.3 |
| Printers.vue 临时 any 兜底未清理（残留 `as any` 断言）| 🟢 低 | dev 启动前 grep `as any` 排查 · 14.2 完成时必须 0 处临时 any | V-14.2.2.4 |
| vite-env.d.ts 字段遗漏（如新增 VITE_* 环境变量未声明）| 🟢 低 | dev 启动前 `grep -r "import.meta.env" src/` 全量扫描 · 字段覆盖率 100% | V-14.2.1.1 |

---

## 六、回归策略

- **Sprint 10.1 codegen baseline 不破坏**（14.2 仅补 Printers 端点 schema · 不改既有 148 ops）→ V-14.2.2.3 `git diff --exit-code` 拦截
- **Sprint 10.5 .vue any 替换不破坏**（5 个 .vue any 替换已 ship · 14.2 新增 codegen 类型导入）→ typecheck:ci 覆盖
- **Sprint 12.2 Printer 端点契约不破坏**（14.2 补 openapi.yaml schema · 不改 backend 实装）→ V-14.2.2.1 schema 对账
- **Sprint 4.3 GmSummary.vue 不破坏**（14.2 仅加 Number() 包装 · 渲染逻辑不变）→ V-14.2.3.4 build 验证
- **后续 Story 14.x IMPL**：14.2 ship 后启动（typecheck:ci clean 前置）→ CI gate 强制

---

## 七、签字

**QA 商鞅 · 2026-06-14 · Story 14.2（13.7）test-design 完成 · 3 验证项 · P0 必做 · Sprint 14 IMPL 起点**

签字：商鞅

时间戳：2026-06-14T21:50:00+08:00

**dev agent Opus 4.8 · 待启动 web-impl IMPL**（1 文件新增 vite-env.d.ts + 1 yaml 文件补 Printers 端点 + regen + 1 文件 2 处 Number() 断言 + 1 文件 2 处 codegen 类型导入 · **P0 必做 · Sprint 14 IMPL 起点 · 必须最先 ship**）
