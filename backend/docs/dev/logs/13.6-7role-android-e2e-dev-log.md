# Story 13.6 - 7 角色 connectedAndroidTest E2E 补齐 Dev Log（backend 视角）

> **dev agent Opus 4.8** · orchestrix · 2026-06-14 IMPL 完成
> 关联 Story：`backend/docs/stories/sprint14/14.1-7role-android-e2e.md`
> 关联 android-impl dev log：`docs/dev/logs/13.6-7role-android-e2e-dev-log.md`（含完整 Page Object + CI 配置）
> 关联 architect REVIEW：`docs/architecture/story-reviews/13.6-review.md`
> 关联 QA test-design：`docs/qa/reviews/14.1-7role-android-e2e.md`
> 启动约束：**13.7 ship 后启动** · 12.1 灰度阶段 4 启动前 ship

---

## 1. backend 仓变更

本 Story 不改 backend 端点（0 端点改动），但需 backend 配合以下事项：

| 项 | 详情 | 责任 |
|---|------|------|
| 7 角色测试账号 seed | admin 创建 8 账号（ENGINEER/PROD_PLANNER/SALES/PURCHASER/WAREHOUSE/QC/OPERATOR/FINANCE）| staging 环境 |
| `draw.acl.gray.{ROLE}` 灰度 flag | sys_dict DRAWING_ACL_FEATURE_FLAG 表 7 角色 | 与 12.1 共用 |
| `DrawingFeatureFlag.invalidate()` 紧急回退 | admin 改 sys_dict 后立即生效 | 与 12.1 共用 |

---

## 2. 与 backend 既有契约对齐

| E2E 测例 | 消费的 backend 端点 | 来源 Story |
|----------|-------------------|-----------|
| 1.1 ENGINEER | GET /api/v1/drawings/{id}/permission + GET /preview | V1.3.7 + 12.1 |
| 1.3 PROD_PLANNER | GET /api/v1/workorders + GET /preview | V1.3.7 |
| 1.5 SALES | GET /api/v1/orders + GET /permission | V1.3.7 |
| 1.7 PURCHASER | GET /api/v1/purchase/orders + GET /permission | V1.3.7 |
| 1.9 WAREHOUSE | GET /api/v1/incoming + GET /permission | V1.3.7 |
| 1.11 QC | GET /api/v1/quality + GET /permission | V1.3.7 |
| 1.13 OPERATOR | GET /api/v1/workorders/current-process + GET /permission | 12.1 |
| 1.15 FINANCE | 登录 → 菜单不可见 | V1.3.7 + 12.1 |
| 3.1 6 角色拦截 | GET /permission (40320/40321/40322) | 12.1 |
| 3.2 灰度 flag | GET /api/v1/dict/list | 12.1 |
| 3.5 工序切换 | GET /workorders/current-process (5min Redis 缓存) | 12.1 |
| 3.6 跨设备同步 | 多 AVD 并行调用 → backend 状态一致 | 12.1 |

**全部为 V1.3.7 + V1.3.8 + V1.3.9 Sprint 12 既有端点 · 0 端点改动 · 0 Flyway 迁移**

---

## 3. IMPL 关键决策（backend 视角）

### 3.1 7 角色测试账号 seed（staging 环境）

admin 通过 `POST /api/v1/users` 创建：
- user_id=1001, role=ENGINEER
- user_id=1002, role=PROD_PLANNER
- user_id=1003, role=SALES
- user_id=1004, role=PURCHASER
- user_id=1005, role=WAREHOUSE
- user_id=1006, role=QC
- user_id=1007, role=OPERATOR
- user_id=1008, role=FINANCE

统一密码 `Test@123`（staging 专用 · 不进 prod seed）

### 3.2 灰度 feature flag 配置

sys_dict `DRAWING_ACL_FEATURE_FLAG` 表：
- `draw.acl.gray.ENGINEER` = true（阶段 1 已开）
- `draw.acl.gray.SALES` = true（阶段 2 已开）
- `draw.acl.gray.PURCHASER/WAREHOUSE/QC` = true（阶段 3 已开）
- `draw.acl.gray.OPERATOR` = false（阶段 4 ≥ 2026-06-20 · IMPL 完成待开启）

### 3.3 OPERATOR 当前工序 seed（5min Redis 缓存）

`POST /api/v1/workorders/current-process`：
- OPERATOR 1007 → processId=3 (P03)
- workorderNo=WO-2026-001

---

## 4. 阻塞/风险/PM 决策需求

### 4.1 阻塞

| # | 阻塞 | 等级 | 缓解 |
|---|------|------|------|
| 1 | backend 沙箱无 Android SDK | 🟢 低 | DevOps 张良全权执行 connectedAndroidTest |
| 2 | 8 账号 seed 需 staging 配合 | 🟢 低 | admin 创建（不在 prod）|

### 4.2 风险

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| 1 | 端点漂移（V1.3.9 后续迭代可能改契约）| 🟢 低 | codegen 类型保护 + E2E 截图回归 |
| 2 | 13.6 → 14.1 P0 重新激活时序错位 | 🟡 中 | 13.7 ship 后启动 · 12.1 灰度阶段 4 启动前 ship |

### 4.3 PM 决策需求（无）

无需 PM 决策（architect 鲁班 APPROVED · 与 Story §9 签字一致）

---

## 5. 签字

**dev agent Opus 4.8** · 2026-06-14 · backend 视角 IMPL 完成（无代码改动 · 仅 seed + flag 配置对齐）
