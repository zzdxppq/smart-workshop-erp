# Story 13.7 - baseline-typecheck-fix 5 错误收口（vite-env.d.ts + Printers.vue + GmSummary.vue）(V1.3.9 Sprint 14 · P0 必做 · typecheck:ci gate)

> orchestrix PO 范蠡 + SM 萧何 · 2026-06-14 SHARD
> 合同：XP-ZPF202606082405 · PRD V1.3.9 · Epic 17 - V1.3.9 收口 + 强化
> multi-repo **web-impl**（单仓 · typecheck 错误收口 + codegen 链路补齐）
> 前置：Sprint 10.1 ✅（OpenAPI TypeScript codegen 集成）+ Sprint 12.2 ✅（Printer 端点已 ship）+ Sprint 10.5 ✅（5 个 .vue any 替换）
> 关联：PM 巡检 `docs/orchestrix-pm-audit-2026-06-14.md` §7.1 修补建议 #5 · 完全独立（除 web-impl 单仓）· parallel_group A
> 优先级：🔴 **P0** · 复杂度 **S** · 工时 0.5-1 天 · **0 端点（补 Sprint 12.2 已 ship 端点的 codegen 链路）** · **0 测例（typecheck:ci 验证）** · Flyway 无

---

## Status

`Sharded`（PO 范蠡分片 · 2026-06-14 · 纳入 V1.3.9 Sprint 14 立项（P0 必做 · typecheck:ci gate）· 待 architect 鲁班 *review → dev agent Opus 4.8 *develop-story → QA 商鞅 *test-design → *test-execute）

> **顺序约束**：13.7 是 Sprint 14 IMPL 起点 · 所有其他 Story dev IMPL 必须在 13.7 ship 后启动 · 不可逆

## Story Metadata

| 字段 | 值 |
|------|-----|
| Story ID | 13.7 |
| Title | baseline-typecheck-fix 5 错误收口 |
| Epic | Epic 17 - V1.3.9 收口 + 强化 |
| Priority | 🔴 **P0**（阻塞 typecheck:ci gate · 所有 web-impl IMPL 阶段前置 · V1.3.9 灰度阶段 1 前置）|
| Complexity | **S**（Small · 1 文件新增 vite-env.d.ts + 1 yaml 文件补端点 + regen + 1 文件 2 处类型断言）|
| Repository Type | **web-impl**（单仓 · 但 yaml 改动跨 backend 仓）|
| AC 数 | 3 个主 AC（AC-13.7.1 vite-env.d.ts / AC-13.7.2 openapi.yaml Printers codegen / AC-13.7.3 GmSummary Dayjs 类型）|
| Depends On Stories | Sprint 10.1 ✅（codegen）+ Sprint 12.2 ✅（Printer 端点 ship）+ Sprint 10.5 ✅（.vue any 替换）|
| Related Stories | PM 巡检建议 #5 · 完全独立（除 web-impl 单仓）· parallel_group A |
| Provides APIs | 0 端点（补 Sprint 12.2 已 ship 端点的 codegen 链路）|
| 端点数 | **0** |
| 测例 | **0**（typecheck:ci + regen diff 验证）|
| Migration | 无（不动 DB schema）|
| Owner SM | 萧何 |
| Next Action | hand_to_architect → hand_to_dev（**Sprint 14 IMPL 起点 · 必须最先 ship**）|

---

## 1. PM 反馈来源

PM 巡检 `docs/orchestrix-pm-audit-2026-06-14.md` §7.1 修补建议 + pre-existing baseline 5 错误

### 1.1 PM 巡检发现

| # | 修补建议 | 责任 | 状态 |
|---|---------|------|------|
| 1 | 修订 `docs/prd.md` L15-17 "文档版本 → V1.3.9" | PO 范蠡 | 🟡 13.9 收口 |
| 2 | 在 `docs/prd.md` 末新增"## 0. V1.3.9 增量章节" | PO 范蠡 | 🟡 进行中 |
| 3 | 在 `docs/prd/2-需求.md` §G7 强化"操作工可见关联图纸" | PO 范蠡 | 🟡 进行中 |
| 4 | 在 `docs/architect-handoff.md` 新增 V1.3.9 增量段 | PO 范蠡 | 🟡 进行中 |
| **5** | **5 个 pre-existing typecheck 错误收口（vite-env.d.ts + Printers.vue + GmSummary.vue）** | **Sprint 14 13.7 收口** | 🟡 **待启动** |
| 6 | tsconfig strict 模式（全量 strict）| Sprint 10 backlog | V1.4 backlog · 13.7 仅 enum drift 对齐 |

### 1.2 当前实装状态

- ✅ Sprint 10.1 ✅（OpenAPI TypeScript codegen 集成 · 8 测例 PASS）
- ✅ Sprint 10.5 ✅（5 个 .vue any 类型替换 · 0 测例 typecheck 验证）
- ✅ Sprint 12.2 ✅（Printer 端点 6 端点 ship · 16 测例 PASS）
- ❌ **baseline 5 错误未收口** · typecheck:ci exit 非 0 · 阻塞 web-impl IMPL

---

## 2. V1.3.7/V1.3.8 现状继承

| # | 移交项 | 来源 Story | 13.7 用途 |
|---|--------|-----------|----------|
| 1 | **OpenAPI TypeScript codegen 集成** | Sprint 10.1 ✅ | 13.7 触发 regen 验证 Printers codegen 链路 |
| 2 | **`sys_printer` 表 + 6 端点** | Sprint 12.2 ✅ | 13.7 补 `openapi.yaml` Printers 端点的 codegen 链路（已 ship · 仅 codegen）|
| 3 | **`GmSummary.vue` 6 项指标 + trend_chart** | Sprint 4.3 ✅ | 13.7 修 L33/47 Dayjs 类型断言（数字/字符串类型）|
| 4 | **`vite + TypeScript` 基础** | web-impl 既有依赖 | 13.7 新增 `vite-env.d.ts` 声明 `import.meta.env` 类型 |
| 5 | **typecheck:ci gate** | V1.3.8 Sprint 8 | 13.7 ship 后 typecheck:ci exit 0 |

---

## 3. 5 个 pre-existing baseline 错误明细

### 3.1 错误清单

| # | 文件 | 行号 | 错误 | 修复 |
|---|------|------|------|------|
| 1 | `web-impl/src/api/http.ts` | L13 | 缺 `vite-env.d.ts`（`import.meta.env` 类型未声明）| 新增 `web-impl/src/vite-env.d.ts` 声明 `VITE_API_BASE_URL` + `VITE_USE_MOCK` + `VITE_APP_TITLE` |
| 2 | `web-impl/src/views/admin/Printers.vue` | L163-164 | codegen 失败（`openapi.yaml` 缺 Printer 端点）| `backend/spec/openapi.yaml` 补 `POST/GET/PUT/DELETE /api/v1/admin/printers`（Sprint 12.2 已 ship · 仅 codegen 链路补齐）|
| 3 | `web-impl/src/views/admin/Printers.vue` | L163-164 | 缺 Printer 类型导入 | codegen 后 `import type { Printer } from '@/api/generated/models/Printer'` |
| 4 | `web-impl/src/views/v138/GmSummary.vue` | L33 | `data.amountThresholdPassedRate * 100` · 数字/字符串类型 | `as number` 类型断言或 generated DTO 字段类型修正 |
| 5 | `web-impl/src/views/v138/GmSummary.vue` | L47 | `data.outsourceCostRatio * 100` · 同 #4 | 同 #4 |

### 3.2 修复示例

#### 修复 #1 · vite-env.d.ts 新增

```typescript
// web-impl/src/vite-env.d.ts (新增)
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_BASE_URL: string
  readonly VITE_USE_MOCK: 'true' | 'false'
  readonly VITE_APP_TITLE: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

#### 修复 #2-3 · openapi.yaml 补 Printers 端点 + codegen

```yaml
# backend/spec/openapi.yaml (补)
paths:
  /api/v1/admin/printers:
    get:
      tags: [admin]
      operationId: listPrinters
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Printer'
    post:
      tags: [admin]
      operationId: createPrinter
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrinterCreateRequest'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Printer'
  /api/v1/admin/printers/{id}:
    put:
      tags: [admin]
      operationId: updatePrinter
      ...
    delete:
      tags: [admin]
      operationId: deletePrinter
      ...
components:
  schemas:
    Printer:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        type: { type: string, enum: [NORMAL, LABEL] }
        ip: { type: string, nullable: true }
        port: { type: integer }
        protocol: { type: string, enum: [PDF_BROWSER, ZPL, TSPL] }
        modelSuggestion: { type: string, enum: [DELI_DL888B, ZEBRA_ZD420, TSC_TTP244PRO, OTHER] }
        enabled: { type: boolean }
    PrinterCreateRequest:
      type: object
      required: [name, type]
      properties:
        name: { type: string, minLength: 1, maxLength: 50 }
        type: { type: string, enum: [NORMAL, LABEL] }
        ...
```

```bash
# codegen 触发
cd web-impl
npm run gen:api  # 二次 regen
git diff --exit-code src/api/generated  # exit 0 验证
```

```typescript
// web-impl/src/views/admin/Printers.vue L163-164 (替换)
- import type { Printer } from '@/api/generated/models/Printer'  // 新增 codegen 类型
```

#### 修复 #4-5 · GmSummary.vue L33/47 Dayjs 类型断言

```vue
<!-- web-impl/src/views/v138/GmSummary.vue L33 (修复) -->
<el-statistic
  title="金额阈值审批通过率"
  :value="(Number(data.amountThresholdPassedRate) * 100).toFixed(1)"
  suffix="%"
/>

<!-- web-impl/src/views/v138/GmSummary.vue L47 (修复) -->
<el-statistic
  title="委外成本占比"
  :value="(Number(data.outsourceCostRatio) * 100).toFixed(1)"
  suffix="%"
/>
```

---

## 4. 端点契约

**0 端点** · 13.7 补 Sprint 12.2 已 ship 端点的 codegen 链路（`openapi.yaml` schema 描述）· 不新增端点。

---

## 5. Acceptance Criteria

### AC-13.7.1 — vite-env.d.ts 就位

**GIVEN** web-impl 已有 `src/api/http.ts` L13 `import.meta.env` 引用
**WHEN** dev agent 新增 `web-impl/src/vite-env.d.ts`
**THEN**：
- `import.meta.env.VITE_API_BASE_URL` 类型为 `string`
- `import.meta.env.VITE_USE_MOCK` 类型为 `'true' | 'false'`
- typecheck:ci 退出 0（无 `import.meta.env` 类型错误）

### AC-13.7.2 — openapi.yaml Printers codegen 链路补齐

**GIVEN** Sprint 12.2 ✅ Printer 端点 6 端点已 ship（backend 实装完成）
**WHEN** dev agent 在 `backend/spec/openapi.yaml` 补 Printers 端点 schema + regen
**THEN**：
- `npm run gen:api` 退出 0
- `src/api/generated/models/Printer.ts` 存在
- `src/api/generated/services/AdminPrinterService.ts` 存在（含 6 方法）
- `git diff --exit-code src/api/generated` 退出 0
- `Printers.vue` L163-164 替换本地 interface 为 generated 类型

### AC-13.7.3 — GmSummary.vue L33/47 Dayjs 类型断言

**GIVEN** `GmSummaryResponse` DTO 已 codegen（Sprint 4.3 + Sprint 10.1）
**WHEN** dev agent 在 L33/47 加 `Number()` 类型断言
**THEN**：
- `data.amountThresholdPassedRate * 100` 类型错误消失
- `data.outsourceCostRatio * 100` 类型错误消失
- typecheck:ci 退出 0

---

## 6. 数据库迁移

**无 Flyway 迁移** · 本 Story 不动 DB schema · 仅 web-impl typecheck 错误收口。

---

## 7. 测试设计

| 测例组 | 数量 | 验证方式 |
|--------|------|----------|
| TC-13.7.1 vite-env.d.ts | 1 | `cat vite-env.d.ts` 字段覆盖度 |
| TC-13.7.2 openapi.yaml Printers codegen | 1 | `npm run gen:api` 退出 0 + `git diff --exit-code` 退出 0 |
| TC-13.7.3 GmSummary Dayjs 类型 | 1 | typecheck:ci 退出 0 |
| **合计** | **3 验证项** | — |

> 注：3 项均为 **验证项**（非业务测例）· 通过 typecheck:ci + regen diff 一致性验证。

### 7.1 验证项详细

| ID | 场景 | 步骤 | 期望 |
|----|------|------|------|
| TC-13.7.1.1 | vite-env.d.ts 字段覆盖 | `cat web-impl/src/vite-env.d.ts` | 含 `VITE_API_BASE_URL` + `VITE_USE_MOCK` + `VITE_APP_TITLE` |
| TC-13.7.2.1 | codegen 链路 | `cd web-impl && npm run gen:api` | exit 0 |
| TC-13.7.2.2 | regen diff | `git diff --exit-code src/api/generated` | exit 0 |
| TC-13.7.2.3 | Printers.vue 替换 | L163-164 codegen 类型导入 | `import type { Printer } from '@/api/generated/models/Printer'` |
| TC-13.7.3.1 | typecheck:ci | `npm run typecheck:ci` | exit 0 |

---

## 8. 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| openapi.yaml 改动可能触发 Sprint 10.1 codegen 既有类型漂移 | 🟢 低 | dev 二次 regen + `git diff --exit-code` 拦截 + 0.5h 增量 |
| Sprint 12.2 已 ship 端点的 schema 描述与 backend 实装不一致 | 🟡 中 | dev 启动前先 `git log 12.2-printer-management.md` 同步字段 + 与 architect 评审 |
| GmSummary.vue Number() 断言可能掩盖 DTO 字段类型 bug | 🟢 低 | 后续 Sprint 14+ 启动 GmSummaryResponse DTO 类型修正（V1.3.10 backlog）|
| typecheck:ci gate 是 Sprint 14 IMPL 起点前置 · 不可逆 | 🟡 中 | 13.7 必须在 Sprint 14 IMPL 起点 ship · 其他 Story IMPL 必须在 13.7 ship 后启动 |
| V1.3.9 灰度阶段 1 启动前 13.7 必须 ship | 🟡 中 | 灰度阶段 1（6/30 admin + ENGINEER）启动前 · web-impl typecheck:ci 必须 clean |

---

## 9. 签字

- **PO 范蠡** · 2026-06-14 · Sprint 14 SHARD · P0 必做 · typecheck:ci gate
- **SM 萧何** · 2026-06-14 · 待启动 Sprint 14 IMPL · 顺序约束：13.7 是 Sprint 14 IMPL 起点 · 必须最先 ship
- **dev agent Opus 4.8** · 待启动 web-impl IMPL · 1 文件新增 + 1 yaml 文件补 + 1 文件 2 处断言
- **QA 商鞅** · 待 3 验证项 *test-design → *test-execute（typecheck:ci + regen diff）
- **architect 鲁班** · 待 *review · 重点 vite-env.d.ts 字段覆盖 + openapi.yaml Printers schema 完整性 + GmSummary 类型断言安全性
- **DevOps 张良** · 不涉及（仅 web-impl 单仓 + 1 yaml 跨 backend 仓）

**Story 13.7 P0 必做 · Sprint 14 IMPL 起点 · 0 端点 · 0 测例 · 0 Flyway · typecheck:ci gate · 12.1 灰度阶段 1 前置**
